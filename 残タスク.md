# 残タスク（統合版）

**作成: 2026-08-17。** 実装・DB・テストの実地確認と、散らばっていた申し送りファイルを1本にまとめたもの。
**「次に何をやるか」はこのファイルだけ見れば決まる**ようにしてある。理由や手順の詳細は各節から元ファイルへ飛ぶこと。

参照元: `TASKS.md` / `ideas/採点システム_残課題.md` / `ideas/セキュリティ_残課題.md` / `design/移植残タスク.md` / `design/定番機能リスト.md` / `ideas/問題作成ガイド.md`

> ### 作業を依頼するときは `tasks/` を使う
>
> **本ファイルは全体像の把握用。** 実際に手を動かすときは **`tasks/README.md`** から
> 該当のタスクを選び、**そのファイルだけを読ませれば着手できる**ようにしてある。
> 各タスクには、先に読むべき資料・依存関係・**判断を仰ぐべき箇所**が書いてある。

> 元ファイルには**すでに完了した項目が未着手のまま残っている**箇所がある。読むときの注意は §I。

---

## 現在地（2026-08-17 実測）

| | |
|---|---|
| テスト | 単体・結合 **657 passed / 63 skipped**（23ファイル・2026-08-19 実測。**E5・C6・E2・C3 の取り込み後**）。skip は実DB接続系（`RUN_DB_TESTS=1` で有効。そちらは **63 passed**） |
| E2E | **101 passed / 1 skipped / 0 failed**（全102件。2026-08-19・G2 で実行）。**`E-455` の2件は G2 で直り、メモ欄の4件（E-461・E-462 × 2幅）が増えた。** skip は E-111（C1 の後に戻す） |
| 投入済み問題 | **74件**（`order` 1〜72・78 + 動作確認用 `order=999`）。**第1章〜第9章は完了**（2026-08-19・A2 第1〜8バッチ）。読解型6種すべてが実採点済み |
| AI採点 1回 | 約 **¥0.04** / 応答 1.2〜4.3秒 / プロンプトキャッシュ命中率 97% |
| 異議申し立て | 稼働中（`problem_feedback` に **3件**記録済み） |
| 環境変数 | Supabase・OpenAI・`RESEND_API_KEY` は設定済み（Resend は C1 で投入。**アプリは読まず Supabase の SMTP 設定に入れる値**）。**Stripe の3つと `NEXT_PUBLIC_APP_URL` は空**（2026-08-19 に確認） |
| デプロイ | 未実施（ローカルのみ） |

ブラウザで「ログイン → ステージ選択 → 問題 → 採点 → リザルト → 異議申し立て → ログアウト」が通る。

---

## 進める順番

```
A. 問題コンテンツ（本線・唯一の停滞要因）
     │
     ├─ A-1 読解型6種の検証  ✅ 完了（2026-08-17）
     │        │
     │        ├─ B-1 ゴールデンセットで測る  ✅ 完了（2026-08-17）
     │        │        │
     │        │        └─→ B-3 core の判定基準を直す  候補1 ✅ 完了（2026-08-18）
     │        │                 ※ 残るは候補2（G15）。A-2 はここを待たなくてよい
     │        │
     │        └─ B-2 §11・§12 の実測  ✅ 完了（どちらも再現せず・対処不要）
     │
     └─ A-2 量産   ← **進行中。第1〜9章 完了（1〜72）・残り 27 問**（2026-08-19）
                       B-3 の候補1で `core` の緩さは塞がった。残る候補2は
                       プロンプトの判断基準の話で、`rubric_items` の書き方は変わらない

C. 公開前に必須 ─── デプロイしたい時点で着手。A/B とは独立
D. 課金開始前に必須 ─ 収益化の判断が出てから。C の後
E. 画面・体験 ───── いつでも（A-1 をブロックしていた E-4 は完了）
```

**いま着手すべきは A-2（問題の量産）。**
B-3 の候補1が入り（2026-08-18）、**合否が入れ替わる率は 10% → 0%** になった。
残る穴は候補2（G15・設問の軸に触れていない回答が63点でクリアする）1件だが、
これは**プロンプトの判断基準の話で、問題データの書き方には影響しない**ので A-2 を止めない。
C 以降は「公開する」と決めた時点で始めればよく、A/B と並行して困らない。

> **採点を触ったら必ず `npm run golden` を前後で回すこと**（各90回・約¥3.6）。
> 現行値は `p4`（`残課題` §8「実測 2回目」）。版が変わると過去の点数と混ざらなくなる。

---

## A. 問題コンテンツ（本線）

**唯一の実質的な停滞要因。** ブロッカーではなく作業量の問題。
構成は `ideas/問題構成案.md`（100問）、作り方は `ideas/問題作成ガイド.md`。

### A-1. 読解型6種の検証 ── ✅ **完了（2026-08-17）**

`depth` の判定条件は読解型ごとに切り替わる。**6種すべてが実採点を通った。**

| 読解型 | 投入済み | 状態 |
|---|---|---|
| トレース | 3件（+ 動作確認用1件） | ✅ 検証済み |
| ズレ | 2件 | ✅ 検証済み |
| 意図 | 1件（ステージ15） | ✅ 検証済み |
| 影響 | 1件（ステージ59） | ✅ 検証済み。**`problems.context` を使う唯一の問題** |
| 命名 | 1件（ステージ53） | ✅ 検証済み |
| 仕様 | 1件（ステージ78） | ✅ 検証済み |

- [x] `意図` 型を1問作って採点を通す
- [x] `命名` 型を1問作って採点を通す
- [x] `仕様` 型を1問作って採点を通す
- [x] `影響` 型を1問作って採点を通す

**4問とも「短くても正しければクリア」「結論が逆ならクリアしない」を満たした。**
①模範解答に近い回答では `depth` が付き、②短い正答では付かないので、
**読解型ごとの `depth` 判定条件が独立に働いていることを確認できた**（これが A-1 の目的）。

結果と気づいた点は `problems/stage-015-053-059-078.md`、経緯は `tasked/A1-読解型の検証.md`。

> **採点の物差しについて3点の観察を記録した。仕組みは触っていない**（A1 の方針）。
> **B-3 で判断すること。** 要点は「ステージ78 の短い正答が 92点（パーフェクト帯）に入る」
> 「`none` / `partial` でも `evidence` が入ってくる」「同じ引用を複数の観点に使い回している」。

### A-2. 量産 ── **進行中。第1〜8バッチ（6〜72）完了（2026-08-19）**

**第1章〜第9章が埋まった。73 / 100 問。** 経緯と判断は `tasks/A2-問題の量産.md` の作業記録。

| バッチ | 範囲 | 記録 | ブラウザで実採点 |
|---|---|---|---|
| 第1 | 6〜14（第1章の残り + 第2章） | `problems/stage-006-014.md` | 6・9・14 |
| 第2 | 16〜24（第3章。15 は A1 済み） | `problems/stage-016-024.md` | 17・21・23 |
| 第3 | 25〜29（第4章） | `problems/stage-025-029.md` | 26・28 |
| 第4 | 30〜40（第5章） | `problems/stage-030-040.md` | 33・35・36 |
| 第5 | 41〜48（第6章） | `problems/stage-041-048.md` | 42・44・48 |
| 第6 | 49〜52・54（第7章。53 は A1 済み） | `problems/stage-049-054.md` | 51・52・54 |
| 第7 | 55〜58（第8章。59 は A1 済み） | `problems/stage-055-058.md` | 56・57 |
| 第8 | 60〜72（第9章・最大の章） | `problems/stage-060-072.md` | 61・65・72 |

- [x] **64問を投入。** 全バッチで `npm run test:db` 63 passed。
  ブラウザで解いた22問すべてで**「短くても正しければ通る」22/22・「結論が逆なら通らない」22/22**
  - オーナー判断: **前提知識は全問に付ける** / **章ごとにまとめて作る** / **投入は担当が直接行う**（中身を見せて確認後）
  - **投入前の機械検査 `problems/preflight.mjs` を常設化した**（A1 の申し送り3）。
    `compose.ts` の実関数を呼ぶので採点側が変われば追随する。次のバッチでも必ず回すこと
  - **`problems/run-code.mjs` でコードを実際に実行して確かめること。** トレース型は
    模範解答の出力が違うと問題ごと壊れるが、機械検査は書式しか見ない
  - ⚠️ **票の冒頭の数字が古かった**（「残り94問」と書かれていたが実際は91問）。着手時に実データで確かめること
- [ ] **残り 27 問。次は第10章（73〜77・79・80 の7問）。** 78 は A1 で投入済み。
  **80（総合演習）は 30（破壊するメソッド）の回収先**、**79 は4問目の影響型**。
  ⚠️ 飛び番があるので `gen-sql.mjs` を範囲指定しないこと。
  螺旋は3本回収済み（28 ←4・5 ／ 33 ←17 ／ 36 ←4）。残る4本（72・96・97・80）は
  **すべて回収元が先に来る並びなので、順番に作れば自然に満たせる**
- [x] ⚠️ **「短いが正しい回答」がパーフェクト帯（80点以上）に入りやすい ── 材料は溜まった。判断は §B-3 で**
  A2 でブラウザ実採点した**11問のうち9問**が 80点以上（92・63・86 / 92・92・63 / 92・83 / 86・89・83）。
  **合否は 11/11 正しい。** 帯に入らなかった2件はどちらも `core` だけ `full` の 63点で、
  **`ground` が付くかどうかが分かれ目**になっている（11件中9件で `ground=full`）。
  設問が「理由も説明してください」と促すので1文でも場所を指す語が入るため、
  **問題側の書き方ではなく設問の形と `ground` の条件の噛み合わせ。**
  → **A2 側でこれ以上材料を足す意味は薄い。§B-3「パーフェクト帯が42%」で判断すること**
- [ ] ⚠️ **採点キーワードがタイトルからも写せる**（A2 第1バッチで発見）。
  `I-802` は設問文しか見ていないが、タイトルも同じ画面に出る。既存9問中4問・第1バッチ9問中5問が該当。
  **`preflight.mjs` では警告どまりにしてある**（既存がこの作り方で、ここで必須にすると
  新旧で基準が変わるため）。実害は限定的（引用の無い回答は層1が10点で頭打ちになりクリアに届かない）で、
  **塞ぐと最も自然な正答の言い回しがキーワードを外す**副作用がある。→ **§B-3 で判断すること**
- [ ] 動作確認用の問題（id=5・`order=999`）を残すか消すか判断
  → **残すことを推奨。** 5パターンの期待値が分かっているので「採点が壊れたのか問題データが悪いのか」の切り分けに使える（`ideas/採点システム_残課題.md` §7）

---

## B. 採点の信頼性

**すべて「測ってから直す」。推測で直すと悪化しても気づけない。**
詳細は `ideas/採点システム_残課題.md`。

### B-1. ゴールデンセットでブレを測る ── ✅ **完了（2026-08-17）**

- [x] **50件のゴールデンセットを組んで回す**
  → `npm run golden`（1周 90回 / 約¥3.6 / 約40秒）。回答文は `tests/golden/cases.json`（**git 管理外**）

| 指標 | 目標 | 実測（`p3`） | |
|---|---|---|---|
| 反転誤答の**誤合格率** | 0% | **0%（0/10）** | ✅ |
| 短いが正しい回答の**合格率** | 100% | **100%（10/10）** | ✅ |
| 想定との一致率（50件） | — | **96%（48/50）** | |
| 合否が入れ替わる率（同一回答5回・閾値付近10件） | 低いほどよい | **10%（1/10）** | 初回測定 |
| パーフェクト帯（80点以上） | 目安 15〜25% | **46%（23/50）** | ⚠️ |

**外れた2件はどちらも「不合格のはずがクリアした」。** 原因は `core` が緩いこと1点で、
うち1件は5回すべてクリアする（揺れではなく基準そのもの）。**B-3 の対象がここに絞れた。**
経緯は `tasked/B1-採点のブレを測る.md`、回答文と点数の対応は `ideas/採点システム_残課題.md` §8。

> **B-3 の前後で必ず `npm run golden` を回すこと。** 上の数字は `p3` のもの。
> プロンプトを触ると版が上がるので、**触る前に一度回して現行値を確認してから**変更する。

### B-2. プロンプト対処が効いているかの実測 ── ✅ **完了（2026-08-17）。どちらも再現せず**

- [x] **数値だけの引用を禁じた指示にモデルが従っているか**（`残課題` §11）
  → 数値だけが誤っている反転誤答4件を測り、**4件すべて矛盾検出・不合格。** 指示は守られている。
  **加えて §11 の症状の記述自体が古い** ── 引用照合が取れない側にも層1の頭打ちが入ったため、
  上限50点でクリア閾値55に届かない。**「60点でクリアしてしまう」経路はもう存在しない**
- [x] **箇条書き回答が捏造検出を誤爆させていないか**（`残課題` §12）
  → 箇条書き5件で**引用の格下げ0件・誤爆なし。** `FABRICATION_DEMOTE_THRESHOLD` は触らなくてよい。
  ただし母数は5件（列挙型の設問が order 4 の1問しかない）。**A-2 でその形式が増えたら測り直す**

### B-3. 判定基準の見直し（測ってから）── **候補1は完了（2026-08-18）。残り2件**

- [x] **`core` が緩い（候補1・「エラーが出ると思います」型）** ✅ **完了（2026-08-18・`p3` → `p4`）**
  プロンプトの `# 校正の基準例` に判断例を1ブロック追加しただけ。**`compose.ts` は1行も触っていない**
  （配点・重み・クリア閾値はすべて変更なし）。前後で `npm run golden` を回して確認した。

  | 指標 | 変更前 `p3` | 変更後 `p4` |
  |---|---|---|
  | **合否が入れ替わる率** | 10%（1/10） | **0%（0/10）** |
  | G05「エラーが出ると思います」の5回 | 34/**60**/34/34/**60** | **34 ×5**（`core=partial` 固定） |
  | 反転誤答の誤合格率 | 0% | **0%**（維持） |
  | 短い正答の合格率 | 100% | **100%**（維持） |
  | パーフェクト帯 | 46% | 42% |

  経緯と判断は `tasks/B3-判定基準の調整.md` の作業記録、数字は `残課題` §8「実測 2回目」
- [ ] **`core` が緩い（候補2・設問の軸に触れていない型）** ← **唯一残った採点の穴**
  G15（合計値だけ述べてログの食い違いに触れていない回答）は **候補1 の後も 63点 ×5 でクリアする。**
  揺れではなく判定基準そのもの。直すなら「`core` は【採点観点】の条件文と照合して判定する」と明示する。
  **入れるなら単独で。** 採点全体が厳しくなる向きなので「短い正答 100%」を必ず測り直すこと
  （2026-08-18 オーナー判断で今回は見送り）
- [ ] `articulation` が厳しすぎる疑い — **2回の測定でその傾向は出ていない。** 優先度を下げてよい
- [ ] パーフェクト帯が 42%（目安15〜25%）。`ground` が付きやすい。**合否は正しいので候補2の次**

### B-4. 分析材料を貯める

- [x] **どの誤読に当たったかを記録**（`matched_reject`）— `残課題` §3
  ✅ **実装済み（2026-08-17・`tasks/B2`）。** `axes.matched_reject` に `"none"` / `"1"`〜`"3"` を保存する。
  実データで確認済み（誤読 →「1」、正答 →「none」）。**番号は3までなので、
  `core_reject` が4件以上の問題を作るときは3ファイルを同時に広げること**（B2 の作業記録）
- [x] **フィードバックが「どの行を見ればよいか」を示すようにする** — `残課題` §4
  ✅ **実装済み（2026-08-17・`tasks/B2`）。** 読み違いを検出したときは、その読み方が
  成り立たなくなる箇所を必ず示させる。実測で「5行目の rate = 0.8 に注目すると〜」が出ている
- [x] ⚠️ **上2件で `PROMPT_VERSION` が p2 → p3 に上がった。** B1 のゴールデンセットは
  p3 で回し、p2 時代の実測値と混ぜないこと
  → **`p3` で測った**（2026-08-17・B1）。p2 の A-1 実測とは別物として扱っている。
  なお `p2` → `p3` で**判定は緩む方向に動いた** ── p2 では 29/53 で不合格だった回答が、
  p3 では 34/60 で**クリアするようになっている**（B-1 の「外れた2件」の1件）

---

## C. 公開（デプロイ）前に必須

### C-1. これが無いと実ユーザーが登録できない

- [x] **メール送信を Resend に切り替える** ── ✅ **仕組みは完成（2026-08-17）**
  Resend 経由の送信・日本語の文面・**メールのリンクからのログインまで実測で通した。**
  `RESEND_API_KEY` に値が入り、Supabase の SMTP 設定も済んでいる。経緯は `tasked/C1-メール送信をResendに切り替える.md` の作業記録
  - 済: 確認メールのリンクを**端末をまたいで開ける形**にした（`app/auth/callback/route.ts` に `token_hash` 方式を追加。
    従来の `code` 方式だと「パソコンで登録 → スマホでメールを開く」でログインできない）。**実測で確認済み**
  - 済: 日本語の文面（`supabase/templates/confirm-signup.html`）と、
    それを API で反映するスクリプト（`supabase/templates/apply.mjs`。管理画面への貼り付けは文字化けの事故が起きたため）
  - 済: `supabase/config.toml` の `email_sent` を本番の実値（30）に合わせた。
    **`2` のままだと `config push` で本番の送信上限が1時間2通に戻る**地雷だった
- [ ] **⚠️ ただし実ユーザーにはまだ届かない → `C-6`（下）。** 送信元がお試しドメインのため
  **iCloud が拒否した**（`Blocked due to content`）。件名の日本語化も C-6 に持ち越し（`apply.mjs` の実行で済む）

### C-6. 独自ドメイン ── **判断は全部済んだ（2026-08-18）。ドメインの購入待ち**

**先送りできない項目だと 2026-08-17 の実測で確定した。** お試しドメインからのメールは iCloud が拒否する
（`Blocked due to content`）。原因は2つあり、**デプロイで消えるのは片方だけ**
── リンクが localhost を指している側はデプロイで解決するが、
**差出人が共用のお試しドメインである側は Resend でのドメイン認証でしか解決しない**
（`*.vercel.app` では代用できない。DNS を触る必要があり、あれは Vercel のもの）。

> ### ✅ 2026-08-18 にオーナーが決めたこと（4件）
>
> | | 決定 |
> |---|---|
> | ドメイン名 | **`ferretcode.com`** |
> | 取得先 | **ムームードメイン**（初年度770円・更新 1,728円/年）。**銀行振込・3年まとめて契約**（自動更新が使えないため）|
> | 差出人 | **`noreply@ferretcode.com`** |
> | TLD | **`.com`**（`.dev` は受信側のスパム判定が厳しく出ることがあり、目的と逆方向） |
>
> **`ferret` 単体は `.io` `.dev` `.app` `.codes` `.tools` `.sh` がすべて取得済みで、
> 加えて日本語圏に同名の先客がいる**（`ferret-plus.com` =「マーケターのよりどころ ferretメディア」）。
> **オーナーがクレジットカードを持っていないため、Cloudflare（登録料がカードのみ）に続いて
> Xserverドメインも外れた** ── 0円特典の商品は申し込み画面で本人確認としてカード登録を要求される。
> **公式の支払い方法一覧には但し書きが無いので、一覧だけでは判断できない**（実測で判明）。
> 理由の全文と、実行する手順は `tasks/C6-独自ドメインを用意する.md` の作業記録。

- [x] **`supabase/config.toml` を本番URLに直した**（2026-08-18・C6）
  `site_url` を `https://ferretcode.com` に、`additional_redirect_urls` に `http://localhost:3000` を追加。
  **アプリのコードは1行も触っていない**（文面のリンクは `{{ .SiteURL }}` を見ている）。
  ⚠️ **`supabase config push` はドメイン取得と Resend の認証が終わるまで、まだ実行しないこと。**
  いま push すると本番の確認メールが「まだ存在しないドメイン」を指す
- [ ] **ドメインを買う**（オーナー・支払い待ち。ペイジー推奨。**自動更新をONにする**）
- [ ] **Resend にドメインを登録して `Verified` にする**
  ⚠️ **ムームーDNS で一番間違えるのはサブドメインの欄。** `send.ferretcode.com` ではなく `send` だけを入れる
  （票の手順3）。MX の値は末尾のドットまで貼る。**反映に1時間かかる**
- [ ] **Supabase の Sender email と Site URL を変える**（票の手順5・6）
- [ ] **`node supabase/templates/apply.mjs` を実行**（C1 が残した件名の日本語化もこれで片付く）
- [ ] **iCloud と Gmail への実配信を確認する** ← **これが C-6 の受け入れ条件**

> **リンクを踏んでログインできるところまでは C-4（デプロイ）の後でないと確認できない。**
> ドメインを取っただけでは `https://ferretcode.com` に中身が無いため。
> C-6 の条件が「届くこと」なのはその意味で正しく、「踏めること」は C-4 側で見る。

### C-2. 法務・信頼

- [x] **利用規約・プライバシーポリシー** ── ✅ **下書きと画面は完成（2026-08-18・C2）**
  `/terms` `/privacy`（ログイン不要・静的）。タイトル・ログインのフッター、新規登録の同意文、
  問題画面の OpenAI 注記の「くわしく」から辿れる。運営者表記と連絡先は `lib/legal.ts`。
  オーナー判断: 回答文は改善に使うと明記 / 退会時はデータを全部消す / 特商法の表記は D2 と同時
  経緯・判断の理由・確認した内容は `tasked/C2-法務文書をそろえる.md` の作業記録。
  **残りは公開ゲート（§C-4 の冒頭）にまとめてある** ── 退会機能と E2E の実行は 2026-08-19・C3 で済み、
  **`E-455` も 2026-08-19・G2 で解消したので、残るのは専門家の確認だけ。**
  - [ ] ⚠️ **本文は AI の下書きで、専門家の確認を受けていない**（→ §C-4 ゲート 1）
  - [ ] ⚠️ **住所を載せていない**（個人事業主の自宅になるため）。D-3 の特商法表記と同時に見直す
- [x] **退会（アカウント削除）** ── ✅ **完了（2026-08-19・C3）**
  `/settings` の退会と `app/api/account/delete/route.ts`。確認は**「削除」の入力とパスワードの再入力の両方**
  （オーナー判断）。消す範囲は文書どおり（回答・採点結果・進行状況・異議申し立て）で、
  **子 → 親の順に明示的に消す**（外部キーの cascade 設定に依存しない。初期スキーマの cascade は未記録だった）。
  **使い捨てアカウントで実地確認済み** ── ログイン情報・`public.users` の行・回答が消え、ログインできなくなる（E-707）。
  ⚠️ **D-2 への申し送り: `subscriptions` の行を消すことは Stripe の解約ではない。**
  決済を入れたら「消す前に解約する」処理を足すこと（忘れると退会後も課金が続く）
- [x] パスワード変更 ── ✅ **完了（2026-08-19・C3）**
  **いまのパスワードを先に確認してから差し替える**（放置した端末からの乗っ取りを防ぐ）。
  `supabase/config.toml` の `secure_password_change` も true にしたが、**反映は `config push` 待ち**
  （`site_url` が localhost のまま）。いま守っているのはアプリ側
- [ ] メール変更（**C-1 と C-6 の後**。確認メール送信が要る）
  → 画面には「準備中」のチップだけ置いてある（2026-08-19・C3。押しても何も起きない機能を置かないため）

### C-3. Supabase の設定（管理画面でしかできない）── **2026-08-18・C4 実施。1件だけ残った**

- [ ] **流出パスワードの拒否（`password_hibp_enabled`）を ON** ← ここだけやれば足りる
  Authentication → Sign In / Providers → Password。**コードでは絶対に塞げない**（登録はブラウザから Supabase Auth へ直接飛ぶため）。
  総当たりは Supabase 側の制限で既に止まっているが、**他所で流出したパスワードの使い回し**はこれでしか止まらない
  → ❌ **2026-08-18 に着手して有効化できなかった。契約プランで選択できない**（`tasked/C4-...md`）。
  **有効化にはプランの引き上げが要る。** 効いてくるのは課金開始後（乗っ取られたアカウントで
  AI採点を回されると原価がこちら持ちで出る）なので、期限は **D-1 に着手する時点**。
  C-5（デプロイ）のブロッカーではないが、**チェックリストから外さないこと**
- [x] Minimum password length を 8 に（付随）
  → ✅ **2026-08-18 に管理画面で実施。6 → 8。** 7文字が `weak_password` で拒否されることを実測。
  画面の案内（`app/register/page.tsx` の「8文字以上」・`lib/auth/errors.ts`）は先に8で書かれていたので、
  **ここで初めて案内と実際が一致した**（それまでは7文字が通っていた）
- [x] 認証エンドポイントのレート制限を確認
  → ✅ **2026-08-18 に確認。全項目 30（既定値のまま）。変更なし**

> **上の2件は「効かないほうだけが入った」状態。** 実際に来る攻撃（他所で流出した
> メアド＋パスワードの使い回し）を止められるのは `password_hibp_enabled` だけで、
> 文字数はもともと付随の項目だった。詳細は `ideas/セキュリティ_残課題.md` §1-2。

> ⚠️ **`supabase config push` はまだ実行しないこと。** `site_url` が `http://127.0.0.1:3000` のままで、push すると**本番の確認メールのリンクが localhost を指す**。部分適用のオプションは CLI に無い。

### C-7. 登録済みメールの案内文 ── **C4 から起票（2026-08-18）**

- [x] **確認メール送信後の画面に一文足す** ── ✅ **完了（2026-08-19・C3 と同梱）**
  文面は「すでにご登録のあるメールアドレスの場合、メールは届きません。そのままログイン画面へお進みください。」
  **`lib/auth/errors.ts` の登録済みの文面とは別の言い方にしてある** ── 同じ文にすると
  E2E の `E-112`（アカウントの有無を伏せる）が落ちるため。経緯は `tasked/C7-...md`
  背景: 登録済みのメールでもう一度登録すると、画面は「確認メールを送りました」と出るのに**メールは届かない**
  （上の §H 参照。Supabase がアカウントの有無を伏せるため）。**利用者は届かないメールを待ち続けていた。**
  全員に同じ文面を出すだけなので、伏せている設計は崩れない

### C-4. デプロイ

> ## 🚧 公開ゲート（済むまで公開しない）
>
> **C2（法務文書・2026-08-18 完了）から出た申し送り。** どれも「コードは通るが、
> 公開したときだけ問題になる」種類なので、テストでもビルドでも捕まらない。
> **2026-08-19・C3 で 2 と 3 が済み、3 の実行で見つかった 4 は同日 G2 で解消。残っているのは 1 だけ。**
>
> | | やること | 済まないまま公開すると |
> |---|---|---|
> | 1 | **法務文書に専門家の確認を受ける** | `/terms` `/privacy` の本文は AI の下書き。事業の実態と食い違う条文が残ったまま人のデータを預かることになる。見てもらう点5つは `tasked/C2-法務文書をそろえる.md` の作業記録 |
> | 2 | ~~**退会機能を入れる**~~ | ✅ **済（2026-08-19・C3）。** 退会でログイン情報・回答・進行状況が消えることを実地確認した（E-707）。規約第12条・ポリシー第7条に書いてあることが実行できる状態になった |
> | 3 | ~~**`npm run test:e2e` を1回通す**~~ | ✅ **実行した（2026-08-19・C3）。97件中 94 passed / 2 failed / 1 skipped。** C2 の `E-457`〜`E-460` は**4件すべて通過**。ただし下の 4 が出た |
> | 4 | ~~**`E-455` の落ちを直す**~~ | ✅ **済（2026-08-19・G2 に同梱）。** 推奨どおり**注記を DOM 上でボタンの後ろに置き、見た目は flex の `order` で保った**（テストは緩めていない）。読み上げ順が変わる点は回答欄の `aria-describedby` で補い、注記が**書き始める前に**届くようになった。chromium・mobile の両方で緑 |
>
> ついでに見直すもの（公開を止めるほどではない）:
> - **`lib/legal.ts` の連絡先が公開リポジトリに載っている**（個人の iCloud アドレス）。
>   法務文書として公開は必要だが、サービス用アドレスに変えるなら**早いほうが履歴に残る量が少ない**
> - **住所を載せていない。** D-3（特商法の表記）と同時に、ポリシーの公表事項も見直す

- [ ] Vercel デプロイ設定
- [ ] 本番環境の環境変数設定
- [ ] **`NEXT_PUBLIC_APP_URL` は Production 環境にだけ設定する**（例 `https://ferret.io`）。
  **Preview には設定しない** — するとプレビューからログインした人が本番へ飛ぶ
- [ ] `site_url` / `additional_redirect_urls` を本番URLに（ドメイン確定後）
- [ ] `.env.local.example` の最終確認

### C-5. 公開前に消すもの

- [ ] **開発用アカウントのパスワードが公開リポジトリに載っている**
  `TASKS.md:111` に `dev@ferret.test` / `FerretDev2026!` が平文で記載。リポジトリは Public。
  ローカル運用中は実害ないが、デプロイすると**パスワードが公開されたアカウントが本番に存在する**状態になる。
  デプロイ前にパスワードを変えるか、記載を伏せること

---

## D. 課金開始前に必須

### D-1. レート制限（プラン別のAI採点上限）

**現在 `/api/score` はプランを見ずに無制限で採点する。** 実装の目印は `app/api/score/route.ts:484` の TODO。

- [ ] Free 1日3問（JST 深夜0時リセット）
- [ ] Pro 月200問 / Pro Plus 月500問
- [ ] `users.plan` の参照ロジック
- [ ] 超過時は層2をスキップし `is_provisional = true` + `scoring_method = 'keyword_only'` で保存
  **層1は最大20点でクリア閾値55に届かないため、不合格ではなく「判定保留」として扱う**（そうしないと上限到達ユーザーに理由不明の全問不合格が並ぶ）
- [ ] 制限超過時のUI表示
- [ ] 「きょうの AI 採点」ウィジェット（右レール・肉球3つ）— **制限の強制と同時に**。
  強制なしで残数だけ出すと「0になっても採点できる」矛盾表示になる

> いま入っている `RATE_LIMITS`（1分10件 / 1時間60件 / 24時間300件）は**使いすぎを止める安全網**であって商品上限ではない。**消さずに、その内側にプラン上限を足すこと。**
> カウントのクエリは `ideas/db仕様.md`「レート制限のクエリ」。リプレイと判定保留は無料枠を消費しない。

> ### ⚠️ 数えているのは「行の有無」であって「採点したかどうか」ではない
>
> **A2 の作業中に実際に踏んだ**（2026-08-19）。`RATE_LIMITS` は `user_attempts` の
> `created_at` を直近24時間ぶん読んで数えるだけなので、**採点を経由せずに入れた行も枠を食う。**
> ステージ解放のために印つきの行を14件まとめて入れたところ、
> **その直後の本命の採点1回目が 429 で弾かれた。**
>
> A2 側は印つきの行の日時を1週間前にずらして回避した（`problems/unlock-seed.mjs`）。
> **プラン上限を実装するときは、何を1回と数えるのかを先に決めること。**
> 少なくとも次の3つは「採点1回」ではないが、素直に行数を数えると1回として計上される。
>
> | 行 | 実態 |
> |---|---|
> | リプレイ（同一回答の再送） | API を呼んでいない。原価0 |
> | 判定保留（`is_provisional = true`） | 層2を回していない。上限超過の結果そのもの |
> | 動作確認・解放のために入れた行 | 人が手で入れたもの |
>
> 上2つは上の項目でも「無料枠を消費しない」と決めてあるが、
> **安全網（`RATE_LIMITS`）のほうは現状どれも区別していない。**
> プラン上限を足すときに、安全網の側も同じ数え方にそろえるのか別のままにするのかを決めること
> （E2 セッションからの申し送り・2026-08-19）。

### D-2. Stripe

- [ ] アカウント設定・価格プラン作成
- [ ] `stripe` パッケージインストール
- [ ] Checkout セッション生成（Pro アップグレード）
- [ ] **Webhook 実装（`/api/webhooks/stripe`）**
  ⚠️ **`stripe.webhooks.constructEvent()` による署名検証を最初に書く。** 無いと誰でも「支払い完了」の JSON を投げるだけで Pro になれる。
  署名検証には**生のリクエストボディ**が必要なので `request.json()` ではなく `request.text()` を使う。
  Webhook は Stripe からのサーバー間通信なので `isCrossSiteRequest` は通す設計でよい
- [ ] 駆け込みパック（単発購入）フロー
- [ ] 右レールの「Pro をみる」ボタンを有効化（現在は準備中で押せない）

### D-3. 課金に伴う法務

- [ ] 特定商取引法に基づく表記
- [ ] 解約導線（Stripe Customer Portal が最安）

---

## E. 画面・体験

### E-1. 画面そのものが無いもの

- [ ] **振り返り画面（`/review/[id]`）** — `user_attempts.axes` に4観点の判定と引用が入っているので**材料は揃っている**
- [ ] せってい画面（サイドバーに「準備中」で置いてある）

### E-2. データが足りなくて出せていないもの

- [x] **フィードバックの2枠表示**（よかったところ / つぎの一歩）✅ **実装済み（2026-08-18・`tasks/E2`）。**
  `user_attempts` に `ai_praise` / `ai_next_focus` を追加（マイグレーション適用済み）。
  **採点の計算・プロンプト・`grader_version` は触っていない。** リザルトは2枠で表示し、
  片方が空の回はその枠を出さない。**リプレイ経路でも2欄を複製する**（ここが抜けると再送で1枠に戻る）
  - オーナー判断: **欄を2つ追加**（JSONB 化はしない。古い形と新しい形の混在が画面の複雑さとして残るため）／
    **過去の分も分け直す。ただし再採点はしない** ── 実データを見たところ保存済みの文章を機械的に2つへ戻せたため、
    **費用ゼロ・点数を1点も動かさず**に復元した（再採点だと点が揺れてレベルとクリア状態が後から動く）
  - 復元は `supabase/backfill/20260818224500_split_feedback.mjs`（冪等・`--apply` で書き込み）。
    **42件中40件を復元、2件は文章を2項目に分ける前の行なので1枠のまま**（どちらも画面には出ない行）
  - ⚠️ **E2E は回せていない**（ブラウザが別セッションで使用中・`fixtures.ts` が共有DBの回答ログを削除するため）
    → ✅ **2026-08-19・C3 が E2 を取り込んだ状態で全98件を回した。** リザルト画面まわりを含めて
    E2 由来の落ちは無し（落ちた2件は `E-455` で C2 由来の既存分）
  → 詳細と判断の理由は `tasks/E2-フィードバックの2枠表示.md` の作業記録
- [x] **XP バー・「+10 XP」表示・レベル** ✅ **実装済み（2026-08-18・`tasks/E3`）。**
  オーナー判断: **クリア +10 / パーフェクト +15**（点数に応じて）・**1レベル = クリア10問ぶん**・
  **上がっても表示が変わるだけ**（段位・認定証には繋げない）・画面での呼び方は**「レベル」**。
  - **`users.xp` は使っていない。** 加算する形をやめ、`user_attempts` から毎回導出する
    （`lib/progress/level.ts`。ストリークと同じ方針）。カウンタ更新の失敗によるズレが
    構造的に起きず、ユーザーに XP を書かせる経路も無い。**採点API（`app/api/score/route.ts`）は1行も変えていない**
  - **問題ごとの最高点だけを見る。** 同じ問題を解き直しても点が伸びなければ増えないので、
    解き直しが稼ぎ場にならない（1回ごとに加算する形だと、リプレイの再送でも増えてしまう）
  - リザルトに XP バー（レベルをまたぐ回は 満たす → 空にする → 新しいぶんを満たす）、
    ステージ選択の右レールに「レベル」のカード。**問い合わせは1本増えただけ**（ステージ側は0本）
  - **`users.xp` カラムは残してある**（消すには `handle_new_user` トリガーの変更が要る）。
    使っていないことは `ideas/db仕様.md` に明記した
- [x] **マップのパーフェクト演出** ✅ **実装済み（2026-08-17・`tasks/E7`）。オーナー判断で案A に確定（2026-08-18）。**
  80点以上のクリア済みノードに金の縁取り（案A）を一重足す。星マーク・発光は**見送り**。
  基準は `lib/ai/compose.ts` の `PERFECT_THRESHOLD` を画面から読む。
  判定は `Stage.perfect` の独立した欄（`status` の4つ目にすると `=== "cleared"` の判定が全部取りこぼす）

### E-3. 見た目

- [x] **Shiki シンタックスハイライト** ✅ **実装済み（2026-08-18・`tasks/E5`）。オーナー判断で既製テーマに確定。**
  カスタムテーマは作らず、既製40種のうち**モックの色といちばん近い `gruvbox-dark-hard`** を選んだ（色差で機械的に比較）。
  色付けはサーバー側（`lib/code/highlight.ts`）で終わり、**ブラウザ側の配布物には Shiki が乗っていない**（実測）。
  パネルの背景・地の文（`globals.css` の `--color-code-bg` / `--color-code-ink`）は**テーマの色に合わせて置き換えた**。
  実行結果（`context`）の枠には色を付けていない（エラー文はコードではないため）。
  → 詳細と判断の理由は `tasked/E5-コードの色付け.md` の作業記録
- [ ] モバイルレイアウト（3カラムは lg 以上のみ。lg 未満は簡易ヘッダーで代用中）
- [x] **0点表示の見直し** ✅ **実装済み（2026-08-18・`tasks/E6`）。オーナー判断で案A（読み違いのときだけ）に確定。**
  読み違い（`user_attempts.contradiction`）を検出した回だけ、文章（フェレットのメモ）を上・大きく、
  点数は1行に畳んで下に置く。**点数は隠していない。** クリア時・その他の低得点帯の見た目は変えていない。
  巨大な「0」自体はデザイン移植の時点で統計チップ3枚に分割済みだった（票が書かれた時点より状況が進んでいた）。
  → 詳細と判断の理由は `tasks/E6-0点の見せ方.md` の作業記録
- [ ] ダークテーマ切替（`design/stages-desktop-dark.html` の `:root` トークンを `[data-theme="dark"]` として足すだけの構造にしてある）
- [ ] **マスコットに Lottie の動きを付ける**（**入れる方針に確定**・2026-08-18・F2）→ **`tasks/E10-マスコットの動きを付ける.md`**
  現状は `app/globals.css` の CSS アニメーション7種（`float` / `sniff` / `pop` / `halo` / `bob` / `blink` / `fall`）で動いている。
  **前提は素材（アニメーションのデータ）を用意できること。** 仕組みだけ入れても再生するものが無いので、票の冒頭に確認事項として書いてある

### E-4. 影響型の2パネル表示 ── ✅ **完了（2026-08-17）**

- [x] **コードと実行ログを分けて表示する構造を決める**
  `problems.context`（TEXT・NULL 可）を新設した。code 内の区切り記法は採らない（書き忘れると壊れる）。
  入っている問題だけコードの下に2枠目が出る。**A-1 の影響型はこれで着手できる**
- [x] **前提知識（旧 G-4）も同じマイグレーションで足した** — `problems.prerequisite`（400字以内・NULL 可）。
  「わからない言葉があるとき」を押すと開く。既存5問には投入済み（`problems/prerequisite-001-005.sql`）
  → 詳細と判断の理由は `tasked/E4-問題データに欄を足す.md` の作業記録

---

## F. その他・技術的負債

- [x] `middleware.ts` → `proxy.ts` への移行（**2026-08-17 完了・F1**）。非推奨警告は消えた。処理は変えていない。ドキュメントの参照もすべて追随済み
- [x] **ブラウザの通しテスト（E2E）が全件落ちている** ── ✅ **完了（2026-08-18・F3）**
  **76 passed / 1 skipped。アプリのコードは1行も触っていない**（画面が正しく、期待値が古かった）。
  原因は3つで、うち2つはタスク票に書いていなかったもの。
  ①アサーションがデザイン移植（`943345a`）に追いついていない（見出し・マップのアイコン・リザルトの点数表示）
  ②`baseURL` の `127.0.0.1` は **`1116f81` ですでに直っていた**（票の記述が古い。`localhost` からは警告0件と実測）
  ③**シード問題が解放されず 404**（実コンテンツが増えてマップの末尾に沈んだ。E4 の申し送りが残していた選択）。
  詳細と判断の理由は `tasked/F3-E2Eの古いアサーションを直す.md` の作業記録。
  **`E-112` と `E-272` で決定と実装のずれが見つかっている**（それぞれ C4・E6 で判断すること）
- [x] **shadcn/ui と Lottie を入れるか判断** ── ✅ **完了（2026-08-18・F2）**
  - **shadcn/ui は見送り** → §H に理由つきで移した。`design/` でトークンを設計し、
    6画面ぶんの部品を Tailwind 直書きで作り終えているため、導入の目的（部品を一から作らずに済ませる）が消えている
  - **Lottie は入れる**（オーナー判断）→ §E-3 と **`tasks/E10-マスコットの動きを付ける.md`**。
    見送りではないので §H には入れていない。**着手の前提は素材（アニメーションのデータ）を用意できること**
- [x] **ステージ選択画面のレイアウト微調整（見出しとノードの位置ずれ）** ✅ **完了（2026-08-17・`tasks/E7`）。**
  原因は2つあった。①モックの `ROW_H = 145` が短いタイトル前提で、実データのタイトルは3行に折り返すため
  章の最後のノードが節の下端を **20px はみ出し**、章見出しとの間隔が 8〜38px でばらついていた
  → `ROW_H` を 180 に、ラベルの高さを固定して**全章 44px に揃えた**（マップは 100問で +20% 縦に長くなる）。
  ②`scrollIntoView({block:"center"})` が sticky バナーの高さを勘定に入れず、
  **起動直後に現在地の1つ上のノードの丸がバナーの裏に隠れていた** → バナーの下の見える範囲の中央に置き、
  それでも帯にかかる丸があれば下へ逃がす（かかるかどうかは画面の高さ次第。**新規ユーザーは
  現在地が STAGE 1 ＝ マップ最下部なので、着地位置がページ末尾で丸められる経路を通る**）。
  進行状況2種 × 画面4種の8通りで「バナーに重なる丸なし」を実測（2026-08-18）

- [x] **E-455（キーボードだけで回答を送信できる）が落ちている** ── **E5 の作業中に発見（2026-08-18）／
  ✅ 2026-08-19・G2 で解消**
  回答欄から Tab を1回押したときに当たるのが送信ボタンではなく、
  注意書きの**「くわしく」リンク**（C2 で `ProblemForm.tsx` に足したもの・`a575ea9`）だった。
  **オーナー判断で案①（リンクをボタンの後ろに置く）を採用。** ただし DOM の並びだけを入れ替え、
  **見た目の左右は flex の `order` で保っている**ので、画面の見え方は変わっていない。
  懸念だった読み上げ順は、回答欄に `aria-describedby` を付けて補った ──
  従来は注記まで読み進めないと届かなかったが、**回答欄に入った時点で読まれる**ので改善している

### MVP後の候補（`design/定番機能リスト.md` C）

デイリー目標 + リマインダーメール（Resend後） / 診断テスト（開始位置スキップ） / 効果音 + ミュート
※「リザルトのシェアボタン」は **§G-1（共有機能）** に統合

---

## G. 追加したい機能（構想・2026-08-17 起案）

**すべて本線（A〜C）の後。** 現時点で A・B・C をブロックするものは無い。
仕様が固まっていないものが多いので、**ここは「やると決めたことの記録」であって設計ではない。**

### G-1. 共有機能

- [ ] **共有機能（仕様は未定）**
  - **方針:** 一通り実装が終わってから、**仕様決めも含めて**着手する
  - F の MVP後候補にある「リザルトのシェアボタン」は、この一部として吸収される見込み
  - 仕様書の将来候補「段位・認定証」「公開プロフィールページ」と地続き

> **設計時に必ず検討すること:** 共有はURLの公開を意味するので、**現在の RLS 方針に穴を開けない形にする必要がある。**
> いま `user_attempts` は「本人だけが読める・誰も書けない」で統一している（§C・`ideas/db仕様.md`）。
> 共有ページを作るなら、**本人以外にも読ませる経路を別に用意する**ことになる。
> 「共有用の限定ビューを作る」のか「公開フラグを立てた行だけ読ませる」のか、
> ここを曖昧にしたまま作ると、スコア偽装を防いでいる設計が静かに崩れる。

### G-2. 問題画面のメモ書き機能 ── ✅ **完了（2026-08-19）**

- [x] **コードを読みながらメモを残せるようにする** ✅ **実装済み（`tasks/G2` → `tasked/G2-メモ書き機能.md`）**
  オーナー判断: **端末の中だけに保存**（案A・DB も API も増えない）／**採点した後も残す**（案B）／
  **コードの下・回答欄の上**に置く（案B）。
  - `app/problems/[id]/MemoPad.tsx`（新規）。保存先は `ferret:memo:{番号}` で、
    回答の下書き（`ferret:draft:{番号}`）とは**別の名前**
  - **回答欄とは別の部品にした。** 同じ部品にまとめると「送る文字列」と「送らない文字列」が
    1つの state に同居する。分けたことで、**メモが `/api/score` へ渡る経路が構造として存在しない**
  - 保存できる量: 1件 2,000字の上限だけを置き、**古いメモを消す仕組みは入れていない**
    （100問 × 2,000字 ≒ 400KB で、ブラウザの上限の1割に届かない。見積もりはコードに残した）
  - **E-455（公開ゲート4）を同梱で解消した**（§C-4・§F）
  - E2E を4件追加（`§6 メモ欄` の E-461・E-462 × 2幅）。全体は **101 passed / 1 skipped**
  - ⚠️ **申し送り: `tests/e2e/テストケース.md` は `.gitignore` されておりワークツリーに無い**ため、
    E-461・E-462 を**ケース定義の側に登録できていない。** 手で足すか、追跡対象にするかの判断が要る

> **案Aの帰結: サーバー側からメモは見えない。** 「メモが消えた」に対して復元する手段は無い。
> 端末をまたぐ形（案B）に広げたくなったら、保存先を差し替えるだけでよく、画面の構造は変わらない。

### G-3. AIチャットボット（有料枠）

- [ ] **答えは教えないが相談できる相手**
  - 回答の方針を示す
  - 問題の中に出てきた分からない語を解説する
  - **有料プラン向けの機能として構想**

> **設計上の核心的な論点が1つある。** `problems.model_answer` と `rubric_items` を
> チャットのプロンプトに渡すかどうか。
> **渡すと**「答えを言わない」がモデルの遵守頼みになる（採点の NGワード検査と同じで、指示だけでは漏れる）。
> **渡さないと**的外れな助言になる。ここを先に決めないと作れない。
>
> **原価の性質が採点と違う。** 採点は1回 ¥0.04 で上限が読めるが、チャットは往復回数が読めないため
> **ターン数の上限が必須。** 有料枠にするなら `users.plan` の参照が要るので、
> **D-1（レート制限）と土台が共通**になる。D-1 を先にやると流用できる。
>
> 既存の守り（PII スキャン・CSRF・Content-Type 検査・本文サイズ上限）は
> `/api/score` と同じものを入れること（`ideas/セキュリティ_残課題.md` §4）。

### G-4. 問題ごとの前提知識 ── ✅ **仕組みは完了（2026-08-17・E-4 に統合）**

- [x] **その問題を解くうえで押さえておくべき知識を、問題画面で参照できるようにする**
  静的コンテンツで足りるという判断どおり、`problems.prerequisite`（TEXT・400字以内・NULL 可）を追加。
  **AI原価ゼロ・待ち時間ゼロ。** 採点プロンプトには渡さない表示専用の欄。
  問題画面の「わからない言葉があるとき」を押すと開く（既定は閉じたまま）
- [x] 既存5問に投入済み（`problems/prerequisite-001-005.sql`）

> **切り分けは当初の提案どおり。** 「全員に同じことを説明すれば足りるもの」は静的なこの欄、
> 「その人の回答に対する個別の応答が要るもの」だけ G-3（AI）。
>
> **残っているのは A-2 側の作業。** 94問ぶんの前提知識を書く手間は量産コストに乗る。
> 書き方の制約（答えを書かない・模範解答を写さない・**コードに無い採点キーワードを書かない**）は
> `ideas/問題作成ガイド.md`「任意の2欄」にあり、`npm run test:db` の I-814〜I-817 が機械で見る。

---

## H. 見送り済み（再検討しないもの）

**判断の記録。** 蒸し返さないために残す。詳細は `ideas/セキュリティ_残課題.md` §3 と `design/定番機能リスト.md` D。

| 項目 | 見送りの理由 |
|---|---|
| CSP の `'unsafe-inline'` 除去 | nonce 方式にすると**全ページが動的レンダリングに変わる**。外部スクリプトは既に遮断済みで `dangerouslySetInnerHTML` / `eval` はゼロ。XSS の入口が今は無い |
| セッション Cookie の `httpOnly: true` | `@supabase/ssr` の仕様。ブラウザ側クライアントが Cookie を読む必要がある。塞ぐには認証を全部 Server Actions に移す作り直しが要る |
| アカウント列挙の防止 | 丁寧なエラー表示は意図的な設計（原因が分からないと離脱する）。消費者向け学習サービスでは列挙リスクが低い<br>⚠️ **2026-08-18・C4 訂正: 新規登録の経路は、この決定と逆に「伏せる」側で動いている。** `enable_confirmations = true` の間、Supabase は登録済みメールの signUp にも成功を返す（偽のユーザーを返しメールは送らない）ため、`lib/auth/errors.ts` の `user_already_exists` に**到達しない**。メール確認を切らない限り止められないので、**この挙動は受け入れる。** 見送っているのは「ログイン失敗の文面を曖昧にすること」だけ、と読み替えること（実測は `tests/e2e/auth.spec.ts` の E-112） |
| マイナンバー検出 | 桁数だけの判定になり誤検知が多すぎる |
| ハート / ライフ制 | 「AI採点の残数」が既に同じ経済の役。二重の制限は理不尽さだけ増える |
| リーグ / リーダーボード | 他人と比べて下位に置く仕組みは**ネガティブワード禁止・装備獲得型の方針と根本的に相性が悪い** |
| フレンド機能 | ターゲット（新人が一人で鍛える）に対して過剰 |
| shadcn/ui（画面の部品集） | 導入の目的は「ボタン・入力欄などを一から作らずに済ませる」ことだったが、`design/` でトークンを設計し**6画面ぶんの部品を Tailwind 直書きで作り終えている。** 目的そのものが消えており、いま入れると出来上がった画面を同じ見た目のまま作り直すだけになる（2026-08-18・F2） |
| Embedding 層による採点 | 「偶数」と「奇数」を高類似度と判定する構造的欠陥（`ideas/採点システム仕様書.md` §7） |

---

## I. 申し送りファイルを読むときの注意

**2026-08-19（Z1）に一巡した。** 下の表のズレはすべて元ファイル側に印を入れてある。

| 元ファイルの記述 | 実際 | 印 |
|---|---|---|
| `採点システム_残課題.md` §6「異議申し立ての導線」＝要検討 | 実装済み（2026-08-17）。`app/api/feedback/route.ts` + `problem_feedback`。**節に書いてある実装案とは形が違う**（`user_attempts.disputed` ではなく別テーブル） | ✅ |
| `採点システム_残課題.md` §7「テスト問題を最後尾へ」＝未着手 | 適用済み。id=5 は `order=999` | ✅ |
| `採点システム_残課題.md` §9「検算スクリプトの常設化」＝未着手 | 完了。`tests/unit/compose.test.ts` §1-1 で `npm test` に載っている | ✅ |
| `採点システム_残課題.md`「推奨する順番」の 1・2 | 1（core の判定・B3 候補1）と 2（0点の表示・E6）は完了。残るは §10 の `articulation` 観察と B3 候補2 | ✅ |
| `採点システム_残課題.md` §10 / `TASKS.md`「`RESEND_API_KEY` は空」 | **もう空ではない。** C1（2026-08-17）で値が入り SMTP 設定も済んでいる。アプリのコードは読まない値 | ✅ |
| `TASKS.md`「読解型はトレース型のみ検証済み」「問題1件」 | 読解型6種すべて実採点済み・問題は10件 | ✅ |
| `セキュリティ_残課題.md` の「用意済みと書いていたのは誤り」 | こちらは `STRIPE_WEBHOOK_SECRET` の話で、**いまも空で記述は正しい**（Resend と混同しないこと） | — |

### ドキュメント自体の更新 ── ✅ **完了（2026-08-19・Z1）**

- [x] **`CLAUDE.md` を実装に合わせて書き直した** — 採点は4観点・`rubric_items`・クリア55点、プロンプトキャッシュは効いている（97%）、
  ディレクトリ構成を実在するものだけに、削除済みカラム（`ai_rubric` / `model_answer_embedding`）の記述を除去。行数も約3割減らした（248行 → 178行。文字数はほぼ同じで、誤りの修正で足した分と釣り合っている）
- [x] `環境構築メモ.md` の「未インストール」リストを更新
- [x] `ideas/db仕様.md` の削除済みカラムの記述を修正（`ai_rubric` の行と `model_answer_embedding` の節が食い違っていた）
- [x] **`TASKS.md` と本ファイルの役割を決めた** — **本ファイルが残タスクの正。`TASKS.md` は完了記録**（オーナー判断・案A）
