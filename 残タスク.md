# 残タスク（統合版）

**作成: 2026-08-17。** 実装・DB・テストの実地確認と、散らばっていた申し送りファイルを1本にまとめたもの。
**「次に何をやるか」はこのファイルだけ見れば決まる**ようにしてある。理由や手順の詳細は各節から元ファイルへ飛ぶこと。

参照元: `TASKS.md` / `ideas/採点システム_残課題.md` / `ideas/セキュリティ_残課題.md` / `design/移植残タスク.md` / `design/定番機能リスト.md` / `ideas/問題作成ガイド.md`

> ### 作業を依頼するときは `tasks/` を使う
>
> **本ファイルは全体像の把握用。** 実際に手を動かすときは **`tasks/README.md`** から
> 該当のタスクを選び、**そのファイルだけを読ませれば着手できる**ようにしてある。
> 各タスクには、先に読むべき資料・依存関係・**判断を仰ぐべき箇所**が書いてある。

> 元ファイルには**すでに完了した項目が未着手のまま残っている**箇所がある。読むときの注意は §I。

---

## 現在地（2026-08-17 実測）

| | |
|---|---|
| テスト | 単体・結合 **558 passed / 63 skipped**（19ファイル）。skip は実DB接続系（`RUN_DB_TESTS=1` で有効。そちらは **63 passed**） |
| E2E | **76 passed / 1 skipped**（`npm run test:e2e`。2026-08-18・F3 で復旧）。skip は E-111（C1 の後に戻す） |
| 投入済み問題 | **10件**（`order` 1〜5・15・53・59・78 + 動作確認用 `order=999`）。**読解型6種すべてが実採点済み** |
| AI採点 1回 | 約 **¥0.04** / 応答 1.2〜4.3秒 / プロンプトキャッシュ命中率 97% |
| 異議申し立て | 稼働中（`problem_feedback` に **3件**記録済み） |
| 環境変数 | Supabase・OpenAI は設定済み。**`RESEND_API_KEY` / `STRIPE_SECRET_KEY` / `NEXT_PUBLIC_APP_URL` は空** |
| デプロイ | 未実施（ローカルのみ） |

ブラウザで「ログイン → ステージ選択 → 問題 → 採点 → リザルト → 異議申し立て → ログアウト」が通る。

---

## 進める順番

```
A. 問題コンテンツ（本線・唯一の停滞要因）
     │
     ├─ A-1 読解型6種の検証  ✅ 完了（2026-08-17）
     │        │
     │        ├─ B-1 ゴールデンセットで測る  ✅ 完了（2026-08-17）
     │        │        │
     │        │        └─→ B-3 core の判定基準を直す  ← いまここ
     │        │                 ※ 測った結果、直すべき箇所は1点に絞れた
     │        │
     │        └─ B-2 §11・§12 の実測  ✅ 完了（どちらも再現せず・対処不要）
     │
     └─ A-2 残り91問の量産   ← B-3 が終わるまで着手しない
                                （基準がズレていると全問書き直しになる）

C. 公開前に必須 ─── デプロイしたい時点で着手。A/B とは独立
D. 課金開始前に必須 ─ 収益化の判断が出てから。C の後
E. 画面・体験 ───── いつでも（A-1 をブロックしていた E-4 は完了）
```

**いま着手すべきは B-3（`core` の判定基準を直す）。**
B-1 で測った結果、**採点の穴は `core` が緩いこと1点に絞れた**（§11・§12 は再現せず）。
直したら `npm run golden` を回して、B-1 の数字と比べること。
C 以降は「公開する」と決めた時点で始めればよく、A/B と並行して困らない。

---

## A. 問題コンテンツ（本線）

**唯一の実質的な停滞要因。** ブロッカーではなく作業量の問題。
構成は `ideas/問題構成案.md`（100問）、作り方は `ideas/問題作成ガイド.md`。

### A-1. 読解型6種の検証 ── ✅ **完了（2026-08-17）**

`depth` の判定条件は読解型ごとに切り替わる。**6種すべてが実採点を通った。**

| 読解型 | 投入済み | 状態 |
|---|---|---|
| トレース | 3件（+ 動作確認用1件） | ✅ 検証済み |
| ズレ | 2件 | ✅ 検証済み |
| 意図 | 1件（ステージ15） | ✅ 検証済み |
| 影響 | 1件（ステージ59） | ✅ 検証済み。**`problems.context` を使う唯一の問題** |
| 命名 | 1件（ステージ53） | ✅ 検証済み |
| 仕様 | 1件（ステージ78） | ✅ 検証済み |

- [x] `意図` 型を1問作って採点を通す
- [x] `命名` 型を1問作って採点を通す
- [x] `仕様` 型を1問作って採点を通す
- [x] `影響` 型を1問作って採点を通す

**4問とも「短くても正しければクリア」「結論が逆ならクリアしない」を満たした。**
①模範解答に近い回答では `depth` が付き、②短い正答では付かないので、
**読解型ごとの `depth` 判定条件が独立に働いていることを確認できた**（これが A-1 の目的）。

結果と気づいた点は `problems/stage-015-053-059-078.md`、経緯は `tasked/A1-読解型の検証.md`。

> **採点の物差しについて3点の観察を記録した。仕組みは触っていない**（A1 の方針）。
> **B-3 で判断すること。** 要点は「ステージ78 の短い正答が 92点（パーフェクト帯）に入る」
> 「`none` / `partial` でも `evidence` が入ってくる」「同じ引用を複数の観点に使い回している」。

### A-2. 量産

- [ ] 残り91問（B の完了後）
- [ ] 動作確認用の問題（id=5・`order=999`）を残すか消すか判断
  → **残すことを推奨。** 5パターンの期待値が分かっているので「採点が壊れたのか問題データが悪いのか」の切り分けに使える（`ideas/採点システム_残課題.md` §7）

---

## B. 採点の信頼性

**すべて「測ってから直す」。推測で直すと悪化しても気づけない。**
詳細は `ideas/採点システム_残課題.md`。

### B-1. ゴールデンセットでブレを測る ── ✅ **完了（2026-08-17）**

- [x] **50件のゴールデンセットを組んで回す**
  → `npm run golden`（1周 90回 / 約¥3.6 / 約40秒）。回答文は `tests/golden/cases.json`（**git 管理外**）

| 指標 | 目標 | 実測（`p3`） | |
|---|---|---|---|
| 反転誤答の**誤合格率** | 0% | **0%（0/10）** | ✅ |
| 短いが正しい回答の**合格率** | 100% | **100%（10/10）** | ✅ |
| 想定との一致率（50件） | — | **96%（48/50）** | |
| 合否が入れ替わる率（同一回答5回・閾値付近10件） | 低いほどよい | **10%（1/10）** | 初回測定 |
| パーフェクト帯（80点以上） | 目安 15〜25% | **46%（23/50）** | ⚠️ |

**外れた2件はどちらも「不合格のはずがクリアした」。** 原因は `core` が緩いこと1点で、
うち1件は5回すべてクリアする（揺れではなく基準そのもの）。**B-3 の対象がここに絞れた。**
経緯は `tasked/B1-採点のブレを測る.md`、回答文と点数の対応は `ideas/採点システム_残課題.md` §8。

> **B-3 の前後で必ず `npm run golden` を回すこと。** 上の数字は `p3` のもの。
> プロンプトを触ると版が上がるので、**触る前に一度回して現行値を確認してから**変更する。

### B-2. プロンプト対処が効いているかの実測 ── ✅ **完了（2026-08-17）。どちらも再現せず**

- [x] **数値だけの引用を禁じた指示にモデルが従っているか**（`残課題` §11）
  → 数値だけが誤っている反転誤答4件を測り、**4件すべて矛盾検出・不合格。** 指示は守られている。
  **加えて §11 の症状の記述自体が古い** ── 引用照合が取れない側にも層1の頭打ちが入ったため、
  上限50点でクリア閾値55に届かない。**「60点でクリアしてしまう」経路はもう存在しない**
- [x] **箇条書き回答が捏造検出を誤爆させていないか**（`残課題` §12）
  → 箇条書き5件で**引用の格下げ0件・誤爆なし。** `FABRICATION_DEMOTE_THRESHOLD` は触らなくてよい。
  ただし母数は5件（列挙型の設問が order 4 の1問しかない）。**A-2 でその形式が増えたら測り直す**

### B-3. 判定基準の見直し（測ってから）← いまここ

- [ ] **`core` が緩い**（B-1 で実測・**これが唯一残った採点の穴**）
  - 「エラーが出ると思います」だけで `core=full`（48点）が付き、**60点でクリアする**（5回中2回）
  - 設問が問うている軸に触れていない回答が、**5回すべて `core=full` で63点**（揺れではない）
  - 見立て: `core` が「回答の内容が正しいか」で付いていて、「その問題の `core` の条件を満たすか」で付いていない
  - 直す候補は `残課題` §8「直す候補（測った結果を反映）」。**配点には手を付けない**
  - **オーナー判断（2026-08-17）: 候補1（判断例を1行追加）から着手し、2 とは同時に入れない。**
    同時に直すと、数字が動いてもどちらが効いたのか切り分けられなくなる。
    **`npm run golden` で現行値を確認 → 1 を入れる → 回して比べる**の順で進める
- [ ] `articulation` が厳しすぎる疑い — **B-1 では厳しすぎる傾向は出ていない。** 優先度を下げてよい
- [ ] パーフェクト帯が 46%（目安15〜25%）。`ground` が付きやすい。**合否は正しいので `core` の次**

### B-4. 分析材料を貯める

- [x] **どの誤読に当たったかを記録**（`matched_reject`）— `残課題` §3
  ✅ **実装済み（2026-08-17・`tasks/B2`）。** `axes.matched_reject` に `"none"` / `"1"`〜`"3"` を保存する。
  実データで確認済み（誤読 →「1」、正答 →「none」）。**番号は3までなので、
  `core_reject` が4件以上の問題を作るときは3ファイルを同時に広げること**（B2 の作業記録）
- [x] **フィードバックが「どの行を見ればよいか」を示すようにする** — `残課題` §4
  ✅ **実装済み（2026-08-17・`tasks/B2`）。** 読み違いを検出したときは、その読み方が
  成り立たなくなる箇所を必ず示させる。実測で「5行目の rate = 0.8 に注目すると〜」が出ている
- [x] ⚠️ **上2件で `PROMPT_VERSION` が p2 → p3 に上がった。** B1 のゴールデンセットは
  p3 で回し、p2 時代の実測値と混ぜないこと
  → **`p3` で測った**（2026-08-17・B1）。p2 の A-1 実測とは別物として扱っている。
  なお `p2` → `p3` で**判定は緩む方向に動いた** ── p2 では 29/53 で不合格だった回答が、
  p3 では 34/60 で**クリアするようになっている**（B-1 の「外れた2件」の1件）

---

## C. 公開（デプロイ）前に必須

### C-1. これが無いと実ユーザーが登録できない

- [x] **メール送信を Resend に切り替える** ── ✅ **仕組みは完成（2026-08-17）**
  Resend 経由の送信・日本語の文面・**メールのリンクからのログインまで実測で通した。**
  `RESEND_API_KEY` に値が入り、Supabase の SMTP 設定も済んでいる。経緯は `tasks/C1-...md` の作業記録
  - 済: 確認メールのリンクを**端末をまたいで開ける形**にした（`app/auth/callback/route.ts` に `token_hash` 方式を追加。
    従来の `code` 方式だと「パソコンで登録 → スマホでメールを開く」でログインできない）。**実測で確認済み**
  - 済: 日本語の文面（`supabase/templates/confirm-signup.html`）と、
    それを API で反映するスクリプト（`supabase/templates/apply.mjs`。管理画面への貼り付けは文字化けの事故が起きたため）
  - 済: `supabase/config.toml` の `email_sent` を本番の実値（30）に合わせた。
    **`2` のままだと `config push` で本番の送信上限が1時間2通に戻る**地雷だった
- [ ] **⚠️ ただし実ユーザーにはまだ届かない → `C-6`（下）。** 送信元がお試しドメインのため
  **iCloud が拒否した**（`Blocked due to content`）。件名の日本語化も C-6 に持ち越し（`apply.mjs` の実行で済む）

### C-6. 独自ドメイン ── **C-4（デプロイ）の前提。費用の判断が要る**

- [ ] **ドメインを1つ取得して、Resend と Vercel に登録する** → **`tasks/C6-独自ドメインを用意する.md`**
  **先送りできない項目だと 2026-08-17 の実測で確定した。** お試しドメインからのメールは iCloud が拒否する
  （`Blocked due to content`）。原因は2つあり、**デプロイで消えるのは片方だけ**
  - リンクが `http://localhost:3000` を指している → **デプロイで解決する見込み**
  - 差出人が共用のお試しドメイン → **デプロイでは解決しない。** Resend でのドメイン認証が必要
  - **`*.vercel.app` では代用できない**（Resend の認証にはDNSを触る必要があり、あれは Vercel のもの）
  - 年 1,000〜2,000円程度の費用と、**プロダクトのURLになる名前の決定**が要る（あとから変えると全部やり直し）

### C-2. 法務・信頼

- [ ] **利用規約・プライバシーポリシー**（文書そのもの）
  回答を OpenAI に送る旨の注記（仕様書 §9.5）の**根拠になる文書が存在しない**。
  ページの箱だけ先に作っても中身が偽物になるので未着手のまま
- [ ] **退会（アカウント削除）** — 個人情報を預かる以上、公開前に必要。admin API のサーバールートが要る
- [ ] パスワード変更（`auth.updateUser` で安い）
- [ ] メール変更（**C-1 の後**。確認メール送信が要る）

### C-3. Supabase の設定（管理画面でしかできない）

- [ ] **流出パスワードの拒否（`password_hibp_enabled`）を ON** ← ここだけやれば足りる
  Authentication → Sign In / Providers → Password。**コードでは絶対に塞げない**（登録はブラウザから Supabase Auth へ直接飛ぶため）。
  総当たりは Supabase 側の制限で既に止まっているが、**他所で流出したパスワードの使い回し**はこれでしか止まらない
- [ ] Minimum password length を 8 に（付随）
- [ ] 認証エンドポイントのレート制限を確認

> ⚠️ **`supabase config push` はまだ実行しないこと。** `site_url` が `http://127.0.0.1:3000` のままで、push すると**本番の確認メールのリンクが localhost を指す**。部分適用のオプションは CLI に無い。

### C-4. デプロイ

- [ ] Vercel デプロイ設定
- [ ] 本番環境の環境変数設定
- [ ] **`NEXT_PUBLIC_APP_URL` は Production 環境にだけ設定する**（例 `https://ferret.io`）。
  **Preview には設定しない** — するとプレビューからログインした人が本番へ飛ぶ
- [ ] `site_url` / `additional_redirect_urls` を本番URLに（ドメイン確定後）
- [ ] `.env.local.example` の最終確認

### C-5. 公開前に消すもの

- [ ] **開発用アカウントのパスワードが公開リポジトリに載っている**
  `TASKS.md:111` に `dev@ferret.test` / `FerretDev2026!` が平文で記載。リポジトリは Public。
  ローカル運用中は実害ないが、デプロイすると**パスワードが公開されたアカウントが本番に存在する**状態になる。
  デプロイ前にパスワードを変えるか、記載を伏せること

---

## D. 課金開始前に必須

### D-1. レート制限（プラン別のAI採点上限）

**現在 `/api/score` はプランを見ずに無制限で採点する。** 実装の目印は `app/api/score/route.ts:484` の TODO。

- [ ] Free 1日3問（JST 深夜0時リセット）
- [ ] Pro 月200問 / Pro Plus 月500問
- [ ] `users.plan` の参照ロジック
- [ ] 超過時は層2をスキップし `is_provisional = true` + `scoring_method = 'keyword_only'` で保存
  **層1は最大20点でクリア閾値55に届かないため、不合格ではなく「判定保留」として扱う**（そうしないと上限到達ユーザーに理由不明の全問不合格が並ぶ）
- [ ] 制限超過時のUI表示
- [ ] 「きょうの AI 採点」ウィジェット（右レール・肉球3つ）— **制限の強制と同時に**。
  強制なしで残数だけ出すと「0になっても採点できる」矛盾表示になる

> いま入っている `RATE_LIMITS`（1分10件 / 1時間60件 / 24時間300件）は**使いすぎを止める安全網**であって商品上限ではない。**消さずに、その内側にプラン上限を足すこと。**
> カウントのクエリは `ideas/db仕様.md`「レート制限のクエリ」。リプレイと判定保留は無料枠を消費しない。

### D-2. Stripe

- [ ] アカウント設定・価格プラン作成
- [ ] `stripe` パッケージインストール
- [ ] Checkout セッション生成（Pro アップグレード）
- [ ] **Webhook 実装（`/api/webhooks/stripe`）**
  ⚠️ **`stripe.webhooks.constructEvent()` による署名検証を最初に書く。** 無いと誰でも「支払い完了」の JSON を投げるだけで Pro になれる。
  署名検証には**生のリクエストボディ**が必要なので `request.json()` ではなく `request.text()` を使う。
  Webhook は Stripe からのサーバー間通信なので `isCrossSiteRequest` は通す設計でよい
- [ ] 駆け込みパック（単発購入）フロー
- [ ] 右レールの「Pro をみる」ボタンを有効化（現在は準備中で押せない）

### D-3. 課金に伴う法務

- [ ] 特定商取引法に基づく表記
- [ ] 解約導線（Stripe Customer Portal が最安）

---

## E. 画面・体験

### E-1. 画面そのものが無いもの

- [ ] **振り返り画面（`/review/[id]`）** — `user_attempts.axes` に4観点の判定と引用が入っているので**材料は揃っている**
- [ ] せってい画面（サイドバーに「準備中」で置いてある）

### E-2. データが足りなくて出せていないもの

- [ ] **フィードバックの2枠表示**（よかったところ / つぎの一歩）
  `lib/ai/scorer.ts:169` が `[praise.text, next.text].join(" ")` で**1本に潰してから**保存しているため復元できない。
  `ai_praise` / `ai_next_focus` のカラム追加か JSONB 化が要る。採点ロジックは変わらないので `grader_version` の変更は不要
- [ ] **XP バー・「+10 XP」表示**
  `users.xp` カラムはあるが**加算するロジックがどこにもない**。採点APIのクリア時に加算 + レベル閾値の設計が要る
- [x] **マップのパーフェクト演出** ✅ **実装済み（2026-08-17・`tasks/E7`）。オーナー判断で案A に確定（2026-08-18）。**
  80点以上のクリア済みノードに金の縁取り（案A）を一重足す。星マーク・発光は**見送り**。
  基準は `lib/ai/compose.ts` の `PERFECT_THRESHOLD` を画面から読む。
  判定は `Stage.perfect` の独立した欄（`status` の4つ目にすると `=== "cleared"` の判定が全部取りこぼす）

### E-3. 見た目

- [ ] **Shiki シンタックスハイライト**（現状 `<pre>` 素通し）。問題画面はサーバーコンポーネントなので SSR ハイライトにできる。モックの `--color-code-*` トークンに合わせたカスタムテーマを作る
- [ ] モバイルレイアウト（3カラムは lg 以上のみ。lg 未満は簡易ヘッダーで代用中）
- [ ] 0点表示の見直し — 巨大な「0」が罰のように見える。矛盾検出時は数字を控えめにし「次に見る場所」を主役にする
- [ ] ダークテーマ切替（`design/stages-desktop-dark.html` の `:root` トークンを `[data-theme="dark"]` として足すだけの構造にしてある）
- [ ] **マスコットに Lottie の動きを付ける**（**入れる方針に確定**・2026-08-18・F2）→ **`tasks/E10-マスコットの動きを付ける.md`**
  現状は `app/globals.css` の CSS アニメーション7種（`float` / `sniff` / `pop` / `halo` / `bob` / `blink` / `fall`）で動いている。
  **前提は素材（アニメーションのデータ）を用意できること。** 仕組みだけ入れても再生するものが無いので、票の冒頭に確認事項として書いてある

### E-4. 影響型の2パネル表示 ── ✅ **完了（2026-08-17）**

- [x] **コードと実行ログを分けて表示する構造を決める**
  `problems.context`（TEXT・NULL 可）を新設した。code 内の区切り記法は採らない（書き忘れると壊れる）。
  入っている問題だけコードの下に2枠目が出る。**A-1 の影響型はこれで着手できる**
- [x] **前提知識（旧 G-4）も同じマイグレーションで足した** — `problems.prerequisite`（400字以内・NULL 可）。
  「わからない言葉があるとき」を押すと開く。既存5問には投入済み（`problems/prerequisite-001-005.sql`）
  → 詳細と判断の理由は `tasked/E4-問題データに欄を足す.md` の作業記録

---

## F. その他・技術的負債

- [x] `middleware.ts` → `proxy.ts` への移行（**2026-08-17 完了・F1**）。非推奨警告は消えた。処理は変えていない。ドキュメントの参照もすべて追随済み
- [x] **ブラウザの通しテスト（E2E）が全件落ちている** ── ✅ **完了（2026-08-18・F3）**
  **76 passed / 1 skipped。アプリのコードは1行も触っていない**（画面が正しく、期待値が古かった）。
  原因は3つで、うち2つはタスク票に書いていなかったもの。
  ①アサーションがデザイン移植（`943345a`）に追いついていない（見出し・マップのアイコン・リザルトの点数表示）
  ②`baseURL` の `127.0.0.1` は **`1116f81` ですでに直っていた**（票の記述が古い。`localhost` からは警告0件と実測）
  ③**シード問題が解放されず 404**（実コンテンツが増えてマップの末尾に沈んだ。E4 の申し送りが残していた選択）。
  詳細と判断の理由は `tasks/F3-E2Eの古いアサーションを直す.md` の作業記録。
  **`E-112` と `E-272` で決定と実装のずれが見つかっている**（それぞれ C4・E6 で判断すること）
- [x] **shadcn/ui と Lottie を入れるか判断** ── ✅ **完了（2026-08-18・F2）**
  - **shadcn/ui は見送り** → §H に理由つきで移した。`design/` でトークンを設計し、
    6画面ぶんの部品を Tailwind 直書きで作り終えているため、導入の目的（部品を一から作らずに済ませる）が消えている
  - **Lottie は入れる**（オーナー判断）→ §E-3 と **`tasks/E10-マスコットの動きを付ける.md`**。
    見送りではないので §H には入れていない。**着手の前提は素材（アニメーションのデータ）を用意できること**
- [x] **ステージ選択画面のレイアウト微調整（見出しとノードの位置ずれ）** ✅ **完了（2026-08-17・`tasks/E7`）。**
  原因は2つあった。①モックの `ROW_H = 145` が短いタイトル前提で、実データのタイトルは3行に折り返すため
  章の最後のノードが節の下端を **20px はみ出し**、章見出しとの間隔が 8〜38px でばらついていた
  → `ROW_H` を 180 に、ラベルの高さを固定して**全章 44px に揃えた**（マップは 100問で +20% 縦に長くなる）。
  ②`scrollIntoView({block:"center"})` が sticky バナーの高さを勘定に入れず、
  **起動直後に現在地の1つ上のノードの丸がバナーの裏に隠れていた** → バナーの下の見える範囲の中央に置き、
  それでも帯にかかる丸があれば下へ逃がす（かかるかどうかは画面の高さ次第。**新規ユーザーは
  現在地が STAGE 1 ＝ マップ最下部なので、着地位置がページ末尾で丸められる経路を通る**）。
  進行状況2種 × 画面4種の8通りで「バナーに重なる丸なし」を実測（2026-08-18）

### MVP後の候補（`design/定番機能リスト.md` C）

デイリー目標 + リマインダーメール（Resend後） / 診断テスト（開始位置スキップ） / 効果音 + ミュート
※「リザルトのシェアボタン」は **§G-1（共有機能）** に統合

---

## G. 追加したい機能（構想・2026-08-17 起案）

**すべて本線（A〜C）の後。** 現時点で A・B・C をブロックするものは無い。
仕様が固まっていないものが多いので、**ここは「やると決めたことの記録」であって設計ではない。**

### G-1. 共有機能

- [ ] **共有機能（仕様は未定）**
  - **方針:** 一通り実装が終わってから、**仕様決めも含めて**着手する
  - F の MVP後候補にある「リザルトのシェアボタン」は、この一部として吸収される見込み
  - 仕様書の将来候補「段位・認定証」「公開プロフィールページ」と地続き

> **設計時に必ず検討すること:** 共有はURLの公開を意味するので、**現在の RLS 方針に穴を開けない形にする必要がある。**
> いま `user_attempts` は「本人だけが読める・誰も書けない」で統一している（§C・`ideas/db仕様.md`）。
> 共有ページを作るなら、**本人以外にも読ませる経路を別に用意する**ことになる。
> 「共有用の限定ビューを作る」のか「公開フラグを立てた行だけ読ませる」のか、
> ここを曖昧にしたまま作ると、スコア偽装を防いでいる設計が静かに崩れる。

### G-2. 問題画面のメモ書き機能

- [ ] **コードを読みながらメモを残せるようにする**
  - トレース型で「変数の値を追いながら書き留める」用途に効く
  - 読解型のうち `トレース` / `影響` と特に相性がよい

> **土台は既にある。** 回答の下書き保持（`ProblemForm.tsx` の `ferret:draft:{problemId}` / localStorage）と同じ仕組みで作れる。
> **まず決めること: 端末をまたいで残す必要があるか。**
> 不要なら localStorage で完結し、DBもAPIも増えない（下書き保持と同じ実装量）。
> 必要ならテーブル追加 + API + RLS ポリシーが要る。**最初は localStorage で十分と思われる。**

### G-3. AIチャットボット（有料枠）

- [ ] **答えは教えないが相談できる相手**
  - 回答の方針を示す
  - 問題の中に出てきた分からない語を解説する
  - **有料プラン向けの機能として構想**

> **設計上の核心的な論点が1つある。** `problems.model_answer` と `rubric_items` を
> チャットのプロンプトに渡すかどうか。
> **渡すと**「答えを言わない」がモデルの遵守頼みになる（採点の NGワード検査と同じで、指示だけでは漏れる）。
> **渡さないと**的外れな助言になる。ここを先に決めないと作れない。
>
> **原価の性質が採点と違う。** 採点は1回 ¥0.04 で上限が読めるが、チャットは往復回数が読めないため
> **ターン数の上限が必須。** 有料枠にするなら `users.plan` の参照が要るので、
> **D-1（レート制限）と土台が共通**になる。D-1 を先にやると流用できる。
>
> 既存の守り（PII スキャン・CSRF・Content-Type 検査・本文サイズ上限）は
> `/api/score` と同じものを入れること（`ideas/セキュリティ_残課題.md` §4）。

### G-4. 問題ごとの前提知識 ── ✅ **仕組みは完了（2026-08-17・E-4 に統合）**

- [x] **その問題を解くうえで押さえておくべき知識を、問題画面で参照できるようにする**
  静的コンテンツで足りるという判断どおり、`problems.prerequisite`（TEXT・400字以内・NULL 可）を追加。
  **AI原価ゼロ・待ち時間ゼロ。** 採点プロンプトには渡さない表示専用の欄。
  問題画面の「わからない言葉があるとき」を押すと開く（既定は閉じたまま）
- [x] 既存5問に投入済み（`problems/prerequisite-001-005.sql`）

> **切り分けは当初の提案どおり。** 「全員に同じことを説明すれば足りるもの」は静的なこの欄、
> 「その人の回答に対する個別の応答が要るもの」だけ G-3（AI）。
>
> **残っているのは A-2 側の作業。** 94問ぶんの前提知識を書く手間は量産コストに乗る。
> 書き方の制約（答えを書かない・模範解答を写さない・**コードに無い採点キーワードを書かない**）は
> `ideas/問題作成ガイド.md`「任意の2欄」にあり、`npm run test:db` の I-814〜I-817 が機械で見る。

---

## H. 見送り済み（再検討しないもの）

**判断の記録。** 蒸し返さないために残す。詳細は `ideas/セキュリティ_残課題.md` §3 と `design/定番機能リスト.md` D。

| 項目 | 見送りの理由 |
|---|---|
| CSP の `'unsafe-inline'` 除去 | nonce 方式にすると**全ページが動的レンダリングに変わる**。外部スクリプトは既に遮断済みで `dangerouslySetInnerHTML` / `eval` はゼロ。XSS の入口が今は無い |
| セッション Cookie の `httpOnly: true` | `@supabase/ssr` の仕様。ブラウザ側クライアントが Cookie を読む必要がある。塞ぐには認証を全部 Server Actions に移す作り直しが要る |
| アカウント列挙の防止 | 丁寧なエラー表示は意図的な設計（原因が分からないと離脱する）。消費者向け学習サービスでは列挙リスクが低い |
| マイナンバー検出 | 桁数だけの判定になり誤検知が多すぎる |
| ハート / ライフ制 | 「AI採点の残数」が既に同じ経済の役。二重の制限は理不尽さだけ増える |
| リーグ / リーダーボード | 他人と比べて下位に置く仕組みは**ネガティブワード禁止・装備獲得型の方針と根本的に相性が悪い** |
| フレンド機能 | ターゲット（新人が一人で鍛える）に対して過剰 |
| shadcn/ui（画面の部品集） | 導入の目的は「ボタン・入力欄などを一から作らずに済ませる」ことだったが、`design/` でトークンを設計し**6画面ぶんの部品を Tailwind 直書きで作り終えている。** 目的そのものが消えており、いま入れると出来上がった画面を同じ見た目のまま作り直すだけになる（2026-08-18・F2） |
| Embedding 層による採点 | 「偶数」と「奇数」を高類似度と判定する構造的欠陥（`ideas/採点システム仕様書.md` §7） |

---

## I. 申し送りファイルを読むときの注意

**元ファイルに、完了済みなのに未着手として残っている項目がある。** 実装を確認した結果は以下。

| 元ファイルの記述 | 実際 |
|---|---|
| `採点システム_残課題.md` §6「異議申し立ての導線」＝要検討 | ✅ **実装済み**（2026-08-17）。`app/api/feedback/route.ts` + `problem_feedback` テーブル。**すでに3件記録されている** |
| `採点システム_残課題.md` §7「テスト問題を最後尾へ」＝未着手 | ✅ **適用済み**。id=5 は `order=999` |
| `採点システム_残課題.md` §9「検算スクリプトの常設化」＝未着手 | ✅ **完了**。`tests/unit/compose.test.ts` §1-1 で `npm test` に載っている |
| `採点システム_残課題.md`「推奨する順番」の 1・2 | ✅ 両方完了。**残っているのは 3以降** |
| `採点システム_残課題.md` §10 / `TASKS.md`「`RESEND_API_KEY` は `.env.local` に用意済み」 | ❌ **誤り。キー名があるだけで値は空** |
| `TASKS.md`「読解型を1つずつ6問作って検証」＝トレース型のみ検証済み | 現在は**トレース4件・ズレ2件**。残り4種が未検証 |
| `TASKS.md` の「問題コンテンツ1件」「登録ユーザー1人」 | 問題は**6件**に増えている |

### ドキュメント自体の更新

- [ ] **`CLAUDE.md` が実態と乖離している**（最も影響が大きい。次のセッションが誤った前提で動く）
  - 採点システムの記述が **v2（5段階・`ai_rubric`）のまま** → v3（4観点・`rubric_items`・クリア55点）へ
  - 「プロンプトキャッシュは考慮不要」→ **v3 では前提が逆**。1,024トークンを超えて発動域に入り実測97%命中
  - ディレクトリ構成に `lib/ai/{compose,schema,prompt,hash}.ts` / `app/logout/` / `lib/auth/` / `lib/progress/` / `lib/stages/` / `tests/` が未記載
  - `problems.ai_rubric` / `model_answer_embedding` は**削除済み**
- [ ] `環境構築メモ.md` の「未インストール」リストが古い（`openai` は導入済み）
- [ ] `TASKS.md` と本ファイルの役割を決める（**本ファイルを残タスクの正とし、TASKS.md は完了記録に寄せる**のが素直）
