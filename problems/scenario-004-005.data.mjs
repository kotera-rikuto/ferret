// ステージ 4・5 の「場面」（A4）。
//
// **このファイルには場面しか入っていない。** 他のステージの場面は
// それぞれの `stage-*.data.mjs` の中（`reading_type` と `code` のあいだ）にある。
// 4・5 問目だけ別置きなのは、**この2問には data.mjs が無い**ため
// （A3 で 1〜3 問目を差し替えたときに 4・5 は据え置いたので、
// 旧い手書きの `stage-001-005.sql` が唯一の記録になっている）。
//
// 投入は**場面だけを書き替える専用スクリプト**で行う。
//
//   node problems/scenario-update.mjs problems/scenario-004-005.data.mjs
//
// ⚠️ **`update.mjs` に渡さないこと。** あちらは1問まるごと差し替えるので、
// このファイルを渡すと `context` と `prerequisite` が null で潰れる。
// （`update.mjs` の先頭にその状態を弾くガードを置いてある）

export const problems = [
  {
    order: 4,
    scenario: `設定の初期値を作っている部分について、後輩から相談を受けた。
一緒に読んでみることになった。`,
  },

  {
    order: 5,
    scenario: `同僚から、この関数のレビューを頼まれた。
上のコメントに書いてあるとおりに動くかを見てほしいとのこと。`,
  },
];
