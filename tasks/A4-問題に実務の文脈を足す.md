# A4. 問題の頭に「実務の場面」を足す

**種別:** コンテンツ作成 + 実装（**判断が主**）
**規模:** 判断次第で**中〜特大**（下記）
**前提として終わっていること:** **A2（101問）** ✅済み・**A3（チュートリアル）** ✅済み

> ### ⚠️ 同時進行の注意
> `app/problems/[id]/page.tsx` を触る（E4・E5 と同じファイル）。
> **問題データを全件触るので、他の問題コンテンツ作業と同時に進めないこと。**

**先に読むもの**
- `ideas/問題作成ガイド.md`
- `app/problems/[id]/page.tsx`（**`context` と `prerequisite` の出し方が手本になる**）
- `tasked/E4-問題データに欄を足す.md`（**欄を足したときの手順がそのまま使える**）

---

## 何をするか

**問題の頭に「どういう場面でこのコードを読むことになったか」を置く。**

**オーナーの案（2026-09-02）:**
> 「上司から〜を頼まれました」のような**4コマ漫画**をそれぞれに入れたい。
> そのほうが文脈があって実務に近い。

### いまの状態（2026-09-02 実測）

設問は**場面の説明が一切なく、いきなりコードの話**から始まる。

```
[1] このコードを実行すると何が出力されますか。その数になるまでの過程も説明してください。
[2] このコードを実行すると何が出力されますか。そうなる理由も説明してください。
[3] 2つ目の console.log は何を出力しますか。1つ目の出力との違いも説明してください。
```

**「なぜこのコードを読んでいるのか」が無い。**

| 欄 | 使用状況 |
|---|---|
| `context` | **4問**（影響型の実行結果に使用済み。**空いていない**） |
| `prerequisite` | 100問（前提知識） |
| **場面を入れる欄** | **無い** |

---

## なぜ必要か

**Ferret の売りは「他人のコードが読める」こと。**

実務でコードを読むときは、必ず**理由**があります。「引き継いだ」「バグの報告が来た」
「レビューを頼まれた」。**その状況が読み方を決めます。**

いまの形は「問題集」に近く、**実務の読解とは別の作業**になっています。
場面があれば、**なぜこの観点で読むのかが自然に伝わります。**

---

## ⚠️ オーナーに確認すること（**ここが本体**）

### ① 「4コマ漫画」は絵ですか、文章ですか ← **一番大きい判断**

> **絵にする場合、101問 × 4コマ = 404枚**が必要です。
> マスコットの素材は `public/` に4種ありますが、**場面ごとの絵は1枚もありません。**
> 個人開発で404枚を用意するのは、**A2（101問）より大きい作業**になります。
>
> - **案A: 文章だけ**（おすすめ）
>   「先輩から、注文の集計処理を引き継ぐことになった。まずは中身を読んでほしいと言われた。」
>   のような**2〜3行**。**AIで下書きでき、101問ぶんでも現実的**です
> - 案B: マスコット + 吹き出し
>   既存のマスコット画像を1枚置いて、その横に文章を出します。
>   **絵は使い回すので新規に描く必要がありません**。「漫画っぽさ」は出ます
> - 案C: 本物の4コマ漫画
>   **404枚。** 外注か、絵を描ける人が要ります。**現実的ではないと思います**
>
> **案Bを勧めます。** 「漫画」の要望に一番近くて、作業量が案Aとほとんど変わりません。

### ② 全101問に入れますか

> - **案A: まず最初の10問程度で試す**（おすすめ）
>   **効果があるか分からないうちに101問書くのは危ない**です。
>   数問で試して、良ければ広げる
> - 案B: 全101問に入れる
>   統一感は出ますが、**やり直しになったとき101問ぶん書き直し**です

### ③ 場面をどう見せますか（画面の縦が伸びます）

> いま問題画面は上から「タイトル → コード → 実行結果 → 設問 → 前提知識 → 回答欄」です。
> 場面を足すと**さらに縦に伸びます**。E11 でスマホ対応をしたばかりなので、
> **スマホだとコードに辿り着くまでスクロールが増えます。**
>
> - **案A: 最初から表示（短く2〜3行）**（おすすめ）— 読ませたいものなので隠さない
> - 案B: たたんでおいて、押すと開く — 場面を読まない人が出ます

### ④ 上司をどう描きますか ← **方針と関わります**

> `CLAUDE.md` に**ネガティブな言い方をしない**という決まりがあります。
> 「上司に怒られた」「ミスを指摘された」のような書き方は方針と衝突します。
>
> **「頼まれた」「引き継いだ」「相談された」**のような、
> **前向きな依頼の形**に統一することを勧めます。

---

## 設計上の要点（実装する人へ）

### 場面を採点に使わないこと

**AI には渡さない。表示専用。**

`prerequisite`（前提知識）が既にこの扱いになっています。同じにしてください。
渡すと**採点基準がぶれます**（場面の文章に引っ張られて点が動く）。

### `context` を使い回さないこと

`context` は**影響型の実行結果**で既に使われています（4問）。
**新しい欄を足すこと**（例: `scenario`）。

### 既存の設問との重複

いまの設問は「このコードを実行すると何が出力されますか」で完結しています。
場面を足すと**設問と内容が重なる**ことがあります。
**場面は「なぜ読むか」、設問は「何を答えるか」**と役割を分けてください。

---

## 手順

1. 上の4点を決める
2. **マイグレーションで欄を足す**（例: `scenario text`。**NULL 可にすること**。既存101問が入っている）
3. 問題画面に表示する
   - `app/problems/[id]/page.tsx` の `select` に足す（**`prerequisite` の出し方が手本**）
   - **`model_answer` と `rubric_items` は渡さないままにすること**
4. **まず数問だけ書いて画面で見る**（②で案Aの場合）
5. `ideas/問題作成ガイド.md` に書き方と例を追記
6. `ideas/db仕様.md` に欄を追記
7. `npm run test:db` `npm test` `npm run build`
8. **スマホ幅（375px）で、コードに辿り着くまでの長さを確認する**

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `supabase/migrations/`（新規） | `scenario` の追加 |
| `app/problems/[id]/page.tsx` | 取得と表示 |
| `problems/`（既存の .sql を更新） | 場面の文章 |
| `ideas/問題作成ガイド.md` | 書き方と例 |
| `ideas/db仕様.md` | 欄の追記 |

---

## 終わったと言える条件

- [x] 4点についてオーナーの判断を得ている（③ は票の3案ではなく「重ねて出す」に決まった）
- [x] 場面を入れた問題で、画面に表示される（order 1〜10）
- [x] **場面が空の問題でも画面が壊れない**（I-376c / E-472 の1問目）
- [x] **場面が採点に影響していない**（I-133 が採点API の列を固定。`scenario` を引いていない）
- [x] **スマホでコードに辿り着くまでが長くなりすぎていない**（閉じると DOM から消えるので 0px 増）
- [x] 文言に禁止語が入っていない（I-821）
- [x] `npm run test:db` / `npm test` / `npm run build` が通る
- [ ] **残り91問**（決定② により、効果を見てから広げる）

---

## 注意

- **場面を長く書かないこと。** 本題はコードを読むこと。**2〜3行**が上限
- **答えを場面に書かないこと。**「この関数はバグっているらしい」と書くと、
  読む前に結論を渡すことになります
- **絵を新規に描く判断は慎重に。** 101問ぶんは A2（101問作成）より大きい作業です
- **採点に渡さないこと。** `prerequisite` と同じ扱い
- **まず数問で試すこと。** 効果を見ずに101問書くと、やり直しが101問ぶんになります

---

## 作業記録

**2026-09-11 実施（ブランチ `task/A4`）。10問ぶんを投入して画面で確認済み。残り91問は未着手。**

### 決めたこと（すべてオーナー判断）

| | 決定 | 理由 |
|---|---|---|
| ① 見せ方 | **文章だけ。絵もマスコットも出さない** | 票の推奨は案B（マスコット＋吹き出し）だったが、**問題画面は既に設問をマスコットの吹き出しで出している。** 場面にも置くと同じ形の吹き出しが上下に2つ並び、マスコットが「案内役」と「依頼してきた人」を兼ねてしまう。オーナーが実機の並びを見て文章だけに変更（「キャラが問題画面にもいるなら最初のカードの方のキャラはなしで文章だけでいいよ」）。あわせて**難しい言葉を使わない**方針も指定された |
| ② 対象 | **まず10問（order 1〜10）** | 効果を見ないうちに101問書くと、やり直しが101問ぶんになる |
| ③ 見せ方の形 | **開いた直後に、薄暗い背景の上のカード。押すと消える**（票の3案のいずれでもない） | 票は「コードの上に常設 / たたむ / 設問に混ぜる」の3択で出したが、オーナーの指定は**重ねる形**だった。常設だとスマホでコードに辿り着くまでが伸びる（E11 で詰めたばかり）。重ねれば**読ませたいものを先に出して、閉じたあとの画面は1pxも変わらない** |
| ③-2 閉じたあと | **完全に消す**（読み返す導線を置かない） | 上部バーにボタンを残す案は採らなかった |
| ③-3 2回目以降 | **毎回出す**（初回だけにしない） | 「見た」を端末に残す作りが要る。加えて端末ごとに出方が変わる |
| ④ 依頼者 | **前向きな依頼の形だけ**（頼まれた・引き継いだ・相談された・説明することになった） | NG語（`CLAUDE.md`）と衝突しない。**機械で止める検査も入れた**（I-821） |

**③-2 と ③-3 を合わせると保存先が要らない**ので、`ScenarioIntro` は状態を外に持たない。

### 作ったもの

| ファイル | 内容 |
|---|---|
| `supabase/migrations/20260911220000_problem_scenario.sql` | `problems.scenario`（TEXT・NULL 可・**120字以内**の CHECK）。**適用済み** |
| `app/problems/[id]/ScenarioIntro.tsx` | カード本体（クライアント部品）。Esc・背景クリック・ボタンで閉じる／背後のスクロール止め／フォーカスをカードへ移す |
| `app/problems/[id]/page.tsx` | `select` に `scenario` を追加。入っている問題だけカードを出す |
| `problems/scenario-update.mjs` | **`scenario` 列だけを書き替える**投入スクリプト（下記） |
| `problems/scenario-004-005.data.mjs` / `.sql` | 4・5問目の場面（この2問だけ `data.mjs` が無いため別置き） |
| `problems/stage-001-003.data.mjs` / `stage-006-014.data.mjs` | 1〜3・6〜10 の場面。`.sql` は実データから再生成した |
| `problems/preflight.mjs` / `gen-sql.mjs` / `update.mjs` | 場面の検査4件・`--scenario` 記録生成・**誤用ガード**を追加 |
| `ideas/問題作成ガイド.md` / `ideas/db仕様.md` | 書き方・記入例・注意点・欄の定義 |

### 120字にした理由（`prerequisite` の400字より短い）

`prerequisite` は**開かない人がいる**が、場面は**閉じる前に必ず全員の目に入る。**
長いとコードに辿り着く前に読むのをやめるので、上限をそこに合わせた。実測は最長67字。

### `update.mjs` を場面の投入に使わなかった

あちらは1問まるごと（title / code / keywords …）を送り直す。場面を足すためだけに全列を
上書きすると、**DB 側にしか無い直しがあったときに、気づかないまま巻き戻る。**
`scenario-update.mjs` は `scenario` 列しか触らないので、その事故が起こりえない。

逆向きの事故（場面だけのファイルを `update.mjs` に渡すと `context` / `prerequisite` が
null で潰れ、しかも「✅ 更新した」と出る）も起きうるので、`update.mjs` の先頭にガードを足した。

### 追加した検査

**場面も「全員が読める場所」**なので、前提知識（I-815〜I-817）とまったく同じ制限がかかる。
そこに NG語の検査（I-821）が1件加わる ── **場面は、人が書いた文章がそのまま全画面に出る唯一の場所。**

| | 何を見るか |
|---|---|
| I-818 | 120字以内（DB制約の再確認） |
| I-819 | 模範解答の文を写していない（14字一致で見る目安） |
| I-820 | コードに無い採点キーワードが入っていない（写すだけで層1が取れるのを防ぐ） |
| I-821 | NG語が入っていない |
| I-814（更新） | 空文字列でない（`scenario` を追加） |
| E-472 / E-472b（E2E） | 開いた直後に出る／閉じると跡形もなく消える／Esc・背景でも閉じられる |
| I-376・I-376c（更新） | `select` の列が固定されている／3欄とも空でも描画できる |
| I-133（更新） | **採点API が `scenario` / `prerequisite` / `context` を引いていない** ＝ 採点に渡らないことの担保 |

`preflight.mjs` の4件は**わざと違反させて4件とも落ちることを確認済み。**

### 確認したこと

- `npm test` **828 passed** / `npm run test:db` **77 passed** / `npm run build` / `npx eslint`（変更分）すべて通る
- **E2E は display.spec.ts を desktop / mobile の2幅で実行し 50 passed / 2 failed。**
  落ちた2件は既知の `E-452`（`order=70` のタイトルに NG語「失敗」・**A2 側の話**で A4 とは無関係。
  `残タスク.md` §現在地 に同じものが記録されている）。
  ログイン済みだと `/` もステージ一覧を描くので、`/stages` だけでなく `/` でも当たる
- **実ブラウザ（Playwright・375px）で実測**
  - カードが出る → 文面が2行で入る → 「コードを読む」で消える
  - **閉じたあとのコード枠の上端は 174px**。場面が無い問題と同じ（`ScenarioIntro` は閉じると
    DOM から消えるので、構造上ずれようがない）
  - 背景は `bg-black/55`（実測 rgb(114,112,106)）。最初 `bg-ink/60` にしていたが、
    明色テーマの上では**暗くならず**ただ色が濁っただけだったので差し替えた
  - フォーカスは**ボタンではなくカードに当てる。** ボタンに当てると、マウスで開いただけの人にも
    フォーカスの枠が出た（`.focus()` が `:focus-visible` の判定を通す）

### 残っていること

1. **残り91問の場面。** 書き方・検査・投入経路はそろっているので、あとは文章を書いて
   `node problems/scenario-update.mjs <データファイル>` を流すだけ
2. **効果を見てから広げる**（決定②の趣旨）。10問で様子を見る
3. `E-452` の2件は A2 側（`order=70` のタイトル）

### ⚠️ C13 と統合するときの注意（2026-09-11 に発見）

**C13（登録なしで最初の数問を読めるようにする）が同じ `app/problems/[id]/page.tsx` を触っている。**
着手時点では `origin/main` に入っておらず、C13 のワークツリー側で作業中だった。

1. **`select` の列が消えると、場面は「出ないだけ」で誰も気づかない。**
   C13 は問題の読み取りを `loadProblem` という関数に切り出し、その中に
   `select` を**書き写している**。写した内容は A4 以前の9列（`scenario` が無い）。
   統合で後から入れるほうが気をつけないと、**列だけ静かに落ちて、
   カードが一生出ない状態**になる。エラーは出ない。
   `I-376`（`select` の列を1文字単位で固定している検査）が拾うはずだが、
   **C13 も同じ検査の期待値を書き換えている**ので、そこで9列のほうを採ると検査ごと通ってしまう。
   統合時は `page.tsx` と `tests/integration/server-components.test.ts` の**両方**で
   `scenario` が残っているかを見ること。

2. **ログイン前に見えるのはステージ1**（`PREVIEW_MAX_ORDER = 1`）で、
   **そのステージ1には A4 で場面が入っている。**
   つまり両方が入ると、**登録していない人が最初に見る画面が、全面のカードになる。**
   コードより先に「なぜ読むのか」が出るので狙いには合っているが、
   **初回訪問の第一印象が変わる**ので、オーナーの判断を仰ぐ価値がある。
   合わないと判断するなら、`ScenarioIntro` を出す条件に
   「ログインしている人だけ」を足すのが一番小さい直し方。

### 申し送り

- **E2E のスタブは `OPENAI_STUB_PORT` だけでは移らない。** `tests/e2e/support/fixtures.ts` の
  `STUB` は `OPENAI_STUB_URL` を見るので、ポートをずらすときは**両方**渡す
  （`OPENAI_STUB_PORT=4031 OPENAI_STUB_URL=http://127.0.0.1:4031`）。片方だけだと
  既定の 4010 に繋ぎにいって「fetch failed」で落ちる
- 再生成で `stage-001-003.sql` の**手書きの注意書き（A3 が足したもの）が消える。**
  `gen-sql.mjs` はヘッダを固定文で出すため。今回は書き戻した
