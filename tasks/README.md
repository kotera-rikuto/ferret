# tasks/ — 作業単位ごとの指示書

**このフォルダの使い方。**

Ferret の残作業を、**1ファイル = 1回のセッションで着手できる単位**に分けたもの。
セッションを始めるときは、このファイルで着手するタスクを選び、**そのタスクのファイルだけを読ませれば作業に入れる**ようにしてある。

---

## 作業する人（AIエージェント）へのお願い

### 1. 勝手に決めない

各ファイルに **「⚠️ オーナーに確認すること」** という節がある場合、
**そこはコードを書き始める前に必ず質問すること。** 黙って決めて進めない。

質問するときは、**専門用語を使わずに、選択肢とそれぞれを選んだ場合どうなるかを添えて**聞くこと。
オーナーは実装の細部まで把握しているとは限らないので、
「AとBどちらにしますか」だけでは判断できない。「Aにすると○○になり、Bにすると××になります」まで書く。

### 2. 前提のファイルを先に読む

各ファイルの冒頭に **「先に読むもの」** がある。ここに挙がっているものは着手前に読むこと。
書いていないものは読まなくてよい（読ませないために書いてある）。

### 3. 終わったら記録を残す

- そのタスクのファイルの末尾に、**やったこと・決めたこと・詰まった点**を追記する
- `残タスク.md` の該当項目にチェックを入れる
- **設計判断をしたら必ずその理由を残す。** 理由のない決定は、後で誰かが善意で元に戻す

### 4. 用語について

`何をするか` と `なぜ必要か` と `オーナーに確認すること` は**専門用語を避けて書く**こと。
`手順` と `触る場所` は正確さ優先でよい。

---

## 同時に複数のタスクを進めるときの決まり

> **複数を並行させるなら、まず [並列実行プラン.md](並列実行プラン.md) を見ること。**
> 衝突しない組み合わせを、バッチ単位（第1バッチ7本〜）にまとめてある。

タスクは分かれているが、**触るファイルは重なっている。** そのままだと後から衝突する。

### 守ること（3つ）

1. **1タスク = 1ブランチ。** `main` で直接作業しない
2. **着手する直前に `main` を取り込む。** 前のタスクの結果を持った状態から始める
3. **自分のタスクのファイル以外を「ついでに」直さない。** 気づいた点は
   そのタスクの `作業記録` に書いて、直すのは担当タスクに任せる

### 同時に進めてはいけない組み合わせ

**下の組は必ず順番にやること。** 同時に走らせると、後の人が手作業で直すことになる。

| 組み合わせ | 理由 | 順番 |
|---|---|---|
| **B2 ↔ B3** | 同じ指示文と、その内容を固定した検査を両方が書き換える | B2 → B3 |
| **B1 ↔ B2 / B3** | **測っている最中に採点基準が変わると、数字の意味が失われる** | B2 → B1 → B3 |
| **B1 ↔ A1 / A2** | 測っている最中に問題が増えると、前後の比較ができない | A → B1 |
| **A1 ↔ A2**、**A2 の章どうし** | どちらも問題を登録するため**表示順の番号が重複する** | 順番に1つずつ |
| ~~**E7 ↔ E8**~~ | ~~どちらもマップの位置計算を作り替える~~ | **両方とも✅済み。制約は消えた** |
| **E2 ↔ E6** | どちらもリザルト画面の点数まわりを組み替える | E2 → E6 |
| ~~**E5 ↔ E9**~~ | ~~どちらも `app/globals.css` に色の定義を足す~~ | **両方とも✅済み。制約は消えた** |
| **Z1 ↔ B3** | Z1 は実装を写すので、**動いている最中に書くと書いた先から古くなる** | 実装 → Z1 |
| **C5 ↔ すべて** | 公開は「いまの `main` をそのまま外に出す」作業 | **単独で行う** |

### ✅ F1 は完了済み（2026-08-17）

`middleware.ts` は **`proxy.ts` になった。** 認証を守っているファイルの名前が変わっただけで、
中身の処理は変わっていない。**以降のタスクは `proxy.ts` を見ること。**

C2・C3・E1 との衝突はこれで消えたので、順番の制約はもう無い。

### 複数のタスクが触るファイル（ホットスポット）

ここに載っているファイルを触るときは、**着手前に `main` を取り込むこと。**

| ファイル | 触るタスク |
|---|---|
| `app/result/[id]/ResultView.tsx` | E2 E3 E6（E1・E8 は**済**。E8 は結局この画面に触っていない） |
| `app/review/[id]/page.tsx` / `app/review/page.tsx` | （E1 で新設。E8 は触っていない ── 375px で崩れていなかった） |
| `app/api/score/route.ts` | B2 D1 E2 E3 G3 |
| `app/problems/[id]/page.tsx` | E4 E5（G2・E8 は**済**） |
| `app/stages/page.tsx` | D1 D2 E3（E7・E8 は**済**） |
| `lib/ai/prompt.ts` | B2 B3 G3 |
| `lib/ai/compose.ts` | B3 D1 E7 |
| `components/stage/StageMap.tsx` | （E7・E8 とも**済**） |
| `components/layout/AppSidebar.tsx` | C3（E1・E8 は**済**。E8 は結局この画面に触っていない） |
| `app/settings/page.tsx` | D2（C3・E9 は**済**） |
| `app/globals.css` | （E5・E9・E10 はすべて**済**）。**新しい色を足すと U-800 が落ちる／新しい動きを足すと U-811 が落ちる** ── 暗い配色と「動きを減らす」設定でどうするかを決めてから足すこと |
| `supabase/config.toml` | C1 C3 C4 C5 |
| `proxy.ts` | C3（C2 も E1 も**触らなかった**。法務文書はログイン不要なので `matcher` に足さないのが正解で、`/review` は**先に入れてあった**） |

### データベースの変更について

複数のタスクが `supabase/migrations/` にファイルを足す（C3・E2・E4）。
**ファイルが別なので衝突しない。** ただし次を守ること。

- ファイル名の日付は**作った時刻**にする（他のタスクと重ならない）
- **既存の行があるテーブルに「必須の欄」を足さない。** 追加する欄は必ず「空でもよい」形にする
- 適用の順番が入れ替わっても壊れない内容にする

---

## いまの状況

ブラウザで「ログイン → ステージ選択 → 問題 → 採点 → リザルト → ログアウト」が通る。
**まだ公開（デプロイ）していないし、課金も始めていない。**

**問題の件数・テストの件数などの数字は `残タスク.md` の「現在地」を見ること**
（ここに書くと二重管理になり、必ず片方が古くなる）。全体像も `残タスク.md`、プロダクトの前提は `CLAUDE.md`。

---

## 動かし方（作業を始める前に）

### アプリを起動する

```bash
npm run dev
```

`http://localhost:3000` で開く。
**ポートが使われていると別の番号（3001、3002…）になる。** 起動時の表示で確認すること。

### ログインする

**トップ画面はログインが必要。** 開発用のアカウントがある。

```
メールアドレス: dev@ferret.test
パスワード:     .env.local の DEV_USER_PASSWORD を見る
```

メール確認済みの状態で作られているので、そのままログインできる。

> ### ⚠️ パスワードをこのファイルに書かないこと
>
> **リポジトリは Public。** 以前ここに平文で書いてあり、**2026-08-22・C5 で
> パスワードを変更したうえで記載を消した**（`TASKS.md` も同時）。
> 置き場所は `.env.local`（コミットされない）だけにしてある。
> E2E 用アカウント（`e2e@ferret.test`）も同じ理由で `.env.local` の
> `E2E_USER_PASSWORD` に移した ── **未設定だと E2E がその旨のエラーで止まる**
> （`tests/e2e/support/fixtures.ts`。以前は既定値でそのまま動いていた）。

### テストを流す

```bash
npm test              # 全部（実DBに繋ぐものは飛ばされる）
npm run test:unit     # 計算やロジックだけ。速い
npm run test:db       # 実DBに繋いで問題データの健全性を見る（問題を追加したら必ず）
npm run test:e2e      # ブラウザを自動で動かす
npm run lint          # 書き方の検査
npm run build         # 本番と同じ形で組み立てられるか
```

**どのタスクでも、最後に `npm test` と `npm run build` を通すこと。**

> **別セッションと同時に E2E を回すときは、ポートを3つ揃えてずらす**（2026-08-19・C3 で判明）。
>
> ```bash
> E2E_PORT=3101 OPENAI_STUB_PORT=4011 OPENAI_STUB_URL=http://127.0.0.1:4011 npx playwright test
> ```
>
> ずらさないと `playwright.config.ts` の `reuseExistingServer` が**別の作業ツリーで動いている
> dev サーバー（3100）を使い回す**ので、自分の変更ではないコードをテストしてしまう。
> **`OPENAI_STUB_URL` を忘れると全テストが `TypeError: fetch failed` で落ちる**
> （テスト側は `OPENAI_STUB_PORT` を見ておらず、既定の 4010 を叩くため）。
> なお **DB は共有のまま**なので、同時実行では相手の回答履歴を壊しうる点は変わらない。

### データベースの中身を見る

Supabase の管理画面 → **SQL Editor** で直接見られる。
表の定義は `ideas/db仕様.md`。

### 採点を試すとき

**1回あたり約4円の実費がかかる。**（OpenAI の利用料）

- 何度も試すときは残高を確認しておくこと
- **同じ回答をもう一度送ると、前の結果をそのまま返す**（費用はかからない）。
  毎回本当に採点させたいときは、回答の文言を少し変えること
- 連続で叩くと使いすぎを止める仕組みに当たる（1分に10回まで）

---

## どれから着手するか

**迷ったら B3 から。** 問題コンテンツが唯一の停滞要因で、それ以外は「やりたくなった時にやればよい」もの。

```
【本線】
  ✅ E4 ──→ ✅ A1 ──→ ✅ B1 ──→ B3 判定基準を直す ──→ A2 問題の量産
                                （B1 で穴は core 1点に絞れた）

【公開したくなったら】
  C1 メール送信 ──→ C6 独自ドメイン ─┐
  ✅ C2 法務文書 ────────────────────┼─→ C5 本番デプロイ
  ✅ C3 せってい画面（+ ✅ C7）───────┤        ↑
  ✅ C4 認証設定 ────────────────────┘   C1〜C4・C6 が終わってから
                                          **+ 残タスク.md §C-4 の公開ゲート**
                                          （2・3 は C3 で、4 = E-455 は G2 で解消。
                                          　**残るは 1 = 法務レビューだけ**）

  ※ C6 は C1 の実測で「先送りできない」と判明して独立した票。
     お試しドメインからのメールは iCloud が拒否する（費用と名前の判断が要る）
  ※ C7 は C4 の作業中に見つかった票（登録済みメールの案内文）。小さいので C3 とまとめてよい。
     C5 のブロッカーではない

【課金を始めるなら】
  D1 採点の上限 ──→ D2 Stripe決済     ※ C（公開）の後

【いつでも】
  B3（残る候補2）
  ※ B2・E1・E2・E3・E5・E6・E7・E8・E9・E10・F3・G2・Z1 は完了（E3・E6 の票は tasked/ へ移動中）
```

---

## 一覧

### A. 問題コンテンツ（本線）

| | タスク | 規模 | 前提 |
|---|---|---|---|
| [A1](A1-読解型の検証.md) | 読解型4種を1問ずつ作って採点を通す | 中 | 影響型のみ **E4** の後 |
| ~~[A2](A2-問題の量産.md)~~ | ~~問題を作る~~ | 特大 | ✅ **2026-08-19 完了。100 / 100 問。** 記録は `problems/stage-*.md` |

### B. 採点の信頼性

| | タスク | 規模 | 前提 |
|---|---|---|---|
| ~~[B1](../tasked/B1-採点のブレを測る.md)~~ | ~~ゴールデンセット50件で実測する~~ | 大 | **✅ 2026-08-17 完了**（`npm run golden`） |
| ~~[B2](../tasked/B2-誤読の記録とフィードバック改善.md)~~ | ~~どの誤読に当たったか記録し、案内を具体的にする~~ | 中 | **✅ 2026-08-17 完了**（`matched_reject`・p2 → p3） |
| [B3](B3-判定基準の調整.md) | 測った結果をもとに採点基準を直す | 中 | **B1 必須 → ✅ 済み。着手できる** |

### C. 公開（デプロイ）前に必要

| | タスク | 規模 | 前提 |
|---|---|---|---|
| [C1](C1-メール送信をResendに切り替える.md) | 確認メールが実際に届くようにする | 中 | **✅ 仕組みは完成（2026-08-17）。実配信は C6 待ち** |
| ~~[C2](../tasked/C2-法務文書をそろえる.md)~~ | ~~利用規約・プライバシーポリシー等~~ | 中 | **✅ 2026-08-18 完了（画面と下書き）。** 残りは**専門家の確認**で、`残タスク.md` §C-4 の公開ゲートに移した |
| ~~[C3](../tasked/C3-せってい画面.md)~~ | ~~退会・パスワード変更・メール変更~~ | 大 | **✅ 2026-08-19 完了。** 退会とパスワード変更を実装（**公開ゲート2 を解消**）。**メール変更だけ C6 待ちで「準備中」表示** |
| ~~[C4](../tasked/C4-Supabaseの認証設定.md)~~ | ~~管理画面での設定（コードでは直せない）~~ | 小 | **✅ 2026-08-18 完了。** ただし**流出パスワードの拒否はプラン制限で入らず**、`残タスク.md` §C-3 に残してある（期限は D1 着手時） |
| [C6](C6-独自ドメインを用意する.md) | メールが誰にでも届くようにする | 中 | **判断は全部済み（2026-08-18・`ferretcode.com`）。ドメインの購入と Resend の認証待ち** |
| ~~[C7](../tasked/C7-登録済みメールの案内文.md)~~ | ~~届かない確認メールを待たせないよう一文足す~~ | 小 | **✅ 2026-08-19 完了（C3 と同梱）** |
| ~~[C5](C5-本番デプロイ.md)~~ | ~~Vercel へ公開する~~ | 中 | **✅ 2026-08-22 完了。https://ferretcode.com 稼働中**（`main` への反映で自動公開）。残りはブラウザでの通し確認と OpenAI の使用量上限 |
| [C8](C8-検索に載るようにする.md) | 検索で見つかるようにする（sitemap・robots・メタデータ・Search Console） | 小 | **C5 の後。着手前に OpenAI の使用量上限を入れること**（D-1 未実装のため） |

### D. 課金を始める前に必要

| | タスク | 規模 | 前提 |
|---|---|---|---|
| [D1](D1-プラン別の採点上限.md) | Free/Pro ごとの回数制限 | 大 | なし |
| [D2](D2-Stripe決済.md) | 支払い・解約 | 特大 | **D1** の後（C2 は✅済み）。**特商法の表記と住所の決定はここでやる**（C2 で保留した） |

### E. 画面・体験

| | タスク | 規模 | 前提 |
|---|---|---|---|
| ~~[E1](E1-振り返り画面.md)~~ | ~~採点の内訳を見せる画面~~ | 中 | **✅ 2026-08-19 完了。** `/review/[id]`（内訳）と `/review`（といた問題の一覧）。サイドバーの「準備中」も外した。**採点の計算には触っていない** |
| ~~[E2](E2-フィードバックの2枠表示.md)~~ | ~~「よかったところ」「つぎの一歩」を分ける~~ | 中 | **✅ 2026-08-18 完了**（`ai_praise` / `ai_next_focus`。過去の行も再採点せず分け直した）。**E2E だけ未実行 → `残タスク.md` §C-4 の公開ゲート** |
| [E3](E3-XPとレベル.md) | 経験値の加算と表示 | 中 | **判断あり** |
| [E4](E4-問題データに欄を足す.md) | 実行結果と前提知識の欄を足す（旧 E4 + 旧 G4） | 小〜中 | **判断あり・A1 と A2 をブロック** |
| ~~[E5](../tasked/E5-コードの色付け.md)~~ | ~~コードを読みやすく色分けする~~ | 中 | **✅ 2026-08-18 完了**（Shiki・既製テーマ `gruvbox-dark-hard`） |
| [E6](E6-0点の見せ方.md) | 0点が罰に見えないようにする | 小 | なし |
| ~~[E7](../tasked/E7-マップの仕上げ.md)~~ | ~~パーフェクト演出・位置ずれの修正~~ | 小 | **✅ 2026-08-17 完了**（演出は 2026-08-18 に案A で確定） |
| ~~[E8](../tasked/E8-スマホ対応.md)~~ | ~~狭い画面のレイアウト~~ | 大 | **✅ 2026-08-19 完了。** オーナー判断で「進行を妨げる所＋ログイン/ログアウト」。**実測したら壊れていたのは2か所だけ**（マップの吹き出しの重なり／問題画面の固定バー）。**パソコン側は座標一致を確認済み** |
| ~~[E9](E9-ダークテーマ.md)~~ | ~~暗い配色への切り替え~~ | 小 | **✅ 2026-08-19 完了。** オーナー判断で**せってい画面のボタンだけ**（端末のダークモードには追従しない）。覚えるのはブラウザ側 |
| ~~[E10](../tasked/E10-マスコットの動きを付ける.md)~~ | ~~マスコットに凝った動きを付ける~~ | 中 | **✅ 2026-08-19 完了。** オーナー判断で**今の絵を部品に切って動かす**・**動かすのは2か所**（採点待ち・リザルトのクリア時）。**Lottie は入れていない**（CSS で足りたため）。`prefers-reduced-motion` にも新規対応 |
| [E11](E11-スマホ版レイアウトの不具合を直す.md) | 続けた日数が出ない・メモの位置・画面の跳ねを直す | 中 | なし（**E8 の続き**） |

### F. 技術的な後片付け

| | タスク | 規模 | 前提 |
|---|---|---|---|
| ~~[F1](../tasked/F1-middlewareをproxyに移行.md)~~ | ~~非推奨の警告を消す~~ | 小 | **✅ 2026-08-17 完了** |
| ~~[F2](../tasked/F2-導入見送りの判断.md)~~ | ~~shadcn/ui・Lottie を入れるか決める~~ | 小 | **✅ 2026-08-18 完了**（shadcn/ui は見送り / Lottie は入れる → E10） |
| ~~[F3](../tasked/F3-E2Eの古いアサーションを直す.md)~~ | ~~ブラウザの通しテストが全部落ちているのを直す~~ | 中 | **✅ 2026-08-18 完了** |
| [F4](F4-セキュリティレビューのエージェントを作る.md) | 「レビューして」で指摘が返る仕組みを作る | 中 | なし（**D2 の前に欲しい**） |
| [F5](F5-保守性を上げる.md) | カバレッジを測り、QAの観点を形にする | 大 | なし（**F4 とまとめると形が揃う**） |

### M. マーケティング・集客

**コードより判断と継続作業が主。** どちらも **C5（公開）が済んだので着手できる。**

| | タスク | 規模 | 前提 |
|---|---|---|---|
| [M1](M1-技術記事でメディアを育てて100人集める.md) | 技術記事を書いて登録100人を目指す | 特大 | **C5 ✅済み。** M2 とセット |
| [M2](M2-LPページを作る.md) | 何のサービスか説明して登録に繋げる | 大 | なし（**M1 の着地点**） |

### G. これから作りたい機能（仕様未定）

**いずれも「まず仕様を決める」ところから。オーナーとの相談が主作業。**

| | タスク | 規模 | 前提 |
|---|---|---|---|
| [G1](G1-共有機能.md) | 結果を人に見せられるようにする | 未定 | **仕様決めから** |
| ~~[G2](../tasked/G2-メモ書き機能.md)~~ | ~~問題を読みながらメモを取れるように~~ | 小 | **✅ 2026-08-19 完了。** 端末の中だけに保存（案A）・採点後も残す・回答欄の上。**公開ゲート4（E-455）も同梱で解消** |
| [G3](G3-AI相談ボット.md) | 答えは教えずに相談できる相手 | 特大 | **D1 の後・判断多数** |

### Z. ドキュメント

| | タスク | 規模 | 前提 |
|---|---|---|---|
| ~~[Z1](../tasked/Z1-ドキュメントを実装に合わせる.md)~~ | ~~`CLAUDE.md` 等の古い記述を直す~~ | 小 | **✅ 2026-08-19 完了。** `TASKS.md` は完了記録、`残タスク.md` が残タスクの正（案A） |

---

## 規模の目安

| | 意味 |
|---|---|
| 小 | 1セッションの一部で終わる |
| 中 | 1セッションで終わる |
| 大 | 1セッションいっぱい使う。分割してもよい |
| 特大 | 複数セッションに分ける前提 |

---

## 共通の決まりごと（全タスク）

`ideas/セキュリティ_残課題.md` §4 より。**新しく何か作るときは必ず守ること。**

- **認証が要る画面を追加したら** `proxy.ts` の `matcher` に追加する
- **問題に紐づく画面を追加したら** `lib/progress/unlock.ts` の `loadProgress` で解放判定する
  （画面と採点APIで判定がズレると、そこが抜け道になる）
- **新しい POST の API を作ったら** 先頭に `isCrossSiteRequest` と `hasJsonContentType` を置く
  （`lib/http/origin.ts`。`app/api/feedback/route.ts` が最新の手本）
- **ユーザーの入力を外部AIに送るなら** 個人情報の検査を通す（`app/api/score/route.ts` の `containsPii`）
- **DBを変えるときは** `supabase/migrations/` にファイルを足して `supabase db push`。手で管理画面から変えない
- **作業の最後に** `npm test` と `npm run build` を通す
