# A2. 残り94問を作る

**種別:** コンテンツ作成
**規模:** 特大（複数セッションに分ける前提）
**前提として終わっていること:** **A1 必須・B1 必須**
**関連:** E4（前提知識の欄）を入れるなら、**着手前に方針を決めておくこと**

> ### ⚠️ 同時進行の注意
> **A1・B1 と同時に進めないこと。** A1 とは**表示順の番号が重複する**（`npm run test:db` が検出）。
> B1 とは、測っている最中に対象が増えて比較できなくなる。
> **章ごとに分けて複数セッションで進める場合も、同時に走らせないこと。**
> 番号の重複を避けるため、**1章ずつ順番に**行う。
> アプリのコードは触らないので、コードの衝突は起きない。

**先に読むもの**
- `ideas/問題作成ガイド.md`（**必読**）
- `ideas/問題構成案.md`（100問の一覧）
- `problems/stage-001-005.md` と `.sql`（手本）
- A1 の作業記録（読ませ方ごとの注意点）

---

## 何をするか

100問の構成のうち、**残り94問を作って登録する。**

現在の登録状況:

| | |
|---|---|
| 登録済み | 6件（本番用5件 + 動作確認用1件） |
| 残り | 94件 |
| 内訳 | JavaScript 80問（1〜80） / TypeScript 20問（81〜100） |

---

## なぜ必要か

**これが唯一の停滞要因。** 仕組みは動いているので、あとは中身。

5問では「積み上げて登る」体験にならない。

---

## ⚠️ 着手前に必ず確認すること

### 1. A1 と B1 が終わっているか

**終わっていないなら、このタスクを始めてはいけない。**

採点の物差しが固まる前に量産すると、後で調整が入ったときに
**作った問題の採点基準を全部書き直すことになる。** これが一番高くつく失敗。

### 2. 前提知識を入れるかどうか（E4 で決まっているはず）

> 各問題に「この問題を解くのに必要な予備知識」を付ける構想があります（E4）。
> **これを入れるなら、問題を作り始める前に決めてください。**
>
> - **入れる場合:** 1問あたりの作業が増えます（解説を1本書くぶん）。
>   ただし94問すべてに一度に書くので、後から追加すると**94問を作り直すことになります**
> - **入れない場合:** いまの形式のまま進めます。後から足すなら、
>   そのとき改めて94問ぶんの解説を書くことになります
>
> **いま決めるほうが安いです。**

### 3. 進め方

> 94問をどう進めますか。
>
> - **案A: 章ごとにまとめて作る**（第1章8問 → 第2章8問 …）
>   関連する内容が続くので作りやすい。ただし後半の章の問題が長く未検証のまま
> - **案B: 各章から少しずつ作る**
>   全体の難易度バランスを早く見られる。ただし文脈が飛ぶので作りにくい
>
> **おすすめは案A**です。1章ぶん（8問前後）が1セッションの区切りとして扱いやすく、
> `npm run test:db` の健全性チェックも章単位で回せるためです。

---

## 手順

**1章ぶん（8問前後）を1セッションの単位にする。**

1. `ideas/問題構成案.md` から対象の章を選ぶ
2. 章内の各問題について `ideas/問題作成ガイド.md` に従って作る
3. `problems/stage-XXX-YYY.md` に中身、`.sql` に登録用SQLを書く
   - **`id` は書かない**（自動で振られる。書くとエラー）
   - `"order"` は引用符が必要
4. Supabase の SQL Editor で登録
5. **`npm run test:db` を実行**（健全性チェック。下記参照）
6. 章の中から**2〜3問を抜き取ってブラウザで実際に解く**
   - 全問やる必要はないが、**新しい読ませ方を使った問題は必ず解くこと**
7. `ideas/問題構成案.md` に進捗の印をつける

### `npm run test:db` が見てくれること

自動で以下を確認してくれる。**登録したら必ず流すこと。**

- 模範解答がキーワードで満点を取れるか
- キーワードが設問文に入っていないか（写して点が入るのを防ぐ）
- 採点基準の中核が1つに絞れているか
- 表記のゆれが十分あるか
- 順番の重複・タイトルの重複がないか
- キーワードがちょうど4組あるか
- 章の範囲に収まっているか

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `problems/`（新規） | 問題の中身と登録用SQL |
| Supabase の SQL Editor | 登録 |
| `ideas/問題構成案.md` | 進捗の印 |

**アプリのコードは触らない。**

---

## 終わったと言える条件

**章ごとに:**
- [ ] その章の全問が登録されている
- [ ] `npm run test:db` が通る
- [ ] 2〜3問をブラウザで解いて、意図した点数帯になっている

**全体として:**
- [ ] 100問が登録されている
- [ ] 各章の難易度が階段になっている（前の章より急に難しくなっていない）

---

## 注意

- **設問文にキーワードを書かない。** 回答者が写すだけで点が入る
- **中核の判定は「結論を1つだけ」書く。** 2つ以上詰めると、正しいが短い回答が落ちる
  （`ideas/問題作成ガイド.md` に実例あり）
- **「よくある勘違い」は2件以上必須。** データベース側で強制されている
- 途中で採点の物差しを変えたくなったら、**そこで量産を止めて B3 に移ること。**
  変えてから残りを作らないと、前半と後半で基準が変わる
- **1問あたりの費用感:** 作った問題の動作確認で数回採点するので、1問あたり数円程度

---

## 作業記録

### 第1バッチ（ステージ 6〜14）── 2026-08-18 完了

**第1章の残り2問 + 第2章まるごと7問 = 9問。** ブランチ `worktree-a2-problems`（`origin/main` から）。
**詳細は `problems/stage-006-014.md` を正とする**（設計の意図・実採点の結果・観点の内訳）。

**投入済み: 18 / 100 問（残り 82 問）。第1章・第2章は完了。**

#### 着手前に決めたこと（オーナー判断・2026-08-18）

| | 決定 | 理由 |
|---|---|---|
| 前提知識 | **全問に付ける** | 既存の本番用9問すべてに入っており、途中から消えると画面の見え方が変わる。あとから 91問ぶんを足すより安い |
| 進め方 | **章ごとにまとめて** | 1章が1セッションの区切りとして扱いやすく、健全性チェックも章単位で回せる |
| 最初の範囲 | **6〜14** | 第1章に2問の穴を残したまま先へ進まない |
| 登録の経路 | **担当（AI）が直接 DB に入れる** | 91問ぶんの貼り付け作業が消える。**投入前に中身を見せて確認を取ってから実行する**運用 |

> **票の数字が古かった。** 「登録済み6件・残り94問」と書かれていたが、着手時点の実データは
> **10件（本番用9 + 動作確認用1）・残り91問**だった。A1 の4問が反映されていない。
> **票の冒頭の数字は着手時に実データで確かめること。**

#### 作ったもの

| ファイル | 内容 |
|---|---|
| `problems/stage-006-014.data.mjs` | **投入内容の正。** 9問のデータ |
| `problems/stage-006-014.md` | 設計の意図・検証結果・論点 |
| `problems/stage-006-014.sql` | 記録用。**投入済みの実データから生成**（手書きしていない） |
| `problems/preflight.mjs` | **投入前**の機械検査（I-801〜I-817 相当 + DB制約 + 個人情報検査） |
| `problems/run-code.mjs` | 問題のコードを実際に実行して出力を出す |
| `problems/insert.mjs` / `gen-sql.mjs` | 投入と SQL 生成 |
| `problems/unlock-seed.mjs` | ブラウザ確認用にステージを開ける／後片付け（印つき） |
| `problems/progress.mjs` / `title-leak-survey.mjs` | 状態確認・調査用 |

**アプリのコードは1行も触っていない**（票の方針どおり）。触ったのは `problems/` と
`ideas/問題構成案.md` の進捗印だけ。

#### 結果

- `node problems/preflight.mjs` → 通過。**模範解答は9問とも層1で 20/20**
- `node problems/run-code.mjs` → **9問とも模範解答に書いた出力と実行結果が一致**
- `npm run test:db` → **63 passed**
- ブラウザで 6・9・14 を実際に解いた → **「短くても正しければ通る」3/3 / 「結論が逆なら通らない」3/3**（実費 ≒ ¥0.32）

### 第2バッチ（16〜24・第3章）── 2026-08-19 完了

**詳細は `problems/stage-016-024.md`。** 15 は A1 で投入済みなので残り9問。
`npm run test:db` 63 passed。**27 / 100 問。**

ブラウザで 17（ズレ）・21（TDZ）・23（意図）を実採点 → **短い正答 3/3 クリア / 逆 3/3 不合格**。

**投入前の機械検査が5件捕まえた。** I-816（前提知識が模範解答と14字一致）2件、
I-817（前提知識にキーワード）2件、I-802（設問文にキーワード）1件。
**すべて投入前に潰れており、DB から消して入れ直す往復が起きていない。**

見つけて直したもの2つ。

1. **解放用に置いた行が採点の枠を食い潰していた。** `RATE_LIMITS`（1分10件）は
   `user_attempts.created_at` の直近24時間を数えるので、印つきの行を14件まとめて入れた直後の
   本命の採点が **429 で弾かれた。** → `unlock-seed.mjs` が日時を1週間前にずらすようにした
2. **`run-code.mjs` が例外の手前までの出力を捨てていた。** 21 は「2行出てから止まる」ことが
   答えそのものなので、これでは確認にならなかった

### 第3バッチ（25〜29・第4章）── 2026-08-19 完了

**詳細は `problems/stage-025-029.md`。** 5問。`npm run test:db` 63 passed。**32 / 100 問。**

ブラウザで 26（`?.` で落ちる）・28（螺旋の回収・ズレ）を実採点
→ **短い正答 2/2 クリア / 逆 2/2 不合格**。

**28 は 4・5（参照）の回収先。** 前提知識にステージ4・5への言及を入れ、
そこで学んだことが効くと分かるようにしてある。

### 第4バッチ（30〜40・第5章）── 2026-08-19 完了

**詳細は `problems/stage-030-040.md`。** 11問。`npm run test:db` 63 passed。**43 / 100 問。**

ブラウザで 33（17の回収）・35（reduce の第2引数）・36（4の回収）を実採点
→ **短い正答 3/3 クリア / 逆 3/3 不合格**。

**螺旋の回収を2本とも回収した。** 33 ← 17（`return` 忘れがコールバックで再発）、
36 ← 4（蓄積オブジェクトを壊すか複製するか）。どちらも**前提知識と模範解答の両方で
回収元のステージ番号に触れてある** ── 「前に見たあれだ」と繋がらないと螺旋の効果が出ないため。

**投入前の機械検査が3件捕まえた。** うち1件は新しい種類の崩れ方だったので下に分けて書く。

> #### キーワードは語幹で持つこと（32 で踏んだ）
>
> スロットに `反映されな` と書いたが、模範解答は「反映され**ません**」で、
> **活用が違うので当たらず I-801（模範解答が層1で満点）が 18点で落ちた。**
> `反映され` に直して通った。
>
> **否定の形が複数ある語（〜ない / 〜ません）は、否定まで含めると片方しか拾えない。**
> 語幹で持てば両方に当たる。ただし語幹だけだと肯定形にも当たるので、
> **結論の向きを層1で判定させないこと**（そこは層2の `core` の仕事、というのが v3 の設計）。

### E2（フィードバックの2枠表示）の取り込み ── 2026-08-19

E2 セッションから依頼があり、`origin/main` を取り込んだうえで
`node supabase/backfill/20260818224500_split_feedback.mjs --apply` を流した。

- **下見（`--apply` なし）で内容を確認してから適用。10 / 10 件を更新。** 再実行で0件（冪等）
- **点数は1点も動いていない。** 触るのは `ai_praise` / `ai_next_focus` の2列だけ
- 残った2件（`problem=5`・動作確認用）は**1枠のままが正しい**
- **新しい採点では2列に分かれて入ることも実測で確認**（第5章のブラウザ確認6件）

#### 次のバッチへの申し送り

1. **`node problems/preflight.mjs` を投入前に必ず回すこと。** A1 の申し送り3 の実装。
   実際に投入前に1件捕まえた（前提知識に採点キーワードが入っていた）
2. **`node problems/run-code.mjs` も必ず回すこと。** トレース型は模範解答の出力が違うと問題ごと壊れる。
   機械検査は書式しか見ないのでここは別に要る
3. **正答を先に送る。** 進行判定は最高点なので、誤答から送ると次のステージが開かず確認が止まる
4. **`unlock-seed.mjs` の印つきの行は必ず `clean` で消すこと。** 残すと実測値と混ざる
5. **前提知識は模範解答と表現がぶつかりやすい**（I-816 で2件落ちた）。同じことを説明するので当然だが、
   14字連続で一致すると落ちる。意識して言い回しを変えること
6. **意図型は設問文にキーワードが漏れやすい。** 「なぜ `return` を並べる形にしたのか」と書くと
   `return` も「入れ子」も設問に入る。**設問では手段の名前を出さず、何をしているかだけ書く**（19 がその形）
7. **キーワードは語幹で持つ**（第4バッチの 32 で踏んだ）。`反映されな` は「反映されません」に当たらない
8. **次は第6章（41〜48 の8問）。** 螺旋の回収は無い。
   **42（数値の罠）は 6（`+` の二つの顔）と地続き**なので、そちらを読んでから組むとよい。
   残る回収は 72 ←46・57 ／ 96 ←11 ／ 97 ←65 ／ 80 ←30 の4本で、
   **すべて回収元が先に来る並びなので順番に作れば満たせる**

#### オーナーに判断をお願いしたい点（量産は止めていない）

**採点キーワードがタイトルからも写せる。** `I-802` は設問文しか見ておらず、
タイトルも同じ画面に出るので同じことが起きる。既存の本番用9問のうち**4問**、
本バッチの9問のうち**5問**が該当（`node problems/title-leak-survey.mjs`）。

`preflight.mjs` では**警告どまりにして落としていない。** 既存がこの作り方なので、
ここで必須にすると新しい問題だけ厳しい基準になり、票が禁じている
「前半と後半で基準が変わる」状態そのものになるため。

実害は限定的で、検証済みの引用が無い回答は層1が10点で頭打ちになり（`compose.ts`）、
**タイトルを写しただけではクリアに届かない。** しかも塞ぐと逆効果になりうる
（タイトルの語を避けると、最も自然な正答の言い回しがキーワードを外す）。
→ **直すなら B3 で。**
