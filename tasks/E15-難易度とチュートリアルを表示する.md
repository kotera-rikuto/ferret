# E15. 難易度の星と「チュートリアル」の印を出す

**種別:** 実装
**規模:** 中
**前提として終わっていること:** なし
**関連:** A3（最初の数問をチュートリアルに）✅済み。**その印を画面に出す話**

> ### ⚠️ 同時進行の注意
> `components/stage/StageMap.tsx` と `app/stages/page.tsx` を触る。
> **E14（画面遷移を速くする）も `app/stages/page.tsx` を触る。** 片方ずつ。

**先に読むもの**
- `components/stage/StageMap.tsx`（**吹き出しの中身と、`Stage` 型の作り**。
  冒頭のコメントに「`status` の4つ目の値にしない」理由が書いてある ── 同じ判断が要る）
- `app/stages/page.tsx`（`Stage` を組み立てているところ）
- `lib/stages/chapters.ts`（章の範囲。`order` の数字で決まる）

---

## 何をするか

2つ。どちらもステージ選択の画面まわり。

### ① 挑む前に難易度を星で出す

丸を押すと出る吹き出し（「このステージに挑みますか？」）に、**星で難易度を出す。**

### ② チュートリアルの問題に印を出す

マップの**丸の上に「チュートリアル」**と出す。

A3 で最初の数問を素直な問題にしたが、**画面上は他と見分けが付かない。**

---

## なぜ必要か

**①は「何に挑むのか」が分からないまま押していること。**

いまの吹き出しはタイトルと「挑みますか？」だけです。
**難しさが分からないと、時間があるときに難問を選ぶ、といった選び方ができません。**

**②は A3 の意図が伝わっていないこと。**

最初の数問を意図的に素直にしたのに、**画面では何の説明も無い**ので、
経験者から見ると「簡単な問題が並んでいる」だけに見えます。
**「ここは肩慣らし」と分かれば、簡単さが意図として伝わります。**

---

## 実測した事実（2026-09-12）

### 難易度は 1〜5 の全域に散っている

```
難易度1:   1問  ★
難易度2:   8問  ★★
難易度3:  27問  ★★★
難易度4:  48問  ★★★★
難易度5:  16問  ★★★★★
```

⚠️ **オーナーの言う「星3〜5個」と食い違う可能性があります。**
**実際のデータは1〜5**で、しかも**4が48問と最多**です。
「星3〜5個で出す」が

- **「5個中いくつ」という意味**なら、そのまま `difficulty` を使えます
- **「3個から5個の範囲で出す」という意味**なら、いまの1〜5を3〜5に**割り当て直す**ことになります

**着手前に確認してください**（下記）。

### チュートリアルの印はどこにも無い

`problems` にも `lib/stages/` にも、**「これはチュートリアル」を表すものがありません。**
A3 は問題の中身を書き換えただけで、印は作っていません。

### 吹き出しはすでにある

`StageMap.tsx` の `selected` で開く部分。**入れ物は用意されています。**

---

## ⚠️ オーナーに確認すること

> **① 「星3〜5個」の意味を教えてください** ← **これが決まらないと作れません**
> いまデータに入っている難易度は **1〜5** で、内訳は
> ★1が1問・★2が8問・★3が27問・★4が48問・★5が16問です。
>
> - **案A: いまの数字をそのまま星の数にする**（おすすめ）
>   ★1〜★5 が出ます。**データを触らなくて済みます**
> - 案B: 3〜5の範囲に寄せる
>   「簡単な問題でも★3」という見え方になります。
>   **101問ぶんの数字を割り当て直す**ことになります
>
> **案Aを勧めます。** ★1が1問しかないのは A3 でチュートリアルを作った結果で、
> **むしろ「ここは易しい」が star の数で伝わります。**
>
> **② 難易度をどこに出しますか**
> - **案A: 吹き出しの中だけ**（おすすめ）— 挑む直前に見える。マップは今のまま
> - 案B: マップの丸にも常時出す — **101個の丸すべてに星が並ぶ**ので、うるさくなります
>
> **③ どの問題を「チュートリアル」にしますか**
> 印を付ける対象を決める方法が2つあります。
> - **案A: `order` が N 以下**（おすすめ）— 数字で決まる。**データを触らない**
> - 案B: `problems` に印の欄を足す — 問題ごとに選べるが、**欄が1つ増える**
>
> **A3 で素直にした範囲と揃える**のが自然です。何問目までかを教えてください。
>
> **④ 「チュートリアル」の表示をどう出しますか**
> - **案A: 丸の上に小さい帯**（ご指示のとおり）
> - 案B: 章の見出しに「第1章（チュートリアル）」と出す — 丸まわりが混まない
>
> ⚠️ **①で「チュートリアル」という語をそのまま画面に出すか**も決めてください。
> `CLAUDE.md` に**ネガティブな言い方をしない**方針があり、
> 「初心者向け」のような言い方は使えません。**「チュートリアル」自体は問題ありません**が、
> 「肩慣らし」「まずはここから」のほうが Ferret の言い方に近いかもしれません。

---

## 設計上の要点

### `status` に4つ目の値を足さないこと

`StageMap.tsx` の `Stage` 型の冒頭に、**満点（`perfect`）を `status` の4つ目にしなかった理由**が
書いてあります。

> `status === "cleared"` で書かれている判定（道の実線・ボタンの文言・見た目）が
> すべて満点ノードを取りこぼす。独立した欄にして「クリア済みに一枚重ねる」形にしてある。

**チュートリアルも同じです。** チュートリアルの問題は「未開放」にも「クリア済み」にもなります。
**`status` ではなく独立した欄**（`difficulty` と `tutorial`）として足してください。

### マップが縦に伸びないようにすること

`StageMap.tsx` は**行の高さ（`--row-h`）とノードの高さ（`NODE_H`）を数字で持っています。**
丸の上に帯を足すと、**ラベルが1つ上の行に重なります**（狭い画面で実際に起きた経緯がコメントにあります）。

**帯を足すなら、`NODE_H` と `--row-h` の対応を見直すこと。**
`tests/unit/stage-map.test.ts` の U-820 / U-823 が、この対応を見ています。

---

## 手順

1. 上の4点を決める
2. `Stage` 型に `difficulty` と（③で案Aなら）チュートリアルかどうかを足す
3. `app/stages/page.tsx` で値を詰める
   - `difficulty` は `loadProgress` の `select` に足す必要があるか確認する
     （いまは `id, order, title` しか取っていない）
4. 吹き出しに星を出す
5. 丸の上に印を出す（**`NODE_H` と `--row-h` を確認してから**）
6. **狭い画面（375px）で、ラベルが重ならないことを確認する**
7. `npm test`（U-820 / U-823 が通ること）と `npm run build`

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `components/stage/StageMap.tsx` | `Stage` 型・吹き出し・丸の上の帯・高さの調整 |
| `app/stages/page.tsx` | 値を詰める |
| `lib/progress/unlock.ts` | `select` に `difficulty` を足す（必要なら） |
| `tests/unit/stage-map.test.ts` | 高さの対応を直したら更新 |

---

## 終わったと言える条件

- [ ] 4点についてオーナーの判断を得ている
- [ ] 吹き出しに難易度が星で出る
- [ ] チュートリアルの問題に印が出る
- [ ] **`status` に4つ目の値を足していない**
- [ ] **狭い画面（375px）でラベルが重なっていない**
- [ ] `npm test`（**U-820 / U-823**）と `npm run build` が通る

---

## 注意

- **`status` を増やさないこと。** 既存の判定が全部取りこぼします（型のコメントに経緯あり）
- **マップの高さの数字は対で直すこと。** `NODE_H` / `--row-h` / markup のクラスが対応していて、
  片方だけ動かすと**狭い画面でラベルが重なります**（実際に起きた経緯がコメントにあります）
- **丸を大きくしないこと。** 道（SVG）の端が丸の中心に合わせてあります
- 文言に禁止語を使わないこと（`CLAUDE.md`）。「初心者向け」は使えません

---

## 作業記録

（決めたこと・実装内容・狭い画面で見た結果を追記する）
