# E4. 問題データに欄を2つ足す（実行結果・前提知識）

**種別:** 判断 + 実装
**規模:** 小〜中
**前提として終わっていること:** なし
**このタスクが終わらないと着手できないもの:** **A1 の影響型** と **A2（問題の量産）**

> **このタスクは旧 E4（影響型の表示構造）と旧 G4（前提知識の学習）を統合したもの。**
> どちらも `problems` に欄を1つ足し、同じ画面を触り、同じ判断領域なので、
> **分けると同じ話を2回考え、データベースの変更も2回になる。**

**先に読むもの**
- `design/移植残タスク.md` の §6
- `design/problem-samples-desktop-light.html`（影響型の見た目）
- `ideas/問題作成ガイド.md`（問題の作り方。ここに項目が増えることになる）

> ### ⚠️ 同時進行の注意
> **同時に進めてはいけない相手は無い。** ただし `app/problems/[id]/page.tsx` は
> E5・E8・G2 も触る。**着手前に `main` を取り込むこと。**
> **A1 と A2 がこのタスクの完了を待っている。** 本線を止めないよう優先度は高い。

---

## 何をするか

問題のデータに、**空でもよい欄を2つ**足す。どちらも「ある問題にだけ入れる」もの。

### ① 実行結果（コードとは別に見せる）

「このエラーはどこが原因か」を読ませる問題（影響型）では、
**コードと、実際に出たエラーの文面の2つ**を並べて見せる必要がある。いまその入れ物がない。

### ② 前提知識（その問題を解くのに必要な予備知識）

「const は一度入れたら入れ替えられない」といった説明を、その場で開けるようにする。

---

## なぜ必要か

**①は、100問のうち「実務で一番よくある読み方」を作れるようにするため。**
エラーが出たときに原因の場所を特定する、という日常の作業そのもの。
入れ物がないと、エラー文をコードの中にコメントとして押し込むしかなく、本物の画面と見え方が変わる。

**②は、分からない言葉1つで手が止まるのを防ぐため。**
そこで検索して別のサイトに行くと戻ってこない。

### どちらも「94問を作り始める前」に決める必要がある

後から足すと、**94問ぶんの中身を書くために、もう一度100問を見直すことになる。**
いま決めれば、問題を作るときに一緒に書ける。**いま決めるほうが確実に安い。**

---

## ⚠️ オーナーに確認すること

**コードを書き始める前に、必ず下の質問をして決めてもらうこと。**

### ① 実行結果の持ち方

> 問題画面に「コード」と「実行結果」を分けて表示したいのですが、
> データの持ち方に2つのやり方があります。
>
> **案A: 1つの文章の中に区切り記号を書く**
> いまの「コード」欄に決めた区切り（例: `---実行結果---`）を書いて、表示するときに分割します。
> - よい点: データベースを変更しなくてよい
> - 悪い点: **書き忘れると、エラー文がコードの一部として表示される。** 見つける仕組みが別に要る
>
> **案B: 「実行結果」専用の欄を足す**
> - よい点: 混ざりようがない。空なら表示しないだけ
> - 悪い点: データベースの変更が必要（②と合わせて1回で済む）
>
> **おすすめは案B**です。いま問題を1問ずつ手で作っている段階なので、
> 「書き忘れると壊れる」仕組みを増やさないほうが安全なためです。

### ② 前提知識を入れるかどうか

> 各問題に「解くのに必要な予備知識」を付けますか。
>
> - **入れる:** 問題を作るとき、説明も一緒に書きます。1問あたりの作業が少し増えます
> - **入れない:** いまの形式のまま。**後から入れるなら94問を作り直すことになります**

### ③ 入れる場合、どのくらいの分量にしますか

> - **案A: 数行の短い説明**（おすすめ）「const は入れ替えられない」程度。読むのに5秒
> - 案B: 教科書のような解説
>
> **おすすめは案A**です。Ferret の価値は「他人のコードを読む訓練」であって
> 知識を教えることではありません。**足りない知識を埋める最小限**に留めるのが位置づけと合います。

### ④ 前提知識をいつ見せますか

> - 案A: 最初から画面に出しておく
> - **案B: 「わからない言葉があるとき」ボタンで開く**（おすすめ）
> - 案C: 一度不合格になってから出す
>
> **案B を勧めます。** 案Aは読まなくても解ける人にとって画面が煩雑になり、
> 案Cは「間違えないと助けてもらえない」形になって方針と合いません。

### ⑤ 既存の5問にも後から追加しますか

> 5問なら手間は小さいです。

決まったことは、このファイルの末尾に理由つきで記録すること。

---

## 手順

1. 上の5点を決める
2. **マイグレーションを1本にまとめて追加する**（欄を2つ同時に足す。適用は1回で済む）
   - `supabase/migrations/` に新しい `.sql` を作る（命名は既存にならう）
   - `problems` に `context text`（実行結果）と `prerequisite text`（前提知識）
   - **どちらも NOT NULL にしないこと**（既存5問が入っているため）
   - 用途がわかるコメントを SQL に残す
3. `supabase db push` で適用
4. 問題画面（`app/problems/[id]/page.tsx`）の `select` に2つの欄を足す
5. 表示を作る
   - `context` が入っているときだけ、コード枠の下に2つ目の枠
   - `prerequisite` が入っているときだけ、決めた見せ方で
6. `ideas/db仕様.md` の `problems` の表に2欄を追記
7. `ideas/問題作成ガイド.md` に書き方を追記
   - **A2 で問題を作る人が迷わないよう、例を1つずつ入れること**
8. 既存の5問に追加する（⑤で決めたなら）
9. `npm test` と `npm run build` を通す

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `supabase/migrations/`（新規1本） | 欄を2つ追加 |
| `app/problems/[id]/page.tsx` | 取得する欄を増やす・表示を分岐 |
| `ideas/db仕様.md` | 表に追記 |
| `ideas/問題作成ガイド.md` | 書き方と例を追記 |

---

## 終わったと言える条件

- [ ] 5点についてオーナーの判断を得ている
- [ ] 実行結果を入れた問題を1つ作ると、画面に2つの枠で表示される
- [ ] 前提知識を入れた問題で、決めた見せ方で表示される
- [ ] **どちらも空の既存5問が、今までどおり表示される**（ここが壊れやすい）
- [ ] `ideas/問題作成ガイド.md` に両方の書き方と例がある
- [ ] `npm test` と `npm run build` が通る

---

## 注意

- **模範解答や採点基準を画面に渡さないこと。**
  `app/problems/[id]/page.tsx` は取得する欄を名指しで指定している。
  これは模範解答が画面に出るのを防ぐためなので、`select("*")` のような書き方に変えてはいけない
- **前提知識に答えを書かないこと。**
  「const は入れ替えられない」は前提知識だが、
  「だからこのコードはエラーになる」は答えになってしまう
- **前提知識を採点に使わないこと。** AIには渡さない。表示専用
- マイグレーションは**1本にまとめる。** 分けると適用が2回になり、
  同時期に走る他のタスクのマイグレーションと順序が絡む

---

## 作業記録

**2026-08-17 実施（ブランチ `task/E4`）。** 5つの判断はすべて「おすすめ」で確定した。

### 決めたこと

| | 決定 | 理由 |
|---|---|---|
| ① 実行結果の持ち方 | **案B: `problems.context`（TEXT・NULL 可）を新設** | 区切り記法は書き忘れた瞬間にエラー文がコードの一部として表示される。しかも「壊れた」と気づく仕組みを別に作らないといけない。欄が別なら混ざりようがなく、空なら出さないだけで既存問題に影響しない |
| ② 前提知識を入れるか | **入れる**（`problems.prerequisite`） | 後から足すと94問ぶんの中身を書くために100問を見直すことになる。いま決めれば問題を作るときに一緒に書ける |
| ③ 分量 | **案A: 数行の短い説明**（**400字以内を DB の CHECK で強制**） | Ferret の価値は読解訓練であって知識の提供ではない。ガイドの注意書きだけでは100問作るあいだに必ず伸びるので、機械側で止める |
| ④ 見せ方 | **案B: 「わからない言葉があるとき」を押すと開く** | 常時表示は読まなくても解ける人の画面を煩雑にする。不合格後に出すのは「間違えないと助けが出ない」形で装備獲得型の方針と合わない。`<details>` なので **JS なしで開閉でき、問題画面はサーバーコンポーネントのまま** |
| ⑤ 既存5問への追加 | **prerequisite は5問すべてに投入。context は入れない** | どれも影響型ではなく、実行ログを読ませる設問になっていないため。`context` の初出は A-1 の影響型 |

### 作ったもの

| ファイル | 内容 |
|---|---|
| `supabase/migrations/20260817153000_problem_context_prerequisite.sql` | 2欄を1本で追加（適用済み） |
| `app/problems/[id]/page.tsx` | `select` に2欄追加。`CodePanel` を切り出してコード枠と実行結果枠で共用。前提知識は `<details>` |
| `problems/prerequisite-001-005.sql` | 既存5問の前提知識。**`problems/` は git 管理外なので、この worktree ではなく本体側（`~/Desktop/Ferret/problems/`）に置いた** |
| `ideas/db仕様.md` / `ideas/問題作成ガイド.md` | 表への追記と、両欄の書き方＋記入例 |

### 実装中に見つけた穴 ── 前提知識は採点キーワードを漏らす

**前提知識に採点キーワードを書くと、写すだけで層1が取れる。** 設問へのキーワード混入を禁じている
I-802 とまったく同じ理屈で、前提知識も全員が読める場所なので同じ制限がかかる。気づかずに94問書くと、
**層1が「読めたか」ではなく「写したか」の検査に変わる。**

禁じるのは**コード本文に出てこない語だけ**にした。`const` や `push` はコードに書かれているので
前提知識で隠しても画面から読める。危ないのは `undefined` / `キー` / `参照` のように
**コードに現れない結論の語彙**で、これを渡すと上の崩れ方をする。

`context` はこの検査の対象外。影響型は「ログのどの行を根拠にしたか」を答えさせる問題なので、
ログに答えの語が出るのは設計どおり。

この制約に合わせて既存5問の前提知識を書いた（例: ステージ4 は「参照」「複製」「コピー」「プリミティブ」が
すべてキーワードなので、残る前提知識は `===` の意味だけになった）。

### 追加した検査

`npm run test:db`（実DB・読み取りのみ）に4件。**わざと違反させて4件とも落ちることを確認済み。**

| | 何を見るか | なぜ |
|---|---|---|
| I-814 | 2欄に空文字列が入っていない | 画面は「空なら枠を出さない」で分岐する。`""` だと入れたつもりなのに出ず、**エラーも出ないので気づけない** |
| I-815 | 前提知識が400字以内 | DB制約の再確認 |
| I-816 | 前提知識が模範解答の文を写していない | 答えを書くと問題が成立しない。14字の一致で見る目安の検査 |
| I-817 | 前提知識にコードに無いキーワードが入っていない | 上記の穴 |

`npm test` 側は I-376（`select` の列を固定）を更新し、**I-376c（2欄が空でも描画できる）** を追加した。
欄を足した瞬間に全問 404 になる壊れ方を止める。

### 確認したこと

- `npm test` **537 passed**（+1）/ `npm run test:db` **63 passed** / `npm run lint` / `npm run build` すべて通る
- **実ブラウザで確認**（dev サーバー + Playwright を直接叩いた）
  - 実行結果を入れた問題 → `pre` が2枠。見出しは「JS / 読んでみよう」と「実行結果 / 実行するとこう出た」
  - 前提知識 → 既定で本文が非表示、押すと表示
  - **実行結果を空に戻すと枠は1つに戻り、前提知識だけが出る**（既存問題が壊れていない）

### 詰まった点・申し送り

1. **E2E（`npm run test:e2e`）は E4 以前から1件も通っていなかった。** 原因は2つとも直した。
   - `tests/e2e/support/fixtures.ts` の `login()` が `getByPlaceholder("メールアドレス")` を探していたが、
     デザイン移植で placeholder が `you@example.com` に変わっていた。**見出し（label）で引くように変更**
   - `playwright.config.ts` の baseURL が `127.0.0.1` で、Next 16 の dev サーバーが
     そのオリジンへ **自分の JS を配らない**（`allowedDevOrigins` の既定に入っていない）。`localhost` に変更
   - **どちらも E4 の範囲外**だが、直さないと追加した E-456 を含め E2E が動かないので手を入れた
2. **追加した E2E（E-456）はまだ実行できていない。** 別の worktree（B2）が同じ Supabase に対して
   E2E を走らせている最中で、**両者が同じテスト用データ（`order >= 9000` と共有のテストユーザー）を
   消し合う**ため。B2 の完了後に単独で流すこと
3. `problems.context` が入った問題はまだ1件も無い（初出は A-1 の影響型）。表示の確認は
   一時的に投入して行い、**確認後に NULL へ戻してある**
4. 見た目はモック（`design/problem-samples-desktop-light.html`）の2パネル構成に合わせたが、
   **右上の `ERROR` バッジは付けなかった。** `context` は型エラーやテスト出力も入る欄で、
   常に赤い ERROR と出すと嘘になる。内容から推測して出し分ける案もあるが、誤判定すると
   同じ嘘になるので、見出しを「実行結果」にして事実だけを書いた
5. worktree に `CLAUDE.md` が無いため `next dev` が代わりに `AGENTS.md` を生成した。
   コミットしていない（本体側は `CLAUDE.md` が git 管理外で同じ内容を持っている）
