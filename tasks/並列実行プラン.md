# 並列実行プラン（worktree 運用・3〜4本編成）

**作成: 2026-08-17。** `tasks/` の各タスクを、**同時に走らせても衝突しない組み合わせ**にまとめたもの。
衝突の根拠は `tasks/README.md` の「同時に複数のタスクを進めるときの決まり」。

---

## なぜ7本ではなく3〜4本なのか

**28タスクのうち22本が、オーナーの判断待ちになる**（判断不要は2本、軽微が4本）。

7本を同時に走らせると、**設計の質問が7個同時にあなたへ飛んでくる。**
エージェントはそこで止まるので、実際の進みは「あなたが何問さばけるか」で決まる。
ファイルの衝突より、こちらが先に詰まる。

もう1つ、**残りのUI系タスクが4ファイルに集中している。**

| ファイル | 奪い合うタスク |
|---|---|
| `app/result/[id]/ResultView.tsx` | E1 E2 E3 E6 E8 |
| `app/problems/[id]/page.tsx` | E4 E5 E8 G2 |
| `app/stages/page.tsx` | D1 D2 E3 E7 E8 |
| `app/api/score/route.ts` | B2 D1 E2 E3 G3 |

**各波でこの4ファイルから1本ずつしか取れない。** 増やしても待ちが増えるだけ。

---

## 運用の前提

- **`origin/main` から worktree を切って、1タスク = 1ブランチ = 1 worktree**
- **波（なみ）単位で進める。** 1つの波が全部 `main` にマージされてから、次の波の worktree を切る

```bash
git -C <repo> fetch origin
git -C <repo> worktree add ../ferret-E4 -b task/E4 origin/main
```

> **統合先は `main` 一本にする（2026-08-17 決定）。**
> 初版はここを `develop` と書いていたが、**`origin/develop` は存在しない。**
> ローカルの `develop` は main より遅れており、E4 を含んでいなかった
> （A1 で `origin/develop` から切ろうとして判明。`tasked/A1-読解型の検証.md`）。
> 実際に E4・F1・B2 が積まれているのは `main` なので、**実態に合わせて main を正とする。**
> 2本を同期し続ける手間を持たない、という判断でもある。

---

## 3つのレーン

タスクは性質の違う3本の流れに分かれる。**各波は、各レーンから1本ずつ取る形になる。**

| レーン | 中身 | 性質 |
|---|---|---|
| **1. 本線** | `E4 → A1 → B1 → B3 → A2` | **止めない。** ここが完成までの最長経路 |
| **2. UI** | `F1 → E7 → E5 → E2 → E6 → E1` | 4ファイルの奪い合いなので**1本ずつ** |
| **3. 公開準備** | `C1 → C4 → C2 → C3 → C5` | **「公開する」と決めてから**始めればよい |

**レーン1 の A2（量産・特大）が最長。** ここに一番厚く人を割くのが、完了を早める唯一の方法。

---

# 第1波（4本）

**最優先は E4。** A1 の影響型と A2 の両方がこれを待っている。

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **E4** 問題データに欄を足す | `app/problems/[id]/page.tsx` + マイグレーション |
| 本線 | **A1** 読解型の検証 | `problems/` のみ（アプリ非依存） |
| UI | ~~**F1** middleware→proxy~~ | **✅ 2026-08-17 完了**（`proxy.ts` になった） |
| 採点 | **B2** 誤読の記録 | `lib/ai/prompt.ts` `schema.ts` `score/route.ts` |

### この4本にした理由

- **E4 が本線の2か所を同時に解放する**（A1 の影響型 / A2 の前提知識の方針）
- ~~**F1 が C2・C3・E1 の3本を将来にわたって解放する。** 小さいので先に済ませる~~ → **完了。この解放は済んでいる**
- **A1 はアプリのコードを一切触らない**ので、他と絶対にぶつからない
- 判断が重いのは E4 だけ。残り3本は軽微または不要

### 注意

- **A1 は「意図・命名・仕様」の3種から始める。** 影響型は E4 の判断が出てから
  （同じ波なので、E4 が先に終われば続けて進められる）

---

# 第2波（4本）

> **着手前に F3 をマージすること。**

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **B1** 採点のブレを測る | `tests/golden/` `package.json` |
| UI | **E7** マップの仕上げ | `app/stages/page.tsx` `StageMap.tsx` |
| 公開 | **C1** メール送信（Resend） | 環境変数・外部サービスの管理画面 |
| 整理 | **F2** 導入見送りの判断 | ドキュメントのみ |

### 前提の確認

**B1 は B2（第1波）と A1（第1波）の完了が必須。**
測っている最中に採点基準や問題が変わると、出た数字の意味が失われる。

---

# 第3波（4本）

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **B3** 判定基準の調整 | `lib/ai/prompt.ts` `compose.ts` |
| UI | **E5** コードの色付け | `app/problems/[id]/page.tsx` `globals.css` `package.json` |
| 公開 | **C2** 法務文書 | `app/terms/` `app/privacy/`（新規） `ProblemForm.tsx` `proxy.ts` |
| 公開 | **C4** Supabase の認証設定 | 管理画面のみ |

### 注意

- **B3 は B1（第2波）の測定結果が必須。** 数字が無いと直せない
- **C2 は判断が3件と多い。** 法務文書の中身はAIが確定させず、下書きに留めること

---

# 第4波（4本）— ここから A2 が走り始める

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **A2** 問題の量産【**以降ずっと走る**】 | `problems/` のみ |
| UI | **E2** フィードバックの2枠表示 | `ResultView.tsx` `scorer.ts` `score/route.ts` + マイグレーション |
| 公開 | **C3** せってい画面 | `app/settings/` `AppSidebar.tsx` `proxy.ts` + マイグレーション |
| 整理 | **Z1** ドキュメントを実装に合わせる | ドキュメントのみ |

### 注意

- **A2 は1章（8問前後）ずつ順番に。** 複数セッションで章を分担すると**表示順の番号が重複する**
- **A2 は特大。** 第5波以降も走り続ける前提で、他のレーンを進める
- **Z1 は B3（第3波）の完了後。** 実装が動いている最中に書くと、書いた先から古くなる

---

# 第5波（3本）

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **A2 継続** | `problems/` |
| UI | **E6** 0点の見せ方 | `ResultView.tsx` `result/page.tsx` |
| 追加 | **G2** メモ書き機能 | `MemoPad.tsx`（新規） `problems/[id]/page.tsx` |

**E6 は E2（第4波）の完了が前提。**

---

# 第6波（3本）

| レーン | タスク | 占有するファイル |
|---|---|---|
| 本線 | **A2 継続** | `problems/` |
| UI | **E1** 振り返り画面 | `app/review/`（新規） `ResultView.tsx` `AppSidebar.tsx` |
| 追加 | **E9** ダークテーマ | `globals.css` `layout.tsx` `settings/page.tsx` |

**E9 の設定画面への追加は C3（第4波）が前提。**

---

# 第7波（1本・**必ず単独**）

| タスク | 備考 |
|---|---|
| **C5** 本番デプロイ | 「いまの `main` をそのまま外に出す」作業。途中の変更が混ざると、何を公開したのか分からなくなる |

**C1〜C4 がすべて終わっていること。**
A2（量産）は別レーンなので、続けて走らせて構わない。

---

# 第8波以降（課金を始めると決めてから）

| | タスク | 備考 |
|---|---|---|
| 第8 | **D1** プラン別の採点上限 | `score/route.ts` `stages/page.tsx` `compose.ts` |
| 第9 | **D2** Stripe決済 | **C5 と D1 が前提。** 特大なので複数セッションに分ける |

---

## いま着手しないほうがいいもの

**下の4本は、意図的に上の波から外してある。** 理由つきで見送っている。

| タスク | 外した理由 |
|---|---|
| **E3** XPとレベル | 設計判断が4件あり、**公開後に数字を変えると利用者が混乱する。** 問題が揃ってからのほうが安全 |
| **E8** スマホ対応 | 単独で6ファイルを触り、**UI系を全部止める。** 公開に必須ではない |
| **G1** 共有機能 | 仕様未定。オーナーが「一通り実装が終わってから」と決めている |
| **G3** AI相談ボット | 仕様未定。**D1 が前提**で、判断も4件ある |

やると決めたら、**E8 は必ず最後**（他のUI系が全部終わってから）。

---

## さらに並列度を上げる手（**要判断・タスク未作成**）

`ResultView.tsx` を機能ごとに分割すると、**E1・E2・E3・E6 が同時に走れる。**
第4〜6波が1つの波に畳めるので、**トータルで2波ぶん短縮**できる。

- **やる場合:** 第1波に小さなタスクとして足す。分割だけで中身は変えない
- **やらない場合:** 上の構成のまま

タスクファイルはまだ作っていない。

---

## 波を終えるときの手順

1. 各 worktree で `npm test` と `npm run build` を通す
2. そのタスクのファイル末尾の `作業記録` に、やったこと・決めたこと・詰まった点を書く
3. `残タスク.md` の該当項目にチェックを入れる
4. `main` にマージする
5. **その波の全員がマージされてから**、次の波の worktree を切る

> **波をまたいで worktree を残さないこと。** 古い `main` から切ったまま作業を続けると、
> このプランが前提にしている「占有するファイル」の関係が崩れる。
