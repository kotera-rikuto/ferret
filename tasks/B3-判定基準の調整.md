# B3. 測った結果をもとに採点基準を直す

> ### 状態: **候補1は完了（2026-08-18・`p3` → `p4`）。この票はまだ閉じない**
>
> **やったこと:** AIへの指示に判断例を1つ足した。`npm run golden` を前後で回して確認済み。
> **効果:** 合否が入れ替わる率 **10% → 0%**。G05（「エラーが出ると思います」）が
> 5回すべて不合格に戻った。反転誤答 0% / 短い正答 100% はどちらも維持。
> **残り:** G15（候補2）・`articulation`・パーフェクト帯。詳細は末尾の作業記録。
> **次に着手する人は「作業記録」から読むこと。** 上の本文は着手前の状態のまま残してある。

**種別:** 実装（判断を多く含む）
**規模:** 中
**前提として終わっていること:** **B1 必須。** 測定結果がないとこのタスクは始められない

> ### ⚠️ 同時進行の注意
> **B2 と同時に進めないこと。** どちらも `lib/ai/prompt.ts` と、
> その内容を固定した検査（スナップショット）を書き換える。**B2 が先。**
> `lib/ai/compose.ts` は D1・E7 も触る。着手前に `main` を取り込むこと。

**先に読むもの**
- `ideas/採点システム_残課題.md` の §8・§11・§12
- `tests/unit/compose.test.ts` の冒頭コメント（**点数の決まり方の根拠がここに書いてある**）
- B1 の作業記録

---

## 何をするか

B1 で測った数字をもとに、**採点の物差しを調整する。**

分かっている問題は3つ。**どれも B1 の数字を見てから着手すること。**

| 疑い | 症状 |
|---|---|
| 同じ回答で点数が振れる | 「エラーが出ると思います」が 29点↔53点 |
| 文章の明確さの判定が厳しい | 明確な一文が「書けていない」と判定された |
| 結論が逆の回答が素通りする | 数字だけを引用したときに検出をすり抜ける |

---

## なぜ必要か

**合格ライン（55点）の近くで点数が振れると、同じ回答が合格したり不合格になったりする。**
学習アプリとして最も信用を失う壊れ方にあたる。

一方で、**むやみに厳しくすると「短いが正しい回答」が落ちる。**
これは設計の根幹（中核さえ読めていれば短くても通す）を壊すので、絶対に避ける。

**両側があるので、片方だけ見て直すと必ずもう片方が壊れる。** だから測ってから直す。

---

## ⚠️ オーナーに確認すること

**このタスクは判断の塊。着手前に必ず方針を確認すること。**

> 採点の調整をします。相反する2つの目標があるので、優先順位を決めてください。
>
> **目標A: 間違った回答を合格させない**
> 厳しくすると達成しやすい。ただし**正しいのに短い回答も落ちやすくなる。**
>
> **目標B: 正しい回答は短くても合格させる**
> 緩くすると達成しやすい。ただし**あいまいな回答も通りやすくなる。**
>
> 現在の設計は **B寄り**（中核が読めていれば短くても通す）です。
> B1 の測定では（結果）でした。
>
> どちらに寄せますか。それとも現状維持で、別の方法（AIへの指示の書き方など）で
> 改善を試みますか。

さらに、**次の値は「触ってはいけないもの」として設計されている。**
変更を提案する場合は、理由を添えて必ず確認を取ること。

| 値 | なぜ触れないか |
|---|---|
| 100点満点（20点 + 80点） | 全問統一。オーナーが差別化要素と位置づけている |
| 合格ライン 55点 | **53点が理論上の下限。** これ以上下げると「中核を読めていない回答が合格しない」保証が崩れる |
| 観点の重み（48 / 16 / 12 / 4） | 中核を重くすることで「短くても通る」を実現している |
| キーワードは4組固定 | データベース側でも強制済み |

---

## 手順

1. B1 の作業記録を読み、どの数字が目標を下回ったかを整理する
2. 上の「オーナーに確認すること」で方針を決める
3. **AIへの指示（`lib/ai/prompt.ts`）から先に試す**
   - 点数の計算式より、指示の書き方を変えるほうが影響が局所的で戻しやすい
   - 例: 判断例に「『エラーが出ます』は中核が半分。エラーになること自体は合っているが、
     何が原因かに触れていない」を追加する
4. 変更したら **B1 を丸ごと回し直す**
5. 数字が改善したか、他が悪化していないかを比べる
6. 指示の変更で足りない場合のみ、点数の計算式（`lib/ai/compose.ts`）に手を入れる
   - **手を入れる前に必ずもう一度確認を取ること**
7. `npm test` を通す

---

## 触る場所

| ファイル | 何をするか | 危険度 |
|---|---|---|
| `lib/ai/prompt.ts` | 判断例の追加 | 低（まずここから） |
| `tests/unit/__snapshots__/prompt.test.ts.snap` | 固定内容の更新 | 低 |
| `lib/ai/compose.ts` | 点数の計算式 | **高（要確認）** |
| `tests/unit/compose.test.ts` | 計算式を変えたら必ず更新 | — |

---

## 終わったと言える条件

- [x] 方針についてオーナーの判断を得ている（2026-08-18。下記「決めたこと」①②③）
- [x] 変更後に B1 を回し直している（変更前も回した。前後2回・計 約¥7.2）
- [x] **「結論が逆の回答」の合格率が 0%**（0/10・維持）
- [x] **「短いが正しい回答」の合格率が 100%**（10/10・維持）
- [x] 合格ライン付近の点数の振れ幅が、変更前より小さくなっている
      → **同じ回答どうしで比べると 平均 11.1 → 7.4 点に縮んだ**（前後の対象10件に共通して入る7件で比較）。
      **合否が入れ替わる率も 10% → 0%。**
      ただし**レポートに出る「平均の幅」は 10.0 → 11.4 と増えて見える。** これは
      繰り返しの対象が「1周目の点が閾値55に近い順」で毎回選び直されるため、**顔ぶれが変わったから**。
      G05 が閾値から離れた結果、代わりに高得点帯の短い正答（G33・G37）が対象に入り、
      その自然な振れが混ざっている。**前後で比べるなら共通の件で見ること。**

      | 件 | 変更前の5回 | 幅 | 変更後の5回 | 幅 |
      |---|---|---|---|---|
      | G05 | 34/60/34/34/60 | 26 | **34 ×5** | **0** |
      | G35 | 34/34/42/34/10 | 32 | **34 ×5** | **0** |
      | G44 | 42/34/42/42/34 | 8 | **34 ×5** | **0** |
      | G15 | 63 ×5 | 0 | 63 ×5 | 0 |
      | G29 | 34 ×5 | 0 | 34 ×5 | 0 |
      | G42 | 66/74/74/74/78 | 12 | 66/86/66/66/66 | 20 |
      | G45 | 42 ×5 | 0 | 42/42/10/42/18 | **32** |

      **最大の幅は 32点のまま縮んでいない**（担い手が G35 から G45 に替わっただけ）。
      G45 は不合格想定の回答で5回とも不合格なので合否には効いていないが、
      **低得点帯の振れは残っている。** 次に測るときの観察点
- [x] `npm test` が通る（**570 passed / 63 skipped**。`npm run build`・`npm run lint` も通過）
- [x] 変更した理由が `ideas/採点システム_残課題.md` に記録されている（§8「実測 2回目」）

---

## 注意

- **「何を書いても最低10点」のような下駄は入れないこと。** 点数の意味がなくなる。
  これは過去に明示的に見送った判断（`ideas/採点システム_残課題.md` §2）
- 点数の計算式には**互いに噛み合った仕組み**がある。
  `tests/unit/compose.test.ts` の §1-7 に「片方だけ動かすと静かに壊れる」関係が
  テストとして固定してある。**このテストが落ちたら、設計を壊している合図**
- 採点の物差しを変えたら、**保存済みの過去の点数と混ざる。**
  `grader_version` という記録があるので、変更時はここを上げること

---

## 作業記録

**2026-08-18 実施（worktree `../ferret-B3` / ブランチ `task/B3`・手元の `main` から作成）。**
**候補1のみを入れた。候補2 には手を付けていない**（B1 の決定⑦ どおり）。

### 変更したファイル（4つ）

| ファイル | 変更 |
|---|---|
| `lib/ai/prompt.ts` | `# 校正の基準例` に判断例を2行追加（下記） |
| `lib/ai/schema.ts` | `PROMPT_VERSION` を `p3` → **`p4`**。理由をコメントに記載 |
| `tests/unit/prompt.test.ts` | U-162 を追加（判断例が消えたら落ちる） |
| `tests/unit/__snapshots__/prompt.test.ts.snap` | 更新 |

**`lib/ai/compose.ts` は1行も触っていない。** 配点・重み・クリア閾値・キーワードの扱いはすべて変更なし。

追加した判断例:

```
模範回答が「const で宣言された rate に再代入しているため TypeError で止まる」の場合。
- 「rate は const なので再代入でエラーになります」→ core は full。短くても、
  何がどうなって起きるのかを指している
- 「エラーが出ると思います」→ core は partial。エラーになること自体は合っているが、
  何が原因かに触れていない。結果だけを述べて原因に触れていない回答は full にしません
```

### 変更前後の数字（同日・同条件で2回測定）

`npm run golden` を **変更前（p3）→ 変更後（p4）** の順に回した。各90回・約¥3.6。

| 指標 | 目標 | 変更前 `p3` | 変更後 `p4` | |
|---|---|---|---|---|
| **合否が入れ替わる率**（同一回答5回・閾値付近10件） | 低いほどよい | 10%（1/10） | **0%（0/10）** | ✅ 改善 |
| 反転誤答の**誤合格率** | 0% | 0%（0/10） | **0%（0/10）** | ✅ 維持 |
| 短いが正しい回答の**合格率** | 100% | 100%（10/10） | **100%（10/10）** | ✅ 維持 |
| 想定との一致率（50件） | — | 98%（49/50） | 98%（49/50） | 変わらず |
| パーフェクト帯（80点以上） | 目安 15〜25% | 46% | 42% | ⚠️ 残る |
| 採点不成立 | 0件 | 0件 | 0件 | |

**狙った1件（G05）は完全に直った。**

| 件 | 回答の性質 | 変更前の5回 | 変更後の5回 |
|---|---|---|---|
| G05 | 結果だけを述べた回答（order 1） | 34 / **60** / 34 / 34 / **60**（2回クリア・`core` が partial↔full で往復） | **34 / 34 / 34 / 34 / 34**（0回クリア・`core` は partial で固定） |
| G15 | 設問が問うている軸に触れていない回答（order 2） | 63 ×5（`core=full` 固定） | **63 ×5（変わらず）** ← 候補2の対象 |

### 1件ずつ突き合わせた結果（1周目・50件）

- **合否が変わった件は0件。** 点数が動いたのは13件で、平均 **-0.3点**・中央値 0
- 区分ごとの平均差: クリア想定 +0.4 / 不合格想定 +0.5 / 反転誤答 +2.0 / **短い正答 -5.0**
- **短い正答の -5.0 は変更の影響ではなく測定のばらつき。** 下がった G37（83→63）・G33（90→74）は
  変更後の繰り返し測定でそれぞれ 63〜83・74〜92 と単独で18〜20点振れており、**差分がその幅に収まる。**
  合格ラインとの距離も最低 63 対 55 で8点あり、100% を維持している
- `core` の判定が変わったのは2件（G39・G43。どちらも `none` → `partial`）で、**どちらも不合格のまま**

### 決めたこと

| | 決定 | 理由 |
|---|---|---|
| ① 方針 | **いまの方針（短くても正しければ通す）を維持し、AIへの指示だけ直す**（オーナー判断） | 目標2本（反転誤答0% / 短い正答100%）は測定で達成済みで、穴は「中身の薄い回答が通る」1点だけだった。全体を厳しくすると設計の根幹を壊す |
| ② 土台 | **手元の `main` から worktree を切る**（オーナー判断） | 指示は `origin/main` だったが、**サーバー側には `tests/golden/cases.json` がまだ無い**（未送信のコミット2つに入っている）。それでは変更前後の比較ができない |
| ③ 測定 | **変更前と変更後の2回回す**（オーナー判断・計 約¥7.2） | AI の応答は毎回わずかに振れる。同日・同条件の前後比較でないと、差が変更のせいか揺れかを切り分けられない |
| ④ 判断例に **full の例も対で置いた** | 候補1が挙げていたのは partial の1行だけだったが、それだけだと「短い回答は下げる」と読まれかねない。**設計の根幹（短くても正しければ通す）を守る側の例を同じ場所に置く**ほうが安全と判断した。結果として短い正答は 100% を維持している |
| ⑤ 判断例を**独立したブロックにした** | 既存の `# 校正の基準例` は「配列から偶数だけを取り出す」模範回答に対する例の並び。そこへ「エラーが出ます」を混ぜると、どの模範回答に対する判断なのか読めなくなる。模範回答を明示した別ブロックにした |
| ⑥ `PROMPT_VERSION` を **p4 に上げた** | 票の「注意」どおり。同じ `p3` の記録に別の指示文で採点した行が混ざると、あとから差の出所が追えなくなる。**副作用として、p3 で採点済みの回答を再送すると再採点になる**（1件 約¥0.04） |
| ⑦ **候補2 には進まない**（オーナー判断・測定後） | 改善が候補1の1変更にひもづいた状態で確定させる。候補2 は採点全体が厳しくなる向きの変更なので、入れるなら単独で測り直す |

### 残っていること（この票を閉じない理由）

1. **G15 が5回とも 63点でクリアする**（候補2の対象）。
   `core` の条件は「集計に入っていない A-3 がログに出るという結論」で、回答は合計値（2000）しか述べていない。
   **揺れではなく判定基準そのもの**なので、候補1では動かなかった。
   直すなら「`core` は【採点観点】の条件文と照合して判定する」と明示する（候補2）
2. **パーフェクト帯 42%**（目安 15〜25%）。`ground` が付きやすい。合否は正しいので優先度は低い
3. **`articulation` が厳しすぎる疑い** — 2回の測定でその傾向は出ていない。優先度を下げてよい

### 詰まった点・申し送り

1. **`origin/main` には `tests/golden/cases.json` が無い。** 手元の `main` が2コミット先行しており、
   ゴールデンセットの回答50件はその中にある。**送信（push）されるまで、`origin/main` から切った
   worktree では `npm run golden` が動かない**（`cases.json` が無いと明示的なエラーで止まる設計）
2. **`.env.local` は git 管理外なので、worktree を切ったら手でコピーが要る。**
   Supabase の URL・service_role キー・`OPENAI_API_KEY` が無いと測定は始まらない
3. **繰り返し測定の対象10件は毎回入れ替わる**（1周目の点が閾値55に近い順に選ばれる）。
   そのため「点数の振れ幅」の平均値は前後で直接比較できない。**比べるべきは合否が入れ替わる率のほう**
4. 測定の生データは `tests/golden/results/`（git 管理外）に2回ぶん残してある
   （`...-p3.json` が変更前、`...-p4.json` が変更後）
