# E10. マスコットに凝った動きを付ける（Lottie）

**種別:** 実装（ただし**先に素材が要る**）
**規模:** 中
**前提として終わっていること:** なし。ただし **動く絵のデータが1つも無いと着手できない**

> ### ⚠️ 同時進行の注意
> - `app/globals.css` を触るので、**E5（コードの色付け）・E9（ダークテーマ）とは順番にやる**
> - `components/ui/Mascot.tsx` は**全画面のマスコットの唯一の入口。** ここに関わるので、
>   画面を触るタスク（E1 E2 E6 E7 E8）と同時に走らせない

**先に読むもの**
- `design/移植残タスク.md`（いまの作り方が分かる）
- `components/ui/Mascot.tsx`（40行たらず。マスコット表示の唯一の入口）

> **この票ができた経緯:** F2（導入見送りの判断）で「Lottie は入れる」とオーナーが判断したため
> 起票した（2026-08-18）。判断の記録は `tasked/F2-導入見送りの判断.md`。

---

## 何をするか

マスコットの動きを、**いまの簡単な揺れより凝ったもの**（歩く・しっぽを振る・のけぞって喜ぶ など）にする。
その動きを再生するための仕組みが Lottie。

> ### ⚠️ この作業の本体は「絵」であって「プログラム」ではない
> **再生する仕組みを入れるだけなら半日で終わる。**
> 動く絵のデータが無いと、仕組みを入れても**再生するものが何も無い。**
> だから手順1は「素材を用意する」から始まる。

---

## なぜ必要か

- いまの動きは7種類だが、**どれも1枚の絵を揺らしているだけ**
  （ふわふわ浮く / 採点中に鼻を動かす / ぽんと出る / 現在地の光の輪 / 吹き出しの上下 / 点滅 / 紙吹雪）
- 表情の絵は3枚（通常・喜ぶ・考える）あるが、**切り替わるだけで、その間の動きが無い**
- この種のアプリでマスコットが効くのは「こちらの操作に反応が返ってきた」と感じられるとき。
  静止画の差し替えでは、そこまで届かない

---

## ⚠️ オーナーに確認すること

### ① 動く絵のデータを、誰がどうやって用意しますか

> **これが決まらないと着手できません。** 仕組みだけ入れても、再生するものがありません。
>
> - **案A: まず1つだけ試作する**（おすすめ）
>   1つあれば「思ったとおりに動くか」「重くならないか」を確かめられます。
>   **10個作ってから「重すぎる」と分かるのが最悪の順番**です
> - 案B: 外注する
>   まとまった費用と、キャラの資料を渡すやり取りが要ります
> - 案C: 自分で作る
>   専用のソフト（After Effects など）と描く時間が要ります
>
> **どの案でも、②で「どこを動かすか」を先に決めておくと、作る枚数が決まります。**

### ② どの場面を動かしますか

> マスコットは **9ファイル・12か所**に出ています。大きさは**とても小さいもの（横 26px 相当）から
> タイトル画面の大きなもの（横 280px 相当）まで**幅があります。
>
> - **案A: 大きく出る3か所だけ**（おすすめ）
>   タイトル画面 / 採点を待っている間 / リザルトの登場。**動かす価値があるのはこの3つ**です
> - 案B: 全部
>   サイドバーの隅などにある小さいマスコットは、**動かしても見えません。**
>   その割に読み込むデータだけ増えます
>
> **案Aなら、用意する素材は3つで足ります。**

### ③ タイトル画面を動かす場合、最初の表示をどうしますか

> タイトルは**アプリを開いて最初に目に入る絵**です。いまは画像なので**開いた瞬間に出ます。**
> 動く絵に替えると、**読み込みを待つぶんだけ出るのが遅れます。**
>
> - **案A: まず今の画像を出し、準備ができたら動く絵に差し替える**（おすすめ）
>   表示の速さを落とさずに動きを足せます。作りは少し増えます
> - 案B: タイトルは今の画像のままにして、他の2か所だけ動かす
>   いちばん安全。**最初の表示速度に一切触りません**
> - 案C: 読み込みを待たせる
>   作りはいちばん簡単ですが、**最初の画面が一瞬空白になります**

---

## 手順

1. **素材を1つ用意する**（②で決めた場面のうち1つ）。ここが済むまで先に進まない
2. ライブラリを入れる（`lottie-react` か `@lottiefiles/dotlottie-react`）。
   **入れる前と後で `npm run build` の出力サイズを控えて、増加量を作業記録に書く**
3. **`components/ui/MascotMotion.tsx` を新規で作る。`Mascot.tsx` は静止画のまま残す**
   - **理由:** `Mascot.tsx` は `app/page.tsx` `app/stages/page.tsx` `app/problems/[id]/page.tsx`
     などサーバーコンポーネントから使われている。Lottie はブラウザ側でしか動かないので `"use client"` が要る。
     1つのファイルにまとめると、**静止画で足りている9か所まで JS を背負うことになる**
   - 静的 import の PNG は `next/image` が寸法を把握している。差し替えるときは
     **同じ表示サイズになるか**を目で確認する（3枚ともキャラの占有率を揃えてある。`Mascot.tsx` のコメント）
4. ②で決めた場面だけ `MascotMotion` に差し替える
5. **`prefers-reduced-motion`（OS の「動きを減らす」設定）に対応する**
   - **現状 `app/globals.css` は未対応で、いまの CSS アニメも止まらない。**
     無限に繰り返す動きが増えるので、ここで一緒に入れる
6. 差し替えた場所の CSS アニメ（`animate-float` `animate-sniff` `animate-pop`）を外す。
   **外し忘れると、Lottie の動きと CSS の揺れが二重にかかる**
7. `npm test` と `npm run build` を通す

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `components/ui/MascotMotion.tsx` | **新規。** Lottie の再生（`"use client"`） |
| `components/ui/Mascot.tsx` | **触らない。** 静止画の入口として残す |
| `public/`（または `assert/`） | 動く絵のデータを置く |
| `app/page.tsx` | ②案A なら差し替え（タイトル・`animate-float`） |
| `app/problems/[id]/ProblemForm.tsx` | ②案A なら差し替え（採点待ち・`animate-sniff`） |
| `app/result/[id]/ResultView.tsx` | ②案A なら差し替え（登場・`animate-pop`） |
| `app/globals.css` | 使わなくなった `@keyframes` の整理 + `prefers-reduced-motion` |
| `package.json` | ライブラリの追加 |

---

## 終わったと言える条件

- [x] 3点についてオーナーの判断を得ている（2026-08-19。下の作業記録）
- [x] 素材が1つ以上あり、**実際に画面で動いている**（2つ用意した）
- [x] 動かさないと決めた場所は**静止画のまま**（`Mascot.tsx` は無変更・16か所そのまま）
- [x] OS の「動きを減らす」設定で動きが止まる（Playwright で再現して確認・U-811 で縛った）
- [x] **二重に動いている場所が無い**（`sniff` は定義ごと削除。U-814 で縛った）
- [x] `npm test` と `npm run build` が通る（708 passed / lint も通る）
- [x] **ビルドサイズの増加量**を作業記録に書いた（JS +11.5KB・内訳つき）

---

## 注意

- **素材の当てが無いうちにライブラリだけ入れないこと。** 使われないコードが残り、
  次に見た人が「何かに使われているはず」と考えて消せなくなる
- **採点待ちの動きは短くする。** 実測の待ち時間は **1.2〜4.3秒**。
  長い動きを作ると、1周する前に画面が切り替わって尻切れになる
- **喜ぶ動きはクリアしたときだけ。** 不合格の画面に出さない（`Mascot.tsx` のコメントと、
  `CLAUDE.md` のネガティブ表現を避ける方針）
- **紙吹雪（`fall`）・現在地の光の輪（`halo`）・点滅（`blink`）はマスコットではない。**
  これらは CSS のままでよく、Lottie にする対象ではない

---

## 作業記録

（決めたこと・理由を追記する）

**2026-08-19・着手前の調査とオーナー判断。コードは1行も触っていない。**

### 決めたこと（オーナー判断）

| 問い | 判断 | 意味するところ |
|---|---|---|
| ① 動く絵をどう作るか | **今の絵を部品に切って動かす** | 描き直さない。**今のフェレットの見た目がそのまま残る。** できる動きは「揺れる・しっぽを振る・首をかしげる・のけぞる」まで。**歩かせるのは無理。** 費用0円、要るのは切り抜きの画像編集 |
| ② どの場面を動かすか | **大きく出る3か所** → ③ と合わせて **実質2か所** | 小さいアイコン（横26〜36px）は動かしても見えないので触らない |
| ③ タイトルの最初の表示 | **タイトルは画像のまま** | 最初に目に入る画面の表示速度に一切触らない。`app/page.tsx` と `animate-float` は**そのまま残す** |

### ①②③ を合成した結論 ── 動かすのは2か所・素材は2つ

| 場面 | ファイル | 素材 | 外す CSS |
|---|---|---|---|
| 採点待ち | `app/problems/[id]/ProblemForm.tsx:184` | `thinking` を動かしたもの。**1周1〜1.5秒**（待ち時間の実測が 1.2〜4.3 秒なので、長いと1周する前に画面が切り替わる） | `animate-sniff` |
| リザルトの登場（**クリア時だけ**） | `app/result/[id]/ResultView.tsx:349` | `happy` を動かしたもの。ループしない一発 | `animate-pop`（クリア時の枝のみ） |

- **リザルトは `mood={cleared ? "happy" : "thinking"}` の分岐になっている。** 未達のときは
  静止画の `thinking` を残す。「喜ぶ動きはクリアしたときだけ」という制約が、そのまま素材の数を1つ減らしている
- **タイトル（`app/page.tsx`）は対象外。** よって③の「まず画像→あとで差し替え」の仕組みは作らない

### 調査で分かったこと（票の記述を更新）

- **票の「9ファイル・12か所」は古い。実測は 13ファイル・18か所**（E1 の振り返り画面などで増えた）。
  大きさは横 26px〜280px。動かす価値があるのは 100px を超える5か所だけで、そのうち2か所が上の表
- **⚠️ 今の絵は「塗った絵」で、Lottie の想定する「線と形の絵」ではない**
  （`public/character_*.png` は 2000〜2048px 角・1枚 1.3〜1.6MB のラスター画像）。
  **票の①は「誰が用意するか」だけを聞いていたが、その前に「今の絵を活かせるのか」という分岐がある。**
  オーナー判断は「活かす」なので、Lottie の**画像レイヤー**として今の PNG の切り抜きを持たせ、
  部品ごとの位置・角度だけを動かす作りになる（＝ベクターで描き直す前提の作例はそのまま使えない）
- **`prefers-reduced-motion` はコードのどこにも無い**（全ファイル検索で0件）。現行の動き7種のうち
  5種（`float` `halo` `bob` `sniff` `blink`）が無限に繰り返す。**手順5は素材が無くても着手できる**
- **動きに依存しているテストは無い**（`tests/` の検索で `animate-*` の参照0件）。差し替えでテストは壊れない

### 順番の申し送り

**着手時点で E8（スマホ対応）が別セッションで進行中**（`.claude/worktrees/E8-mobile`・locked）。
`app/problems/[id]/ProblemForm.tsx` に**未コミットの変更**が入っている ── E10 が差し替えたい2か所のうちの1つ。
票の「同時進行の注意」どおり、**コードを触る工程は E8 の完了を待つ。** 素材づくりは今から並行して進められる。

### 次にやること

**素材2つの用意。** ここが済むまで手順2（ライブラリの追加）に進まない
（票の注意「素材の当てが無いうちにライブラリだけ入れないこと」）。

---

**2026-08-19・実装。** `origin/main`（`5ec03f3`）から worktree（`task/E10`）を切って作業。

### いちばん大きな判断: Lottie を入れなかった

判断①で「今の絵を部品に切って動かす」と決まった時点で、**Lottie を入れる理由が消えた。**

Lottie は「専用ソフトで作り込んだ動きを再生する」ための仕組み。今回作るのは
「元の絵をしっぽと本体に切り分けて、位置と角度を動かす」だけで、**それは CSS でそのまま書ける。**

| | CSS（採用） | Lottie |
|---|---|---|
| ライブラリ | 増えない | 増える |
| 実行時の処理 | 無い（`@keyframes` だけ） | 再生する処理が動く |
| 「動きを減らす」設定 | CSS 側で一括して止まる | 別に対応が要る |
| 今回の範囲でできる動き | **同じ** | 同じ |

F2 で「Lottie は入れる」と決めているが、あれは**ちゃんとした Lottie 用の素材を用意する前提**だった。
①で前提が変わったので、票の注意「素材の当てが無いうちにライブラリだけ入れないこと」に従って入れていない。
**将来ベクターの動きを外注するなら、その時点で入れ直せばよい**（そのとき捨てるものが何も無い）。

### 触ったもの

| ファイル | 何をしたか |
|---|---|
| `design/mascot/cut-parts.py` | **新規。** 絵を部品に切り分ける。**切る位置がこの作業の設計判断そのもの**なので script として残した（絵を差し替えたときに同じ判断をやり直せる） |
| `public/mascot/{thinking-body,happy-body,tail}.png` | **新規。** 768px 角・3枚で 553KB（元の1枚が 1,335KB） |
| `components/ui/MascotMotion.tsx` | **新規。** 部品を重ねて動かす。`"use client"` なし・状態なし |
| `components/ui/Mascot.tsx` | **触っていない。** 静止画の入口として16か所を支えたまま |
| `app/globals.css` | `sniff` を削除。マスコットの動き5つを追加。**`prefers-reduced-motion` を新規に対応** |
| `app/problems/[id]/ProblemForm.tsx` | 採点待ちを差し替え（1行 + コメント） |
| `app/result/[id]/ResultView.tsx` | クリア時だけ差し替え。**未達は静止画のまま**に分岐 |
| `tests/unit/mascot-motion.test.ts` | **新規。** §17・U-810〜816（7件） |

### 作った動き

| 場面 | 動き | 長さ |
|---|---|---|
| 採点待ち | 息づかい（縮むと横に広がる。軸は足元なので立ち位置が動かない）＋ しっぽを振る ＋ **虫眼鏡のレンズを光が横切る** | しっぽ 1.3秒／息づかいと光 2.6秒。**待ち時間の実測 1.2〜4.3秒より短くしてある** |
| リザルト（クリア時のみ） | ためて跳んで着地する（跳ぶとき縦に伸び、着地でつぶれる）＋ しっぽを1回強く振って収まる | 0.95秒・繰り返さない |

### 動かせなかった部位と、その理由

**元の絵は1枚の塗り絵で、隠れている部分は描かれていない。** 動かすと「描かれていない部分」が露出する。

| 部位 | 判断 | 理由 |
|---|---|---|
| しっぽ | **動かせた** | 体との間に背景の隙間があり、重なるのは腕（前足）の裏に入る短い区間だけ |
| 虫眼鏡 | **動かさない** | レンズが顔に重なっており、ずらすと**顔の輪郭が描かれていない部分**が出る。代わりにレンズの内側だけを光が横切る（絵を切らずに済む） |
| 頭を傾ける | **やめた** | この絵に**首が無い。** 頭の裏に体が描かれていないので、傾けると首元に隙間が出る |
| 歩く・両手を挙げる | **無理** | その絵が存在しない。描き足しが要る |

### しっぽの切り方で詰まった点

**座標だけで「体側へ食い込ませる」と失敗した。** 背景の隙間へはみ出した切り口が、
回したときに体から離れて**細いトゲ**に見える（等倍の9倍に拡大して確認した時点で発覚）。

→ 食い込みを**体の形（不透明な画素）を14px 内側に縮めた範囲**で切り落とすようにした
（`cut-parts.py` の `OVERLAP_MARGIN`）。これで座標の精度に頼らず、絵の形そのものが境界を決める。

**回転軸は2か所に書いてある**（`cut-parts.py` の `PIVOT` と `MascotMotion` の `TAIL_ORIGIN`）。
片方だけ直すと付け根が浮くので、**一致を検査で縛った**（U-815）。

### 「動きを減らす」設定（新規対応）

**このリポジトリは一度も対応していなかった**（`app/globals.css` に一言も無く、既存の動き7種のうち5種が無限に繰り返していた）。

- `animation: none` で足りる。どの `@keyframes` も「素の見た目」を起点にしてあるので、止めれば素の見た目に戻る
- **例外は紙吹雪だけ。** 止めると画面の上端に貼り付いたまま残るので `display: none` で消す
- Playwright の `reducedMotion: "reduce"` で再現し、**コマ送り8枚が全部同一**になることを目で確認した

### ビルドサイズ

`origin/main` を別 worktree で素のままビルドし、同じ環境で比べた（どちらも2回ビルドして同じ値）。

| | 変更前 | 変更後 | 差 |
|---|---|---|---|
| ブラウザに配る JS | 947,258 B | 958,804 B | **+11,546 B（+1.2%）** |
| CSS | 325,532 B | 326,462 B | +930 B |
| 画像（`.next/static/media`） | 9,976 KB | 10,536 KB | +560 KB（部品3枚） |

**+11.5KB は新しいコードではない。** 内訳を追うと、`Mascot.tsx` が変更前は共有チャンク1つに
入っていたのが、変更後は**ルート別の4チャンクに複製されている**（Turbopack のチャンク分割の組み替え）。
**ライブラリは1つも増えていない**（配布物に `lottie` の文字列は0件）。

### テスト

`npm test` **708 passed / 63 skipped**（26ファイル。E10 で +7件）。`npm run lint` と `npm run build` も通る。

新しい7件が守っているのは「決めた形が保たれているか」だけ（見た目そのものは測れない）。

- **U-811 … 動きを足して「動きを減らす」設定への対応を忘れたら落ちる。** これが無いと、
  今回入れた対応が次の1件から静かに欠ける
- U-812 … その規則がどの層にも入っていない（層に入ると Tailwind の `animate-*` に負ける）
- U-814 … 使われない `@keyframes` が残っていない（`sniff` の置き換えで実際に必要になった）
- U-815 … しっぽの回転軸が、絵を切った位置と一致している

### 目視確認のやり方

一時的に `app/mascot-preview/page.tsx` を作り、**全部のアニメを止めて負の遅延でコマ送りにして**撮った
（`animation-play-state: paused` + `animation-delay: -Xs`）。**確認後に削除済み**（ビルドのルート一覧にも出ない）。

### 詰まった点・申し送り

- ⚠️ **E8（スマホ対応）が同じ `ProblemForm.tsx` を触っている。** 着手時点で未コミットの変更があり、
  作業中に E8 側は `958590f` まで進んだ。**マージのとき採点待ちのブロックで衝突する可能性がある**
  （E10 側の変更は「`<Mascot mood="thinking" className="w-36 animate-sniff" />` を
  `<MascotMotion motion="thinking" className="w-36" />` に替えた」1行 + コメント。取り込みは E10 側を採る）
- **E2E は回していない。** 別セッションが動いており、`fixtures.ts` が共有DBの回答ログを消すため
  （E2 のときと同じ理由）。動きに依存しているアサーションは0件と確認済み
- ⚠️ **ステージ選択の「現在地へ戻る」は「動きを減らす」設定を無視する。**
  JS の `scrollTo({ behavior: "smooth" })` なので CSS では止まらない（`components/stage/StageMap.tsx:381`）。
  E10 の担当範囲外なので触っていない → `残タスク.md` §E-3 に申し送り
- **`ideas/キャラクター設定.md` の「喜び＝両手を上げてジャンプ」という絵は存在しない。**
  実物の happy も虫眼鏡を持って立っている。**happy と thinking は顔以外1ピクセルも同じ**
  （実測: 画面の上52%より下の差分 0.00%）。おかげで**しっぽの部品を1つで使い回せた**
- Chrome が別セッションで使用中で MCP からつなげず、目視確認はプロジェクトに入っている
  Playwright を直接叩いて撮った
