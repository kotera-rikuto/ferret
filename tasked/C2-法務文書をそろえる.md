# C2. 利用規約・プライバシーポリシーをそろえる

**種別:** 文書作成 + 実装（判断が主）
**規模:** 中
**前提として終わっていること:** なし
**このタスクが終わらないと着手できないもの:** C5（本番デプロイ）

> ### ⚠️ 同時進行の注意
> `app/problems/[id]/ProblemForm.tsx` は E8・G2 も触る。着手前に `main` を取り込むこと。

**先に読むもの**
- `design/定番機能リスト.md` の B
- `ideas/仕様書.md` の §9.5（AI採点に関する取り決め）

---

## 何をするか

**「回答が外部のAIに送られます」と画面に書いてあるが、その根拠になる文書が存在しない。**

問題画面の下に常時この注記を出している。これは正しい対応だが、
**利用規約もプライバシーポリシーも無い状態**では、注記だけが浮いている。

必要な文書を用意し、画面に置く。

---

## なぜ必要か

- **個人情報（メールアドレス）を預かっている**
- **ユーザーが書いた文章を外部のAI事業者に送っている**
- 課金を始めるなら、さらに**特定商取引法に基づく表記**が必要になる

文書が無いまま公開すると、上記をどう扱うかの取り決めが無いまま
人のデータを預かることになる。

---

## ⚠️ オーナーに確認すること（このタスクは判断が主）

**文書の中身は、AIが勝手に書いて確定させてはいけない。**
ひな形の下書きまでは作れるが、**内容の確定には必ずオーナーの判断が要る。**

着手前に次を確認すること。

> 法務文書について、進め方を決めてください。
>
> **① どこまでAIが作りますか**
> - **案A: 下書きだけ作る**（おすすめ）
>   一般的な内容のひな形を作ります。そのうえで、**専門家の確認を受けてから公開**してください。
>   AIが書いた文章は、あなたの事業の実態と食い違う箇所が必ず残ります
> - **案B: ひな形サービスを使う**
>   規約作成サービスの出力を貼り込む形にします。この場合AIは画面への設置だけ行います
>
> **② 中身について、下の点を教えてください**（下書きに必要）
> - 運営者の表記（個人名 or 屋号）。**法人化前なので個人名の記載が必要になる場合があります**
> - 連絡先のメールアドレス
> - ユーザーが書いた回答文を、**サービス改善のために保存・分析してよいか**
>   （いまの実装では保存しています。問題の改善に使う想定）
> - **退会したときに、その人の回答データを消すか残すか**
>   消す場合はC3の退会機能の作りが変わります
>
> **③ いま作るのはどこまでですか**
> - 利用規約・プライバシーポリシー: **公開前に必要**
> - 特定商取引法に基づく表記: **課金を始めるときに必要**（いま作らなくてもよい）

---

## 手順

1. 上の確認で方針を決める
2. 文書を用意する（下書き or 貼り込み）
3. 画面を作る
   - `app/terms/page.tsx`（利用規約）
   - `app/privacy/page.tsx`（プライバシーポリシー）
   - 課金を始めるなら `app/legal/page.tsx`（特定商取引法に基づく表記）
   - **ログインが不要な画面にすること**（`proxy.ts` の保護対象に**入れない**）
4. 導線を置く
   - タイトル画面とログイン画面のフッターが定位置（`design/定番機能リスト.md` B）
5. 問題画面の注記から、プライバシーポリシーへリンクする
6. `npm run build` を通す

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `app/terms/page.tsx`（新規） | 利用規約 |
| `app/privacy/page.tsx`（新規） | プライバシーポリシー |
| `app/page.tsx` / `app/login/page.tsx` | フッターに導線 |
| `app/problems/[id]/ProblemForm.tsx` | 注記からのリンク |

---

## 終わったと言える条件

- [x] 方針についてオーナーの判断を得ている（2026-08-18・作業記録の表）
- [x] 利用規約・プライバシーポリシーの画面がある（`/terms` `/privacy`）
- [x] **ログインしていなくても読める**（`proxy.ts` は未変更・`I-310` で固定・ブラウザで実測）
- [x] タイトル画面・ログイン画面から辿れる（`I-399` で固定・クリックして実測）
- [x] 問題画面の「OpenAI に送信されます」の注記から辿れる（別タブで `/privacy`。E2E は未実行）
- [x] **AIが下書きした場合、「専門家の確認が必要」と申し送りに明記されている**
- [x] `npm run build` が通る（`/terms` `/privacy` は静的）

---

## 注意

- **AIが書いた文書をそのまま公開しないこと。** 下書きとして扱う
- 文言に**禁止されている言葉を使わない**（`CLAUDE.md` の文言ルール）。
  ただし法務文書は正確さが優先。衝突する場合は正確さを取り、その旨を報告する
- **保存しているデータの実態と文書を一致させること。**
  いま保存しているのは、メールアドレス（Supabase の認証側）、
  回答文、点数、採点の内訳、異議申し立ての理由。
  実装を確認してから書くこと（`ideas/db仕様.md` に一覧がある）

---

## 作業記録

### 2026-08-18 — 下書きと画面を作った（⚠️ 専門家の確認は**まだ受けていない**）

> ## ⚠️ この文書は公開前に専門家の確認が必要
>
> `app/terms/page.tsx` と `app/privacy/page.tsx` の本文は **AI が書いた下書き**で、
> 弁護士等の確認を受けていない。**C5（本番デプロイ）の前に確認を受けること。**
> 確認してもらうときに特に見てほしい箇所は、この節の最後「専門家に見てもらう点」にまとめた。

#### オーナーが決めたこと（2026-08-18）

| 論点 | 決定 |
|---|---|
| どこまでAIが作るか | **案A（下書きを作る）**。専門家の確認を前提とする |
| 回答文をサービス改善に使うか | **使うと書く**（実装が既に保存・分析している実態に合わせる） |
| 退会したときの回答データ | **全部消す**（残さない・匿名化もしない） |
| 今回の対象 | **利用規約とプライバシーポリシーの2本**。特商法の表記は課金開始（D2）と同時 |
| 運営者の表記・連絡先 | **小寺陸叶 / rikuto042700@icloud.com**（`lib/legal.ts`） |

#### 作ったもの

| ファイル | 内容 |
|---|---|
| `lib/legal.ts` | 運営者名・連絡先・制定日・委託先の一覧。**2つの文書が同じ値を参照する**ための定数 |
| `components/legal/LegalPage.tsx` | 文書2枚の共通の枠。体裁を `[&_h2]` などで一括指定し、条文側は素のタグだけ書く |
| `components/legal/LegalFooter.tsx` | 法務文書への導線。タイトル・ログインの下端（`design/定番機能リスト.md` B の定位置） |
| `app/terms/page.tsx` | 利用規約（全16条） |
| `app/privacy/page.tsx` | プライバシーポリシー（全12条） |
| `app/page.tsx` / `app/login/page.tsx` | フッターを追加。**中央寄せを崩さないため 1段目が余りを取る grid に変えた** |
| `app/register/page.tsx` | 「登録すると利用規約とプライバシーポリシーに同意したものとみなします」を登録ボタンの直下に追加 |
| `app/problems/[id]/ProblemForm.tsx` | OpenAI 送信の注記に「くわしく」を追加。**別タブ**で `/privacy` を開く |

**ログイン不要になっていることの確認:** `proxy.ts` は触っていない（`matcher` に足さないのが正解）。
`next build` の出力で `/terms` `/privacy` はともに **○（静的）**。

#### 実装と文書を突き合わせた結果

**文書は実装の実態に合わせて書いた。**「取得する情報」の一覧は `ideas/db仕様.md` と
`app/api/score/route.ts` を読んで作ったもので、次のものを列挙している。

- メールアドレス / パスワード（ハッシュ値のみ）
- 回答の文章、点数（層1・層2・合計）、4観点の判定と回答からの引用、講評、採点方式と版、
  `answer_hash`（同一回答の再送を再現するための照合用の値）、回答日時、トークン使用量
- 異議申し立て・問題の誤り報告に書かれた理由（`problem_feedback`）
- 経験値・プラン、配信事業者に残るアクセス記録

**書かなかったこと:** 決済情報（未実装なので「有償プランを開始した場合」の書き方に留めた）。

#### 判断したこと（理由つき）

1. **同意の一文は新規登録画面のボタン直下に置き、フッターの共通リンクとは別にした。**
   押す前に「押す操作の意味」が読める位置に置く必要があり、フッターでは役を兼ねられない。
   同じ画面にリンクを二重に置かないため、新規登録画面には `LegalFooter` を置いていない。
2. **問題画面の「くわしく」は別タブで開く。** 下書きは localStorage に残るので画面遷移でも
   消えないが、書きかけの人の画面を差し替えないほうがよい。
3. **住所は載せていない。** 個人事業主の自宅住所になるため。→ 下の「残っている宿題」参照。
4. **賠償の上限を「支払額、無償なら1万円」にした。** 「一切責任を負わない」は消費者契約法で
   無効になり得るので、故意・重過失を除外したうえで上限を置く形にしている。
5. **禁止語（`CLAUDE.md`）との衝突は無かった。** 「誤り」は既存UIの「問題の誤りを報告」と
   同じ用法（利用者の読み方ではなく問題データを指す）なので揃えた。
   `弱点` `間違い` `初心者` `失敗` `不足` などは1語も使っていない。

#### 確認したこと

- `npx eslint`（変更した10ファイル）… 指摘0件
- `npm test` … 単独で **571 passed**、**main（B3）を取り込んだあと 572 passed / 63 skipped**（新規2件を含む）
  - `I-310` matcher が法務文書の画面を含まない（`tests/integration/auth-routes.test.ts`）
  - `I-399` ログイン前の画面から法務文書へ辿れる（`tests/integration/architecture.test.ts`）
- `npm run build` … 成功。`/terms` `/privacy` は静的
- **ブラウザで実測**（worktree の dev サーバー :3210・デスクトップ1280 / モバイル390）
  - 5画面を描画。コンソールエラー0件。タイトル・ログインは**フッターを足しても中央寄せのまま**
  - 導線を実際にクリックして確認: `/` と `/login` の両方から2文書へ、文書どうしの往復、
    新規登録の同意文のリンク、いずれも到達した
- **E2E は書いたが回していない**（`tests/e2e/display.spec.ts` に `E-457`〜`E-460` を追加）
  理由: worktree に `.env.local` が無く、かつ E2E のフィクスチャは
  **他セッションと共有している DB の回答履歴と問題を書き換える**（`並列実行プラン.md` の申し送り）。
  **main へ入れたあと、他セッションが実測していないときに `npm run test:e2e` を回すこと。**
  `E-460`（問題画面の「くわしく」）はログインが必要なため、ブラウザでの実測もそこだけ未了

#### 残っている宿題（このタスクの外）

- [ ] **専門家の確認**（下記）。これが済むまで公開しない
- [ ] **退会機能（C3）を C5 より先に入れる。** 規約第12条とポリシー第7条が
      「退会したらデータを消す」と約束しているが、**退会の機能はまだ無い。**
      文書だけ先に公開すると、書いてあることが実行できない状態になる
- [ ] **住所の記載。** 特商法の表記（D2）でバーチャルオフィス等を決めるときに、
      プライバシーポリシーの公表事項もあわせて見直す
- [ ] **OAuth を有効化したとき（C4）に同意文の位置を見直す。**
      いまの同意文はメール登録のボタンの下にあり、Google / GitHub のボタン（準備中）は
      その上にある。OAuth を開けるなら、同意文をボタン群より前に出すか各ボタンの近くに置く
- [ ] `design/定番機能リスト.md` B の「利用規約・プライバシーポリシー」の
      「足りないもの」欄が**文書そのもの → 専門家の確認**に変わった

#### 専門家に見てもらう点（この5つは特に自信が無い）

1. **回答文の分析利用（規約第8条2項・ポリシー第2条）** — 利用者が書いた文章を
   サービス改善に使う旨の書き方。同意の取り方がこれで足りるか
2. **国外移転（ポリシー第4条）** — 委託先が全て米国事業者で、
   「本サービスの利用にあたり移転に同意するものとする」という書き方で足りるか。
   **各社のサーバーの所在地（リージョン）を実際に確認していない**。Supabase の
   プロジェクトのリージョンは要確認
3. **「回答が OpenAI のAIの性能改良に利用されない」（ポリシー第3条2項）** —
   API 経由の送信は既定で学習に使われないという理解で書いたが、**規約原文の確認が必要**
4. **賠償責任の上限（規約第13条3項）** — 無償利用者の上限1万円という設定
5. **13歳未満を利用不可とした点（規約第2条2項・ポリシー第10条）** —
   年齢の線引きと、18歳未満の親権者同意の書き方

### 2026-08-18 — main へマージし、票を `tasked/` へ移した

- `origin/main` へ push 済み（`d4229f6`）。push の前に **origin/main が2度先に進んでいた**ため、
  B3（採点基準）と E3・C4 を取り込んでからマージした。競合なし
- マージ後の状態で再検証: `npm test` **607 passed / 63 skipped**（20ファイル）、
  `npm run build` 成功、`npm run lint` 指摘0件
- **引き継ぎの置き場所を決めた。** 会話に残すと消えるので、次の3か所に書き分けた
  | 置き場所 | 何が書いてあるか |
  |---|---|
  | **`残タスク.md` §C-4 の「🚧 公開ゲート」** | **公開前に必ずやる3点**（専門家の確認 / 退会機能 / E2E の実行）。デプロイする人が最初に読む場所に置いた |
  | `残タスク.md` §C-2 | 何ができたか、判断の要約、住所を保留した理由 |
  | **本ファイル（この作業記録）** | 経緯・判断の理由・専門家に見てもらう点5つ |
- `tasks/README.md` の一覧・流れ図・ホットスポット表、`design/定番機能リスト.md` の
  参照先も移動先に合わせた（`proxy.ts` の行は「C2 は触らなかった」に直した。
  **法務文書をログイン必須にしないことが仕様**なので、次の人が足しに行かないように）
