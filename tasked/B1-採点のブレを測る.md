# B1. ゴールデンセット50件で採点のブレを測る

**種別:** 検証（測るだけ。直すのは B3）
**規模:** 大
**前提として終わっていること:** A1 が終わっていると材料が揃って望ましい（必須ではない）
**このタスクが終わらないと着手できないもの:** B3（判定基準の調整）、A2（問題の量産）

> ### ⚠️ 同時進行の注意
> **B2・B3 が動いている間は測らないこと。** どちらも採点の指示文を書き換える。
> **測っている最中に基準が変わると、出た数字が何を表しているのか分からなくなる。**
> 同じ理由で、**A1・A2 が問題を追加している最中も避ける**こと（対象が増えると比較できない）。
> `package.json` は E5 も触る。**着手前に `main` を取り込むこと。**

**先に読むもの**
- `ideas/採点システム_残課題.md` の §8・§11・§12（**必読**。測る内容の根拠）

---

## 何をするか

**同じ回答を出しても、採点の点数が毎回変わってしまう。** どのくらい変わるのかを50件で測る。

実際に起きたこと:

| 回答 | 1回目 | 2回目 | 3回目 |
|---|---|---|---|
| 「const だから再代入でエラーになります」 | 63点 | 71点 | 73点 |
| **「このコードはエラーが出ると思います」** | **29点** | **53点** | **29点** |

2つ目が問題。**24点も動いていて、53点は合格ライン（55点）のすぐ下。**
もう一度振れたら、同じ回答が合格になってしまう。

---

## なぜ必要か

**直す前に測る。** 測らずに直すと、悪化しても気づけない。

いま「たぶんここが原因だろう」という心当たりはあるが、確証がない。
50件の決まった回答を用意しておけば、**採点の仕組みを触るたびに同じ問題を解かせて、
良くなったか悪くなったかを数字で比べられる。**

費用は **1周およそ2円**（50件の採点で）。何度でも回せる。

---

## 用意する50件の内訳

| 種類 | 件数 | 満たすべき条件 |
|---|---|---|
| クリアするはずの回答 | 15 | — |
| 不合格のはずの回答 | 15 | — |
| **結論が逆の回答** | 10 | **1件も合格してはいけない** |
| └ うち**数字だけが違うもの** | 3件以上 | **必ず含めること**（下記） |
| **短いが正しい回答** | 10 | **全部合格しなければいけない** |

### 「数字だけが違うもの」を必ず入れる理由

`ideas/採点システム_残課題.md` §11 の対策が効いているかを確かめる唯一の方法。

結論が逆の回答を見つける仕組みは、**回答の中から証拠になる部分を引用して照合する**という作り。
ところが引用が `900` や `20` のように短い数字だけだと、照合の下限（4文字）に届かず**素通りする。**
対策としてAIに「数字だけを引用しないように」と指示したが、
**指示に従っているかは一度も測っていない。**

従っていない場合、**結論が逆なのに60点で合格してしまう。**

対象になりやすいのは、答えが3文字以下の数字になる問題。
既存問題では `900` / `A-3` / `20` が該当する。

---

## 手順

1. 上の内訳に沿って50件の回答文を用意する
   - 既存6問（`order` 1〜5 と 999）に対する回答として作る
   - **`ideas/採点システム_残課題.md` §7 に、動作確認用問題の期待値の一覧がある。まずそれを使う**
2. 保存する場所を決めて置く（例: `tests/golden/` に JSON）
   - **⚠️ 置き場所と実行方法は下の「オーナーに確認すること」を参照**
3. 全件を採点にかける
4. 下の数字を出す

### 出す数字

| 指標 | 目標 |
|---|---|
| 結論が逆の回答の**合格率** | **0%** |
| 短いが正しい回答の**合格率** | **100%** |
| 同じ回答を5回採点したときの**合格・不合格が入れ替わる率** | 低いほどよい（初回測定） |
| 全体の合格率 | 60〜70% |

### あわせて測るもの（§12）

箇条書きで答えた回答が、**不正な回答と誤判定されていないか。**

「順番に答えてください」型の設問は箇条書き（`1. 〜` `2. 〜`）を誘発する。
このとき引用の照合が外れやすく、3つ以上外れると**正しい回答が「ズルをした」扱いになって減点される。**

`user_attempts` の `axes` に記録が残っているので、設問の形式ごとに発生率を分けて見る。

---

## ⚠️ オーナーに確認すること ── ✅ **着手前・測定後とも確認済み（2026-08-17）**

> 着手前: 実行方法は**案B（専用コマンド）**、採点の経路は**採点関数を直接呼ぶ**。
> 測定後: **B3 は候補1から着手・候補2 とは同時に入れない**、**回答文は git 管理外のまま**。
> 詳細は末尾「作業記録 → 決めたこと」の①②⑦⑧。

**着手前に1つ、終わってから1つ。**

### 着手前

> 50件の回答をどこに置き、どうやって流すかを決めたいです。
>
> **案A: テストの一部にする**（`npm test` で一緒に走る）
> - よい点: 忘れずに実行される
> - 悪い点: **1回2円かかる採点が、テストのたびに走ってしまう。** 普段のテストが遅く・有料になる
>
> **案B: 手で流す専用のコマンドにする**（例: `npm run golden`）
> - よい点: 流したいときだけ流せる。普段のテストは今までどおり速い
> - 悪い点: 流し忘れる可能性がある
>
> **おすすめは案B**です。採点の仕組みを触ったときだけ流せばよく、
> 毎回のテストに実費と待ち時間を持ち込まずに済むためです。

### 測り終わってから

**数字が出ても、その場で直さないこと。** 直すのは B3。

> 測定が終わりました。（結果の要約）
> このうち○○が目標を下回っています。原因の候補は（A / B）です。
> どちらから手をつけますか。

---

## 触る場所

| ファイル | 何をするか |
|---|---|
| `tests/golden/cases.json`（新規） | 50件の回答文。**git 管理外** |
| `tests/golden/golden.test.ts`（新規） | 測定と集計。公開する |
| `vitest.golden.mts`（新規） | 専用の実行設定 |
| `package.json` | `npm run golden` を追加 |
| `.gitignore` | 回答文と測定結果を除外 |
| `ideas/採点システム_残課題.md` §8 | 測定結果を追記 |

**`lib/ai/` は触らない。** ここは測るだけのタスク。

---

## 終わったと言える条件

- [x] 50件が用意され、リポジトリに入っている
      → `tests/golden/cases.json`。**ただし git 管理外**（決定③の理由。`/problems/` と同じ扱い）
- [x] **「数字だけが違う回答」が3件以上含まれている** → 4件
- [x] 全件の採点が済み、上の4つの数字が出ている
- [x] 箇条書き回答の誤判定率が出ている → 5件で格下げ0件（誤爆なし）
- [x] 結果が `ideas/採点システム_残課題.md` §8 に記録されている
- [x] **仕組みのコード（`lib/ai/`）を変更していない** → `git diff` で確認済み

---

## 注意

- **費用が発生する。** 1周2円程度だが、何度も回すなら残高を確認しておくこと
- 採点は1回あたり1〜4秒かかる。50件を一気に流すと、使いすぎを防ぐ制限
  （1分10件 / 1時間60件）に当たる。**間隔を空けて流すこと**
- 同じ回答を再送すると、前の結果をそのまま返す仕組み（保存された結果の再利用）がある。
  **ブレを測るには、これを避けて毎回本当に採点させる必要がある。**
  回答文をわずかに変えるか、再利用を無効にする手段を用意すること

---

## 作業記録

**2026-08-17 実施（ブランチ `task/B1`）。測定は完了。`lib/ai/` は1行も触っていない。**

### 作ったもの

| ファイル | 内容 | 公開 |
|---|---|---|
| `tests/golden/golden.test.ts` | 測定・集計・レポート出力 | ✅ する |
| `tests/golden/cases.json` | 50件の回答文 | ❌ **git 管理外** |
| `tests/golden/results/` | 生データ（JSON）とレポート | ❌ **git 管理外** |
| `vitest.golden.mts` | 専用の実行設定 | ✅ する |
| `package.json` | `npm run golden` を追加 | ✅ する |

```bash
npm run golden                                  # 1周 = 90回 / 約¥3.6 / 約40秒
GOLDEN_ONLY=G05 GOLDEN_FLIP_RUNS=2 npm run golden   # 1件だけ試す（B3 用）
```

`npm test` の設定（`vitest.config.mts`）は `tests/unit` と `tests/integration` しか収集しないので、
**`tests/golden/` は普段のテストでは走らない**（＝課金されない）。

### 結果 ── **タスクが定めた合格ライン2本はどちらも達成**

| 指標 | 目標 | 実測 | |
|---|---|---|---|
| 反転誤答の**誤合格率** | 0% | **0%（0/10）** | ✅ |
| 短いが正しい回答の**合格率** | 100% | **100%（10/10）** | ✅ |
| 想定との一致率（50件） | — | **96%（48/50）** | |
| 合否が入れ替わる率（同一回答5回・閾値付近10件） | 低いほどよい | **10%（1/10）** | 初回測定 |
| 点数の振れ幅（同上） | — | 最大 **32点** / 平均 11.4点 | |
| パーフェクト帯（80点以上） | 目安 15〜25% | **46%（23/50）** | ⚠️ |

**このセットの合格率は 54%。** 50件のうち25件を誤答で固めているので構成上の期待値が50%であり、
**残課題 §8 の「クリア率 60〜70%」はこのセットには当てはまらない**（あれは実利用者の分布の話）。
指標として置き換えたのが「想定との一致率」。

### 想定と外れたのは2件。**どちらも「不合格のはずがクリアした」**

- 揺れているのは `core` だけで、`full`(48点) と `partial`(24点) を往復している。
  **`core` の1段が24点なので、1段動くだけでクリア閾値55を越える。**
- 2件のうち1件は**5回すべてクリア**した。揺れではなく判定基準そのものの問題で、こちらのほうが重い。
- **原因の見立て:** `core` が「回答の内容が正しいか」で付いていて、
  「その問題の `core` の条件を満たすか」で付いていない。
  残課題 §8 が挙げていた心当たり（「回答が短いことは減点の理由になりません」が効きすぎ）は裏付けられた。

> **回答文と点数の対応は `ideas/採点システム_残課題.md` §8「実測 1回目」に書いた。**
> このファイルは公開リポジトリに入るので、**どう書けばクリアできてしまうかを新たに書かない**
> （`.gitignore` が `/problems/` と `残課題.md` を外しているのと同じ理由）。

### 測るのが目的だった2件は、どちらも**再現しなかった**

- **§11（数値だけが誤っている反転誤答が veto を素通りする）** … 4件入れて4件すべて矛盾検出・不合格。
  プロンプトでの縛りは効いている。**加えて §11 の症状の記述自体が古い** ──
  引用照合が取れない側の分岐にも層1の頭打ちが入ったので、上限は50点でクリアに届かない
- **§12（箇条書きが捏造検出を誤爆させる）** … 箇条書き5件で格下げ0件。誤爆なし。
  `FABRICATION_DEMOTE_THRESHOLD` を触る必要は無い

**B3 でやることが1点に絞れた**（`core` の判定基準）。上の2件は B3 の対象から外してよい。

### 決めたこと

| | 決定 | 理由 |
|---|---|---|
| ① 実行方法 | **専用コマンド `npm run golden`**（オーナー判断・案B） | 1周¥3.6 の実費を普段のテストに持ち込まない |
| ② 採点の経路 | **`scoreAnswer` を直接呼ぶ**（オーナー判断） | API 経由だと同一回答の再利用が働いて**ブレそのものが測れない。** 回数制限（1分10件）も回避できる |
| ③ 回答文は git 管理外 | **`cases.json` を `.gitignore` に追加** | 10問ぶんの正答と「どう書くと落ちるか」の一覧＝`/problems/` と同じ性質。リポジトリは Public |
| ④ 回答文を **JSON** にした（TS ではない） | `tsconfig.json` が `**/*.ts` を型検査するので、**公開コードから非公開の `.ts` を import すると、クローン直後の `next build` が落ちる**（＝Vercel デプロイが壊れる）。JSON なら実行時読み込みで済む |
| ⑤ 繰り返しは閾値付近10件だけ | 全50件×5回は¥10。**合否が入れ替わるのは閾値付近だけ**なので、1周目の点が55に近い順に10件選ぶ（オーナー判断・約¥4の案） |
| ⑥ `problems.context` は採点器に渡さない | **本番がそうしているから。** 実行結果は問題画面にしか出しておらず、`/api/score` の select に `context` が無い。ここで足すと本番より材料の多い条件で測ることになる |
| ⑦ **B3 は候補1（判断例を1行追加）から。候補2 と同時に入れない**（オーナー判断・測定後） | **同時に直すと、数字が動いてもどちらが効いたのか切り分けられない。** 悪化した場合も同じ。`残課題` §8「直す候補」に手順を書いた |
| ⑧ **回答文は git 管理外のままにする**（オーナー判断・測定後） | リポジトリが Public で、10問ぶんの模範解答がそのまま読める状態を避ける。代償として**別の端末では `cases.json` を手で持ち込まないと `npm run golden` が流せない**（無い場合は明示的なエラーで止まる） |

### 詰まった点・申し送り

1. **採点器は `problems.context`（実行結果）を見ていない。** 影響型（order 59）は
   スタックトレースを学習者にだけ見せて採点している。模範解答とルーブリックに
   ログの内容が書かれているので採点は成立しており（模範解答に近い回答は94点）、
   **不具合ではなく設計の選択に見える。** ただし「実行結果の何行目を手がかりにしたか」を
   問う設問なので、**E4 か B3 でどちらが正なのか一度決めるとよい。**
   ゴールデンセットは本番に合わせて渡していない（決定⑥）
2. **フィードバックがテンプレートに差し替わった率が 40%。** ただし20件すべて42点以下の低得点帯で、
   `praise` が空文字列になったケース。「褒めるところが無ければ空にする」という指示どおりの動作で
   NGワード違反ではない。**E2（2枠表示）を作るときに「よかったところ」が空の見せ方が要る**
3. **パーフェクト帯 46%** は目安（15〜25%）の倍以上。短い正答が軒並み80点台に乗る。
   `ground` が付きやすいのが原因で、A1 が観察した「order 78 の短い正答が92点」は
   1問固有ではなく**全体の傾向**だった。合否は正しいので B3 の優先度は `core` の次
4. **lint が `.claude/worktrees/` の複製まで走査して 594 errors 出る。** F3 の worktree が
   存在する間だけの現象で、`eslint.config.mjs` の `globalIgnores` に入っていないため。
   B1 の変更分（`npx eslint tests/golden vitest.golden.mts`）は0件。**F3 のマージ後に消える**
5. **`tests/golden/results/` には測定1回ぶん（`2026-08-17T09-05-47...`）だけを残した。**
   配線確認で `GOLDEN_ONLY` を使った2回ぶんは削除済み（絞った実行の数字を
   測定結果と取り違えないため）。git 管理外なので消しても差し支えない
6. **⚠️ 着手時に worktree を切らなかった。これは手順違反。** `並列実行プラン.md` は
   「1タスク = 1ブランチ = 1 worktree」と `git worktree add` のコマンドまで書いているのに、
   `git checkout -b task/B1` だけで始めてしまった。結果、**B1・C1・E7 の3タスクが
   `/Users/rikutokotera/Desktop/Ferret` を共有し、未コミット変更が混在した。**
   C1 セッションの `git checkout -b task/C1` で HEAD が `main` に戻り、
   B1 のブランチも維持できていない。**次以降は着手の一番最初に worktree を切ること。**

   **立て直し済み（オーナー判断）。** `.claude/worktrees/B1` を `main` から切り
   （`git worktree add` は共有ツリーの HEAD もファイルも動かさないので他タスクに影響しない）、
   B1 の6ファイルだけを移してコミットした。worktree 単独で
   **558 passed / 型検査クリーン / ビルド成功**（他タスクの作業に依存していないことの確認）。
   - `残タスク.md` は**共有ツリー側に E7 の編集が混ざっていた**ので丸ごとコピーせず、
     `main` の版に B節と「進める順番」の変更だけを当て直した（差分4ハンク）
   - **共有ツリー側では `.gitignore` の変更だけ残してある。** 戻すと
     `tests/golden/cases.json`（模範解答相当）が未追跡ファイルとして見え、
     他セッションの `git add -A` で Public リポジトリに入りかねないため
   - `cases.json` と `results/` は git 管理外なので**共有ツリーと worktree の両方に実体がある。**
     worktree を消す前に、共有ツリー側に残っていることを確かめること
